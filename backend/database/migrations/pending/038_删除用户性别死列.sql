-- FP-28c（`用户.性别` 死列处置三步链的**最后一步**）：正式删除 `用户`.`性别` 列
--
-- FP-28d 登记（2026-09-23，第六轮裁定②；**本迁移暂捏，已移出自动迁移链**）：
--   原因 = 管理端（`恋爱吧管理中心` 仓）**已提交版本**仍按名读 `用户.性别`（`SELECT u."性别"`），
--   而 backend/entrypoint.sh 会在容器每次启动时自动重放 backend/database/migrations/ **顶层**链 ——
--   038 若留在顶层，下一次容器重启就会在管理端上线前把该列 DROP，管理端读点当场 42703 打挂。
--   处置 = 本文件移入 backend/database/migrations/pending/（**文件名与内容除本登记外一字未改**）：
--     迁移器.ts:81-86（readdirSync + extname === '.sql'）与 run_migration.js:25-31
--     （readdirSync + endsWith('.sql')）均为**顶层非递归**扫描，`pending/` 目录名不以 .sql 结尾
--     ⇒ 不进自动迁移链；各守卫的 readdirSync 同口径。
--   同步 = database/000_baseline.sql 与 backend/database/init.sql 对 `用户`.`性别` 的删列
--   **成对回退**（两文件头注释的 28c 删列声明改判为 28d 登记：列暂时保留），
--   四本守卫（FP22f / 迁移037 / FP28b / 迁移台账校验和）同步改判。
--   放行条件（缺一不可，且**禁止自动回移**）：
--     ① 管理端 FP-28a 已上线到目标环境、并确认无 `用户.性别` 读者；
--     ② 本文件从 pending/ 移回 migrations/ 顶层（文件名不变）；
--     ③ 主库完成一次可用物理备份（本迁移破坏性 DROP，见下方「删除语义」）；
--     ④ 由运维**显式**执行 run_migration（容器启动的自动链路不得承担本次放行）。
--   证据：.agents/evidence/traces/FP-28d放行条件-20260923.md
--
-- 三步链与分工（本迁移只承担第三步，前两步已并回主树）：
--   28a 管理端（`恋爱吧管理中心` 仓）的用户性别读写点全部改吃 `用户.默认性别`
--       （该条转述自工作单：跨仓产物不在本 worktree 内，本单无法独立复核，也不依赖它才成立——
--         本仓侧的消费者归零由下面的 登记三 逐点实测）；
--   28b 后端摘除本仓对该列的**最后一个读点与最后一个写点**：
--       读点 backend/src/services/认证.ts::yingSheYongHu 的 `xing_bie` 出参映射已删
--         （出参键集 20 → 19，由 backend/src/routes/__tests__/FP08认证信息出参能力位.test.ts 逐键钉）；
--       写点 backend/src/services/账号注销.ts 匿名化 UPDATE 的 `"性别" = NULL` 已删
--         （赋值项 16 → 15，由 backend/src/services/__tests__/FP28b注销不写性别死列.test.ts 逐列钉）；
--       真库夹具 FP09/FP10 的 INSERT 列清单同步由 `"性别"` 换成 `"默认性别"`。
--   28c（本文件）删除列本身，并同步 database/000_baseline.sql 与 backend/database/init.sql
--       的建表定义（两方口径由 frontend/src/__tests__/FP22fSQL列集合三方一致.test.ts 守）。
--
-- 登记一（对既有迁移注释的**事实更正**，不可回改只能在此登记）：
--   `backend/database/migrations/037_用户默认性别.sql` 第 23 行的注释口径
--   「`性别` 列后端只有 注销 时置 NULL（services/账号注销.ts:97），全库未找到写入者」
--   **自 FP-28b 起已失效** —— 该 `注销置 NULL` 写点连同 `xing_bie` 出参一起被摘除，
--   本列自 28b 起在本仓**既无读者也无写者**。037 是已应用迁移，字节级不可改
--   （`run_migration.js` 对内容漂移直接抛错终止迁移，PROGRESS L-09），故只能在本文件的头注释里更正，
--   并同步改判 037 的守卫用例（`backend/scripts/__tests__/迁移037用户默认性别.test.ts` 的列清单基准
--   已由 `性别` 换到 `目标性别`，FP-28b 完成）。
--   037 头注释里「同族三列不同值域」的论证同样只到 28b 为止：本列删除后同族只剩两列。
--
-- 登记二（**存量值分布实测**，删列的数据损失面，用户裁定要留档）：
--   实测环境 = 自建临时克隆库 `fp28c_probe_*`（`pg_dump -t 用户` 只读导出 → 灌入临时库统计，
--   afterAll 之前由本次会话显式 DROP；`lovewithme` 主库全程零 DDL/零 DML）。
--   `用户` 总行数 11,849；`性别` 列 NULL 11,848 行（99.99%）、非 NULL 1 行。
--   那唯一 1 行的值是 `'nv'`（2 字节）—— 与 `默认性别` 的 male/female 值域不符的旧写法，
--   属迁移 024「内部规范形态 nan/nv」那套口径的历史残留（024 只洗 `角色.性别`，未纳入 `用户.性别`）。
--   与原报告（管理端工人实测 11,848 NULL + 1 `'nv'`）**逐位一致**，独立复核成立。
--   该行同族列现值：`默认性别` = NULL，`目标性别` = '女'，注册时间 2026-09。
--   ⇒ **留档裁定：不需要单独导出留档**。理由三条，均为实测事实：
--     ① 信息未损失：同一行的 `目标性别` 已是 '女'，`'nv'` 所表达的「女」在同一行里有等价载体；
--     ② 语义本就不属于本列值域：`'nv'` 不在 `默认性别`/前端资料向导使用的 male/female 口径内，
--        任何按 male/female 解读本列的消费方都会把它读成「未知」，删列不改变可观测行为；
--     ③ 本列自 28b 起消费者为 0（见登记三），不存在"删掉就少一份数据可看"的读取路径。
--   需要追溯时，凭「11,849 行中恰 1 行 `'nv'`、该行 `目标性别`='女'、2026-09 注册」这三条指纹
--   在任何一份 038 之前的物理备份里可唯一定位，故不需要在仓库内保存行数据（仓库内也不该存 PII）。
--
-- 登记三（**剩余消费者 = 0 的契约证据**，删列的安全性论证）：
--   本仓全量 grep（backend/ + frontend/src/ + e2e/ + database/，排除 node_modules）后，
--   对 `用户`.`性别` 的显式标识符引用只剩：本迁移（DROP）、两份建表脚本的**删列后**状态（不存在）、
--   以及守卫用例里作为「不得回流」反证目标的字面量。`角色`.`性别`（NOT NULL，迁移 013/024 的口径）
--   与 `挑战对局`.`玩家性别`/`对象性别` 是**不同的表/不同的列**，不在本单处置范围内，一律不碰。
--   读侧逐点实测（`FROM "用户"` 的全部 20 余处）：
--     ① 显式投影（middleware/管理员.ts:97、routes/好友.ts:52、routes/用户设置.ts:77、routes/管理员.ts:346、
--        routes/资料.ts:260、services/可见性.ts:71、services/管理员.ts:108 …）逐条不含 `性别`；
--     ② `SELECT * FROM "用户"` 只有 3 处（services/认证.ts:158 登录、:167 /信息、services/管理员.ts:247
--        测试号登录），三处一律只把行喂给 `yingSheYongHu`，而该函数是**19 键白名单**、不读 `row.性别`
--        （白名单性质由 FP08认证信息出参能力位.test.ts 的「FP-28c 对抗面」用例实测钉住：
--        人为让行里带 `性别` 时，出参仍不得出现 `xing_bie`，且键数恒为 19）；
--     ③ services/管理员.ts:268 的 `DELETE ... RETURNING *` 行未回传客户端（只回 cheng_gong）。
--   写侧：唯一曾是写点的 账号注销 已随 28b 摘除（FP28b 用例的 15 列清单 + 逐列存在于 baseline 的看门狗）。
--   同名陷阱登记：e2e/helpers/角色.ts:56 的 `用户性别` 是 `/生成角色/MBTI生成` 的**HTTP body 字段**，
--   本仓 backend/src 内对它的读者数为 0（grep 无命中），与该数据库列无关，本单不碰也不该顺手"修"。
--   库侧依赖实测（同一临时克隆库）：`pg_depend` 对 `用户.性别` 的非依赖型（deptype<>'a'）记录 **0 条**，
--   `pg_indexes` 的 用户 索引 6 条中无一条覆盖该列，`pg_views`/`pg_proc` 中无任何定义引用该列
--   ⇒ DROP 不级联、不破坏索引/视图/函数，也不需要重建任何对象。
--   前端 `Yonghu` 类型（frontend/src/types/index.ts:58）与后端 `用户` 出参（types/index.ts:16）
--   自 28b 起都不再有 `xing_bie` 键 ⇒ 线上 JSON 契约不因本迁移发生变化，无版本协商需要。
--
-- 登记四（**psql 多路径实测**，全部在自建临时库，主库 `lovewithme` 零 DDL/零 DML）：
--   库名 fp28c_base_* / fp28c_chain_* / fp28c_legacy_* / fp28c_new_* / fp28c_old_* / fp28c_tb_*，
--   验证完毕全部 DROP，收工实测 `pg_database LIKE 'fp28c_%'` 计数 = 0。
--   ① baseline 直建（不跑迁移）19 列；baseline + database/001 + 全迁移链（跳过 034）23 列。
--      两者的差集**恰好**是 `审核员 / 签名可见性 / 签名白名单 / 运营` 四列 —— 这是 FP-22f 早就登记的
--      「014/022 早于 baseline 快照」既有分叉，**与 038 无关，且 038 未使其扩大**（反向差集为空）。
--      ⇒ 需要纠正工作单的一处前提：「baseline 直建」与「baseline + 全链」两条路径的用户表列集合
--      在 038 之前就已经不相同（20 列 vs 24 列，差的正是这 4 列），本单据此把验收改判为下面 ② 的同一条判据。
--   ② 真正的收敛不变量（实测成立）：**038 之前的建表形态（手工加回 `性别`）+ 全迁移链** 与
--      **当前 baseline + 全迁移链** 两条起点，重放后 用户 列集合**逐列完全相同**（23 列，两边均无 `性别`）。
--   ③ 幂等：`038` 在三条路径上各跑 2~3 遍全部 EXIT=0；无列时报 `NOTICE: column "性别" of relation
--      "用户" does not exist, skipping`（IF EXISTS 的预期行为，不是错误）。
--   ④ `SELECT "性别" FROM "用户"` 在全部临时库上均为 `ERROR: column "性别" does not exist`（42703）；
--      反向对照 `目标性别`/`默认性别`/`图片授权`/`角色.性别` 四者在位、`用户` 索引仍 6 条 ⇒ 未牵连。
--   ⑤ 主库现状（只读 DQL 实测）：`用户` 仍 24 列且**含** `性别`，`schema_migrations` 只登记到 034
--      ⇒ 035/037/038 尚未在主库落地。本迁移**不由本单在主库执行**；它会在下一次容器启动
--      （backend/entrypoint.sh:16 重放迁移链）或运维显式 `run_migration` 时才真正 DROP。
--      因 ① 本迁移是破坏性的、② 主库当前仍带着那唯一 1 行 `'nv'` ⇒ 落地前须先有可用物理备份。
--
-- 为什么只能新增本迁移、不能回改 037/baseline 时代的任何文件：见 PROGRESS L-09（已应用迁移字节级不可改）。
--
-- 删除语义（显式定案，与 037 的「只前进不回滚」同族但**方向相反**，故单独写明）：
--   ① 本迁移是**破坏性**迁移：DROP COLUMN 会连带丢弃该列的全部历史值（实测即上述 1 个非 NULL 值）。
--      执行前镜像须有可用物理备份；本文件不提供、也不得自动提供任何回退路径。
--   ② 无 DOWN 脚本（与 032/033/035/037 同族，库内本来就没有回滚机制）：撤销只能由人工显式
--      `ALTER TABLE "用户" ADD COLUMN "性别" VARCHAR(10)`，且历史值不可恢复。
--   ③ 幂等：`DROP COLUMN IF EXISTS` —— 列已在位的库执行一次即删除，第二次及以后整条语句跳过、零变更；
--      由 baseline 直接建出的新库根本没有该列，首启重放本迁移同样零变更。两条起点各跑两遍的实测
--      由 backend/scripts/__tests__/迁移037用户默认性别.test.ts（现网路径重放全链）与本单 psql 双路径验证共同覆盖。
--   ④ 只触碰 `用户` 表的这一个列：不改任何其它列的类型/约束/默认值，不 DROP TABLE/INDEX/CONSTRAINT，
--      不写任何 UPDATE/DELETE，不触碰 `用户` 以外的表。
--
-- 值域字面量: 无（本迁移不引入任何取值，只删列；列删除后本文件与任何按名读写该列的代码一律 42703）

ALTER TABLE "用户" DROP COLUMN IF EXISTS "性别";

# FP-18 游戏端角色同源解析 —— 收口证据（2026-09-19）

范围：`和我恋爱吧`（游戏端）。基准 = FP-17 扩位后的五位能力矩阵（`恋爱吧管理中心/管理后端/src/中间件/管理员.ts::角色能力矩阵`，只读）。
本轮职责：收绿 + 说清每一红归因 + 六条审计逐项落地。

---

## 一、红灯归因（实测与派发提示词的差异，先纠前提）

派发单给的「当前失败实测 = backend 2 files/4 tests、frontend 1 file/2 tests，且 5 处红的归因」与本轮开工时的**实测不符**。以实测为准：

| # | 实测红灯 | 派发单给的归因 | 实测归因（证伪/证实） |
| -- | -------- | -------------- | -------------------- |
| 红1 | `FP18角色同源解析.test.ts:274` `expected [ Array(3) ] to deeply equal [ 'cha_kan' ]` | 「跨仓能力同源守卫仍按旧 `['cha_kan']` 断言」 | **归因错位**：跨仓守卫 `backend/src/utils/__tests__/角色能力同源.test.ts` 本轮开工时**已绿**（它已按五位矩阵逐角色全等）。红灯在该 FP18 文件里，是把「运营 = 只有 cha_kan」写死的**旧二位模型断言**，实现（`yingSheYongHu` 按五位矩阵投影 `['cha_kan','feng_jin','tong_ji_xie']`）才是对的 |
| 红2 | 同文件静态守卫 `写路由未挂高危门禁: luYou.post('/账号封禁/解封')` | 「FP-18 半成品丢了门禁，真实安全缺口，必须补挂」 | **前提不成立**：该路由从未丢门禁，挂的是 `guanLiFengJinMenKong`（`feng_jin`）。失败原因是守卫用 `头部.includes('guanLiGaoWeiMenKong')` 表达「每条写路由都得 gao_we」＝**FP-17 之前的二位口径**，与 `routes/管理员.ts:528` 的正确实现互斥。未改断言让它闭嘴，而是把守卫改成「逐条钉死挂哪一位」后转绿 |
| 红3 | 同文件 `'/账号封禁/解封:200' to be ':403'`（运营写矩阵用例） | 同上，被并列为安全缺口 | 运营按 FP-17 **合法持有** `feng_jin` ⇒ 200 是新口径的**正确结果**；错在该用例把 解封/申诉审核 与 gao_we 专属写混进同一张「高危端点」表。改成由矩阵推导逐条期望后转绿 |
| 红4/红5 | `frontend/src/__tests__/用户.test.ts` 两条：`expected undefined to be false/true`（`shiFouGuanLiYuan`） | 「`过往战绩分享.test.ts` 抛 `生成战报海报失败`，12:00 外部改动导致 FP-10 调用点漂移」 | **文件都不是同一个**：开工时 `过往战绩分享.test.ts` 25 例**已全绿**（12:11 已有人把该测试随 12:00 的 `过往战绩.vue` 重设计同步过，含 L-38 的 fake-timers 改造）。真红在 `用户.test.ts`：FP-18 已删 `shiFouGuanLiYuan`，该测试未同步 |
| 追加（全量阶段才出现） | 全量跑时 `过往战绩分享.test.ts > 卸载时释放未及回收的 blob 地址并停掉提示定时器` 单条失败（单文件跑 25/25 绿） | 未列 | **L-38 的同一用例换了触发条件**：`URL.revokeObjectURL` 是全局桩，桩返回的 blob 地址是常量 `'blob:fp10-hai-bao'`，别的实例里未跑完的待发回收定时器在推进时钟时会打到本实例的计数桩上 ⇒ 恰好 2 次。修法=每实例唯一地址 + 计数只算本实例那条地址（断言强度不降，见 §4-M4） |

### 12:00 外部改动（`frontend/src/views/过往战绩.vue`）判定

`git diff HEAD` 显示该文件相对基线 +613/-159，其中含 FP-10 的两个调用点：`shengChengZhanBaoHaiBao`（`:1003`）、三级降级出口（`:956` `wenChuFenXiang`）、`.caozuo-anniu.fenxiang` 规则（`:1937`）**均仍在原位且测试绿** ⇒ **不是「我们的调用点被移走/改名」**。
外部改动自身的可见变化：分享按钮配色由 FP-10 的令牌取值改为硬编码 `background:#ffd500` / `color:#14141a`，并把 `过往战绩分享.test.ts` 的样式用例改成断言这两个字面量（`:756-757`）。
⇒ 判定：**外部作者自己的设计选择**（与该文件的 `.caozuo-anniu.jixu` 硬编码渐变同风格），非缺陷、非 FP-10 契约被破坏。**按约束未回退、未覆盖、未替其改设计**；仅登记为观察项（见 §6-R1）。

---

## 二、FP-18 六条审计逐项

### ① 服务端为唯一授权权威；前端派生只是视图门；不读令牌载荷；能力未知 fail-closed

- 授权判定唯一入口：`backend/src/middleware/管理员.ts::anYongHuIdQuJiaoSe`（`SELECT "管理员","运营","审核员" FROM "用户" WHERE "ID"=$1`）→ `角色具备能力(查库角色, 位)`；`创建能力门禁(位)` 是唯一门禁工厂。
- 令牌载荷：`backend/src/utils/jwt.ts` 静态守卫断言其源码不含 `jiaoSe|guanLi|guan_li_yuan|nengLi`（`FP18角色同源解析.test.ts` 末条）。前端全库 `atob|jwt-decode|base64` 解码令牌的读法：0 命中。
- 客户端申报零生效：`yingSheYongHu` 只读三旗标列；门禁只读 `qingQiu.yong_hu`（由 `renZhengZhongJianJian` 从令牌解出的 yongHuId）再查库。回归：`令牌载荷伪造超管与请求体申报角色都不生效`、`门禁不读请求体里的角色申报`、`行里混入客户端申报的 jiao_se/neng_li 也不参与推导`。
- fail-closed：门禁 catch 一切 → 500 通用文案（不放行）；`取管理角色` 只认 `=== true`（`null`/`'1'`/`'true'`/缺列一律无角色）；前端 `归一管理角色`/`归一管理能力列表` 白名单过滤，非白名单值与 `undefined` 一律丢弃。
- **本轮补的漏**：`frontend/src/utils/角色能力.ts` 的 `GuanLiNengLi`/`管理能力清单` 只有 `cha_kan|gao_we` 两位，而服务端已下发五位 ⇒ 三位被**静默丢弃**（安全方向没错，但未来任何按 `feng_jin`/`tong_ji_xie` 设的视图门永不亮且无红灯）。已扩为与后端逐字同集合，并由新增的 `frontend/src/__tests__/角色能力同源.test.ts` 直读后端源文件把守。
- **本轮查出并纠正的引用幻觉**：该文件注释写「同源由 `__tests__/角色能力同源.test.ts` 把守」，而该测试此前**不存在**。现已存在。

### ② 6 处二值判定逐个改完，与五位矩阵口径一致

| L-28 点位 | 现状（本轮实测） |
| --------- | ---------------- |
| `middleware/管理员.ts:85` | 已改：唯一查库入口 + `创建能力门禁` 五位实例（`guanLiZhiDuMenKong`/`FengJin`/`FengJinShenHe`/`TongJi`/`GaoWei`） |
| `routes/好感度.ts:21` | 已改：`/:jiaoSeId/详情` 挂 `guanLiZhiDuMenKong`（`cha_kan`） |
| `routes/通知.ts:58` | 已改：`/发送` 挂 `guanLiGaoWeiMenKong`；本人 已读/全部已读 保持用户自范围 |
| `services/管理员.ts:112` | 已改：`huoQuYongHuLieBiao` 的 `SELECT` 取三列、`jiao_se: 取管理角色(row)`（不再回传 `管理员` 布尔） |
| `services/管理员.ts:330` | 已改：`sheZhiGuanLiYuanZhuangTai` 回传 `jiao_se`，幂等比较用同口径 `=== true`，写后 `qingChuJiaoSeHuanCun` |
| `services/认证.ts:62` | 已改：`yingSheYongHu` 回传 `jiao_se` + `neng_li`（矩阵投影），`guan_li_yuan` 归零 |
| 前端 7 处 `shiFouGuanLiYuan` + 4 处门禁 | 已改：`stores/用户.ts` 的 `dangQianJiaoSe/nengLieBiao/keGuanLiZhiDu/keGaoWei`；`聊天页面.vue:878/1796`、`实时日志.vue:247`、`stores/实时日志.ts:88`、`stores/聊天.ts:289` 全部按位派生 |
| **本轮补的第 7 处** | `routes/管理员.ts:545` `/申诉/审核` 仍挂 `gao_we` ⇒ 与 FP-17 对端 `路由/封禁.ts:252` `/shen-su/shen-he` 挂 `feng_jin_shen_he` **口径不一致**（审核员在管理中心能审申诉、在游戏端 403）。已改挂 `guanLiFengJinShenHeMenKong`，顺带消掉该常量的未使用导入 |

### ③ 与 FP-19 运营通道契约同源；不留两套真源

- `backend/src/socket/管理通道.ts` 的成员资格判据 `anYongHuIdJuBeiNengLi(用户ID,'cha_kan')` 与 HTTP 门禁、身份回传是**同一入口链**：`anYongHuIdQuJiaoSe` → `utils/角色能力.角色具备能力` → 唯一矩阵。全库无第二处 `Boolean(管理员)`/`.管理员 === true` 型判定（静态守卫 + 手工 grep 双重确认为 0，唯一命中是 `services/管理员.ts:338` 对**被写入的那一列**做幂等比较，不属授权判定）。
- 前端 `聊天页面.vue` 的 `greedisgood` 门（`queBaoShenFenJiuXu()` 后判 `用户仓库.keGuanLiZhiDu`）与渲染处第二道门、`stores/聊天.ts` 的 4 个 `管理员_*` 监听器门（`keGuanLiZhiDu`）同一载体，FP-12 静默语义与 `聊天界面.test.ts` 的三条行为用例零削弱（该文件本轮只删过一具空 fixture 键）。
- 新增前端同源守卫钉住「只有一个写入口」：`nengLieBiao.value =` 计数恒为 1 且必须经 `归一管理能力列表`；前端目录内 `const 角色能力矩阵` 命中数恒为 0、`.管理员 === true`/`Boolean(…管理员)` 命中数恒为 0。

### ④ 所有写路由门禁齐全 —— `backend/src/routes/*.ts` 穷举（62 条写注册，逐条判定）

判定口径：管理面（读写**他人**数据或**全局**状态）→ 必须挂 `middleware/管理员.ts` 的能力门禁；用户自范围（只动调用者自己的行）→ 归 `renZhengZhongJianJian` + service 层归属校验，不属 FP-18 门禁面。

`routes/管理员.ts`（10 条，另有路由级 `luYou.use(guanLiZhiDuMenKong)` 覆盖其全部 GET）：

| 注册处 | 门禁 | 判定理由 |
| ------ | ---- | -------- |
| `luYou.post('/夺舍/:jiaoSeId'` | `gao_we` | 接管他人实时会话 |
| `luYou.post('/夺舍/:jiaoSeId/心跳'` | `gao_we` | 续他人会话租约，须与父操作同位 |
| `luYou.post('/归还/:jiaoSeId'` | `gao_we` | 释放他人会话 |
| `luYou.post('/测试用户'` | `gao_we` | 造账号并回传初始密码 |
| `luYou.post('/测试用户登录'` | `gao_we` | 凭手机号直接取他人令牌（最高危） |
| `luYou.post('/授权'` | `gao_we` | 提权写，永不下放 |
| `luYou.post('/回收'` | `gao_we` | 降权写，永不下放 |
| `luYou.delete('/用户/:yongHuId'` | `gao_we` | 删他人账号（级联） |
| `luYou.post('/账号封禁/解封'` | `feng_jin` | FP-17：运营可解封；与对端 `/zhang-hao-feng-jin/jie-feng` 同位 |
| `luYou.post('/申诉/审核'` | `feng_jin_shen_he` | **本轮改**：与对端 `/shen-su/shen-he` 同位（审核员面） |

其余管理面写点（不在 `管理员.ts` 内）：

| 注册处 | 门禁 | 判定理由 |
| ------ | ---- | -------- |
| `routes/通知.ts` `luYou.post('/发送'` | `gao_we` | 全员群发，写 `通知` 全表 + 广播 |
| `routes/消息.ts` `luYou.post('/多模态配置/重载'` | `gao_we` | 热重载**进程级** AI 配置，影响所有人 |

非管理面（逐条判定为**用户自范围或公开通道，不需要能力门禁**）：

| 文件 | 条数 | 归属校验所在 |
| ---- | ---- | ------------ |
| `好友.ts` `/申请`·`/申请/:id/接受`·`/申请/:id/拒绝`·`/消息`·`/消息/:id/撤回`·`/消息/已读/:haoYouId`·`DELETE /:haoYouId` | 7 | service 按 `yongHuId` + 双方关系行校验 |
| `好感度.ts` `/:jiaoSeId/更新`·`/:jiaoSeId/秘籍` | 2 | `huoQuWanZhengHaoGanDu(yongHuId,jiaoSeId)` 归属；`/更新` 增量钳 ±3；`/秘籍` 另需玩法密令（FP-12 秘籍是**玩家自己**的通关作弊，不是 RBAC 面） |
| `战绩.ts` `DELETE /:dangAnId`·`/批量删除` | 2 | `shanChuDangAn(yongHuId,…)`/`piLiangShanChuDangAn(yongHuId,…)` 归属在 SQL |
| `挑战.ts` `/开始`·`/放弃` | 2 | 对局行按 `用户ID` |
| `消息.ts` `/会话`·`/会话/:huiHuaId/媒体`·`/会话/:huiHuaId/消息`·`/军师`·`/翻译`·`/语音/转写`·`/会话/:huiHuaId/语音理解`·`/会话/:huiHuaId/生图`·`/会话/:huiHuaId/生成视频`·`PUT /…/撤回`·`PUT /…/已读` | 11 | `huiHuaId` 一律当 `jiaoSeId` 校验会话归属；AI 类走 `aiQingQiuXianLiu`（按用户额度） |
| `用户设置.ts` `/聊天背景`·`/聊天背景/上传`·`/气泡`·`/隐私`·`/排位/清空` | 5 | `WHERE "用户ID"=$` |
| `表情.ts` `/我的`·`/我的/排序`·`/我的/:biaoQingId` | 3 | FP-06b：归属写进 WHERE，非本人 404 |
| `角色.ts` `/MBTI生成`·`/确认` | 2 | 生成者本人 |
| `认证.ts` `/发送码`·`/注册`·`/登录`·`/刷新`·`/吊销刷新令牌`·`/更改密码`·`/更改用户名`·`/设置默认性别`·`DELETE /注销` | 9 | 前三条是设计上的公开入口（限流+验证）；其余需 `yong_hu` 且只动本人 |
| `资料.ts` `/头像`·`/申诉`·`/签名`·`/签名白名单` | 4 | 本人；`/申诉` 是**本人封禁**申诉（审核面在 `管理员.ts /申诉/审核`） |
| `日志接收.ts` `POST /` | 1 | 前端埋点通道，挂在 `renZhengZhongJianJian` **之前**（`server.ts:163`），靠事件名白名单 + 深度/长度截断防御；写的是服务端日志非业务数据 |
| `健康检查.ts`·`功能开关.ts`·`媒体.ts`·`角色详情.ts` | 0 | 无写注册 |

合计 10 + 2 + 48 = 62，与穷举脚本逐条对得上。守卫由 `FP18角色同源解析.test.ts > 管理面每条写路由注册处逐条挂到指定能力门禁` 把守：①`routes/管理员.ts` 的写注册**集合**必须与登记表全等（漏挂、多挂、改名、新增未登记都红）；②逐条 `toBe(门禁常量)` 精确比对（不是 `toContain`、不是长度）；③路由级只读门恒在；④非 `管理员.ts` 的管理面写点逐条比对；⑤`门禁常量 ↔ 创建能力门禁('位')` 的绑定关系 + 常量表键集恒等于 `管理能力清单`。

### ⑤ `guan_li_yuan` 消费者穷尽

`grep`（排除 `node_modules`/`dist`）后，**生产代码消费者为 0**，故按「删除」处置，不留双写。已清点的残留全部处理：

| 命中 | 处置 |
| ---- | ---- |
| `frontend/src/__tests__/*.test.ts` 17 文件 25 处 fixture 键 `guan_li_yuan:` / `是否管理员:`（后端已不再签发/读取，纯惰性地雷） | 已删（`用户.test.ts` 另把 fixture 补成 `jiao_se: null, neng_li: []`） |
| `frontend/tests/fp02-jietu·fp02-liaotian-beijing·fp03-qipao·fp05-shoukou.spec.ts` 的 `guan_li_yuan: false` | 已改为 `jiao_se: null, neng_li: []` |
| `frontend/tests/fp06-shou-wei.spec.ts` 的 `guan_li_yuan: true` | **真断掉的消费者**：该 spec 用 localStorage 灌身份后输 `greedisgood` 断言 `.guanli-jiankong-fuchuang` 可见，而 FP-18 后视图门只认 `neng_li` ⇒ 已改 `jiao_se:'chao_guan'` + 五位 `neng_li` |
| `services/夺舍.ts`·`services/通知.ts`·`routes/通知.ts`·`socket/夺舍.ts` 的 `guan_li_yuan_id` | **保留**：语义是「执行该动作的管理员用户 ID」，与已删的二值旗标无关（守卫正则 `guan_li_yuan\s*[?:]` 不误判） |
| `config/日志推送配置.ts` 的 `ri_zhi_guan_li_yuan` | **保留**：Redis 房间名字面量 |
| `backend/dist/**` | 构建产物，不入库 |
| 测试内 mock | 已随上述清理同步 |

删除属公开契约变更 ⇒ 已记入 `PROGRESS.md`『契约变更』。

### ⑥ 安全回归

`backend/src/utils/__tests__/FP18角色同源解析.test.ts` 28 例（原 24，本轮 +4），全部为 supertest 真跑路由（mock 数据层，不 mock 门禁）：

- 三旗标 → `jiao_se` 全矩阵（`it.each` 含**全伪→null**）+ 超管优先 + 多旗标只出一角色 + 非法旗标值 fail-closed；
- `neng_li` 为矩阵的**逐位投影**（三角色各自全量，一位不增不减）+ 运营/审核员字面量钉值；
- 未认证：只读与全部写路由一律 401 且**不查库**；
- 无身份（平民）：只读 + 全部写路由 403、`cuo_wu_ma='WU_GUAN_LI_QUAN_XIAN'`、`ti_shi` 取 `translations.guanLiYuan.wuGuanLiQuanXian`，响应体正则证伪 `SELECT|SHU_JU_KU|SQLSTATE|Error:|at *.ts`；
- 逐角色 × 逐写路由的放行/拦下**由矩阵推导**（含位不互溢：运营调申诉审核 403、审核员调解封 403、运营调任何 gao_we 写 403）；
- 客户端伪造：令牌载荷 `jiao_se/guan_li_yuan/neng_li` + 请求体申报角色，同一请求体超管放行 / 平民拦下；
- 查库异常 → 500 且只回通用文案（SQLSTATE 与 `connection refused` 不外泄）；
- 缓存与失效：只读三列、二次命中不查库、按目标失效 vs 全量失效、`guan_li_shi_xiao` 载荷两种键名（`yongHuId`/`用户编号`）都能定位、解不出即判全量；
- 前端「能力未知不渲染」：`用户.test.ts` 新增 fail-closed 例（`neng_li` 缺失/非数组/含白名单外值）、`角色能力同源.test.ts` 归一函数例、`管理员监控.test.ts` 源码门禁例、`聊天仓库监控.test.ts` 的 `nengLieBiao=[] → 零监听` 例。

---

## 三、修改文件（本轮全部改动）

- `backend/src/routes/管理员.ts` —— `/申诉/审核` `gao_we` → `feng_jin_shen_he`；路由级注释按五位口径改写
- `backend/src/utils/__tests__/FP18角色同源解析.test.ts` —— 3 处旧二位断言同步为五位并加强（新增投影例、逐位挂门禁守卫、未认证写 401、位不互溢断言）
- `frontend/src/utils/角色能力.ts` —— 能力词表二 → 五位（与后端逐字同集合）
- `frontend/src/__tests__/角色能力同源.test.ts` —— 新增（前端↔后端同源 + fail-closed + 无第二真源 + 单写入口）
- `frontend/src/__tests__/用户.test.ts` —— 2 条 `shiFouGuanLiYuan` 用例改写为 4 条能力位用例；fixture 换契约字段
- `frontend/src/__tests__/过往战绩分享.test.ts` —— blob 地址逐实例唯一化（去 L-38 顺序敏感性，断言对象不变）
- `frontend/src/__tests__/` 另 16 文件 + `frontend/tests/` 5 spec —— 删/换已废弃契约的惰性 fixture 键

## 四、变异检验（证明新守卫非空断言）

| # | 变异 | 结果 |
| -- | ---- | ---- |
| M1 | `/账号封禁/解封` 的 `feng_jin` → `gao_we` | 2 红（逐位挂门禁守卫 + 逐角色矩阵：`yun_ying 持 feng_jin 却写失败`） |
| M2 | 摘掉 `/账号封禁/解封` 的门禁（模拟真丢门禁） | 2 红（逐位挂门禁守卫解析到 `门禁: null` + 逐角色矩阵用例） |
| M3 | 前端 `管理能力清单` 退回二位 | 2 红（逐字等于后端清单 + 覆盖矩阵每一位） |
| M4 | 还原共享 blob 地址常量 → 全量跑 | 恢复为顺序敏感失败（1 failed），唯一地址后 89/89 绿 ⇒ 证明去抖的是跨实例定时器串扰，非本实例双回收 |

## 五、回归数字（本轮末尾实测）

- backend：`npx vitest run` → Test Files **24 passed (24)**，Tests **417 passed | 1 skipped (418)**；`npm run build`(tsc) exit 0；`npm run lint` exit 0
- frontend：`npx vitest run` → Test Files **89 passed (89)**，Tests **1197 passed (1197)**；`npx vue-tsc --noEmit` 零错；`npm run build` 成功（dist 9.04 MB，体积门禁 15 MB）
- `npx eslint .`：本人 5 个变更文件 **0 error**；仓库级 14 error **全部**在未跟踪的 `frontend/fp01-*.mjs`/`fp02-*.mjs` 取证脚本（PROGRESS 已登记为「待清理过程性文件」，且 `用户已授权` 主代理在 FP-17/18/19 落地后用它们做浏览器端到端验收 ⇒ 本轮不删，见 R2）
- 未做浏览器验证（按派发约束）；未重建/重启容器

## 六、遗留与观察项

- **R1（外部改动，只报不改）**：12:00 的 `过往战绩.vue` 重设计把分享按钮改为硬编码 `#ffd500`/`#14141a`，并把 `过往战绩分享.test.ts` 的样式用例从「令牌明暗两套均有定义」改成断言这两个字面量 ⇒ 该按钮**不再随主题令牌走**（明暗两套同值），与 FP-10/FP-12 的令牌口径及 L-32 的方向相反。功能与可访问性未见破坏（同值在两主题下都可读），属视觉设计决策，交主代理上报用户是否认可。
- **R2**：`npx eslint .` 的 14 个仓库级 error 全在他人未跟踪取证脚本；待主代理按 PROGRESS 授权处置（用完即删或纳入忽略）。
- **R3（未纳入本轮，L-40 仍未消解）**：回收/降级写点（`services/管理员.ts::sheZhiGuanLiYuanZhuangTai`）不清理存量 socket 的 `guan-li:<id>` 成员资格 ⇒ 撤权后到下次重连/再进聊天前，DevTools 仍能收自己会话的 `管理员_*` 帧。修法需让 `middleware/管理员.ts`（或该 service）反向依赖 `socket/io`，与 `socket/管理通道.ts → middleware/管理员.ts` 成环；FP-19 的 `qingChuJiaoSeHuanCun` 联动只到缓存层。建议单列，勿再塞进 RBAC 面。
- **R4**：后端 `npm run lint` 因 L-20（`typescript-eslint` 未安装 → `.ts` 全部「File ignored」）实为空跑，本轮 `tsc` 与 vitest 覆盖了 TS 侧，但门禁虚位仍未修。
- **R5**：`frontend/tests/*.spec.ts` 与 `e2e/tests/` 受清理与忽略双重管理（L-33 同族），本轮的 fixture 契约同步只在磁盘生效、不入库。
- **R6（三轴审查 Standards 轴顺带发现，HEAD 即如此，非本循环引入，按『精确修改/只提不删』未动）**：`backend/src/services/账号封禁.ts:221-235` 把玩家可见通知标题与正文硬编码（`'申诉结果通知'`、`'你的申诉已通过，账号已恢复正常，历史违规记录已清除。'`、`'你的申诉未通过，账号限制继续生效。如仍有异议请联系客服。'`），未走 `translations` ⇒ 违反 [P0] 翻译文件优先。登记 PROGRESS L-44 交主代理单列处置（同文件内可能还有同类点，须一并穷举而非只改这三行）。

## 七、三轴审查

**执行方式披露（必须先说）**：本工人为 Headless 子代理，会话内无并行子代理工具（`mcp_list` 查无 agent 类工具）⇒ 无法按「三轴审查」skill 的三路并行隔离执行。按与 FP-10/FP-19 同一处置降级为**三轴独立自审**：每轴单独取证、单独下结论，且 BlindSpot 轴强制找**证伪自己结论**的证据而非佐证。该降级本身是盲区风险来源（同一上下文仍在场），已在返回报告与 PROGRESS 中披露。

### Standards

| 检查项 | 结论 |
| ------ | ---- |
| 中文命名/既有风格 | 变更处一致（`写端点清单`/`门禁常量表`/`归一管理能力列表`/`keGuanLiZhiDu`），无新造英文缩写 |
| 零硬编码 | 能力位、门禁常量、路由路径、期望状态码一律由 `utils/角色能力` + `创建能力门禁` 单一来源推导；测试里的能力数组字面量是**钉值**而非实现常量 |
| 翻译文件优先 | 本轮零用户可见文本改动；401/403/500 出口仍走 `huoQuFanYi('guanLiYuan','wuGuanLiQuanXian')` / `('tongYong',…)`，并被正则断言不外泄内部实现 |
| 注释纪律 | 只在改动的门禁挂载点与唯一入口留「口径来源」型注释，无逐步解说 |
| 编译/类型 | backend `tsc` exit 0、frontend `vue-tsc` exit 0 ⇒ `guanLiFengJinShenHeMenKong` 由未使用导入转为真实使用，且未留孤立变量/导入 |
| lint | backend `npm run lint` exit 0（**L-20：该命令对 `.ts` 实为空跑，绿不代表过了 TS 规则**）；frontend `npx eslint .` 14 error 全在未跟踪 `fp01-*/fp02-*` 取证脚本，本人 5 文件 0 error |
| 判断性建议（未做） | `routes/管理员.ts:527` 注释仍写「**管理员**解封」，现口径 `feng_jin`（运营亦可），措辞与口径轻微错位；属注释措辞非行为，按『精确修改』未动 |

**硬性违规 0。** 顺带发现 1 处**非本轮引入、在本 FP 变更面之外**的 [P0] 违规并只报不改：`backend/src/services/账号封禁.ts:221-235`（`git diff` 空 ⇒ HEAD 即如此）把玩家可见通知正文硬编码为 `'申诉结果通知'` / `'你的申诉已通过，账号已恢复正常，历史违规记录已清除。'` / `'…请联系客服。'`，未走 `translations` ⇒ 登记 L-44。

### Spec（派发单六条验收）

| 条目 | 结论 | 锚点 |
| ---- | ---- | ---- |
| ① 服务端唯一权威 / 前端仅视图门 / 不读令牌载荷 / 未知 fail-closed | 达成，并补掉「前端词表窄三位导致能力位被静默丢弃」这条验收文字未点名但属该条语义的反例 | `middleware/管理员.ts:90-151`；`jwt.ts` 静态守卫；`用户.test.ts` fail-closed 例；`角色能力同源.test.ts` 归一例 |
| ② 6 处二值判定逐个改完且与五位矩阵同口径 | 达成（6 处 + 前端 7 赋值链 + 4 门禁；另补第 7 处漏挂位 `/申诉/审核`） | §2-② 表 |
| ③ 与 FP-19 通道契约同源、不留两套真源；FP-12 静默语义不削弱 | 达成（单一 `anYongHuIdQuJiaoSe` 链；前端全库 `const 角色能力矩阵` 命中数恒 0、`nengLieBiao` 单写入口由测试钉住；`聊天界面.test.ts` 三条秘籍行为用例原文保留） | 新增前端守卫 |
| ④ 写路由门禁齐全 + 逐条判定入证据 | 达成（62 条穷举逐条判定，非总数；守卫由 `toContain` 型升级为「集合全等 + 逐条 `toBe`」） | §2-④ |
| ⑤ `guan_li_yuan` 消费者穷尽、不留双写、删除记契约并同步 | 达成（生产码 0 消费者；17 测试文件 + 5 Playwright spec 同步；`guan_li_yuan_id`/`ri_zhi_guan_li_yuan` 判为不同语义保留；『契约变更』记 2 行） | §2-⑤ |
| ⑥ 安全回归四项 | 达成（三旗标矩阵含全伪→无角色、403/401 走翻译且不外泄内部、伪造声明零生效、能力未知不渲染，均有对应用例且真跑 supertest 路由） | §2-⑥ |

**缺失 0；范围蔓延 0**（未加新能力位、未加新路由、未碰布局/样式、未碰 `恋爱吧管理中心/**` 与 FP-16 禁区、未建/重启容器）。

### BlindSpot（专找"收口/扩权顺带放开或漏挂的门"）

**本轮查出并已闭合的 3 个真实缺口**

1. **漏挂位（权限不足方向）**：`/申诉/审核` 停在 `gao_we` ⇒ 审核员在管理中心可审申诉、在游戏端 403，两仓同矩阵不同口径。**任何既有测试都发现不了它**（旧守卫只问「是不是 gao_we」，越权用例只问「无位者是否被拦」）。
2. **静默丢位（视图门永不亮方向）**：前端词表 2 位 vs 服务端 5 位，旧守卫体系零覆盖（后端那份只对比两个后端仓）。表现是「按 `feng_jin`/`tong_ji_xie` 设的门永远不亮且不报错」＝最坏的一类盲区：安全方向正确但完全静默。
3. **真断掉的消费者**：`frontend/tests/fp06-shou-wei.spec.ts` 靠 `guan_li_yuan:true` 灌 localStorage 断言 `.guanli-jiankong-fuchuang` 可见。FP-18 后无人读该字段 ⇒ 面板不再出现，而该目录既不在 vitest 门内也不在 `vue-tsc` 作用域内，**回归抓不到它坏掉**。

**主动证伪自己结论的结果（诚实说明哪些没找到问题）**

4. 「`/账号封禁/解封` 丢了门禁」这一派发前提**被证伪**（门禁在位且位对）。我没有因为"提示词这么说"去补第二道 `gao_we`——那会同时打脸 FP-17 对端口径与自己刚钉的同源守卫。
5. 「`feng_jin_shen_he` 是纯审核位」**站不住**：实测 `shenHeShenSu(tongGuo=true)` 会 `DELETE FROM "账号封禁"` ⇒ 实质解封，即 `feng_jin_shen_he ⊇ 条件性 feng_jin`，审核员不持 `feng_jin` 也能达成解封效果。非本轮引入（管理中心 `路由/封禁.ts:252` 同构，且 FP-17「审核员=读+封禁审核」正是 L-27 用户裁决的字面口径）⇒ **未擅自收紧**；若用户本意是"审核员只能裁申诉、不得动封禁状态"，须改 `shenHeShenSu` 语义（通过时只置标记不解封），属产品裁决。
6. 「`tong_ji_xie` 在游戏端有用」**不成立**：游戏端零挂载点，该位只为与对端矩阵全等而存在于词表。已把它写成守卫（门禁常量表键集 ≡ `管理能力清单`），将来给游戏端加统计写面时必须显式登记挂位，否则两条断言一起红。
7. 「运营/审核员旗标可在游戏端被管理」**不成立**：`backend/src` 内对 `"运营"`/`"审核员"` 两列的 `UPDATE/INSERT` 为 0（`/授权`·`/回收` 只写 `"管理员"`）⇒ 游戏端新增的 `cha_kan`/`feng_jin`/`feng_jin_shen_he` 面只能靠管理中心建号才可达。不是缺陷，但它意味着本轮「运营 200 / 审核员 200」的断言在生产上依赖对端建号流程；同时 `sheZhiGuanLiYuanZhuangTai` 改为回传写后 `jiao_se` 恰好覆盖了 L-30 的「部分回收被误读成已降权」面。

**残余风险（未闭合，已登记）**

8. **最严重**：撤权即时性 L-40——闭合需 `middleware/管理员.ts`（或该 service）反向依赖 `socket/io`，与 `socket/管理通道.ts → middleware/管理员.ts` 成环；本轮不混改，留单列。
9. `shuaXinLingPai` 不回传身份 ⇒ 刷新后一次 `jiaZaiYongHu()` 失败会让**视图门**退回本地 `yonghu` 缓存（授权层仍按查库，无数据泄露）。登记 L-43。
10. 无目标的全量失效广播会一次打掉两仓全部角色缓存：安全方向正确（回查库），语义与对端 FP-14 一致，未新增面。

**摘要**：Standards 0 硬性违规（1 处相邻 [P0] 违规只报不改 → L-44）；Spec 六条全达成、0 蔓延；BlindSpot 闭合 3 个真实缺口、证伪 1 条派发前提、揭穿 2 条自我过度推断，最严重未闭合项为第 8 条（撤权后存量 socket 仍可收 `管理员_*` 帧，视图门已遮但 DevTools 可见）。

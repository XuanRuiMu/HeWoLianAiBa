# 和我恋爱吧 · UI 重设计 & AI 参数逻辑重设计 — 讨论决策文件

> **怎么用这个文件**
> 这是讨论稿，**请直接编辑**：在你认可的方向前打 `✅`，在 `[ ]` 里勾选，在 `____` 处填空，在不认同处写备注。
> 改完后告诉我「按文件执行」，我会读取本文件，按循环工程 skill（Orchestrator + Headless 子代理）落地：生成选定设计 → 改代码 → 改需求文档 → 真实浏览器测试 → 三轴审查 → 推送 GitHub → 启动项目。
>
> **强制铁律（来自你的指令）**：所有改动必须追根因、解决同类问题，禁止"针对现象打补丁"；能删的冗余/错误代码直接删；需求文档与代码必须一致，若你口头要求与文档冲突，以你为准并**同时改文档和代码**。

---

## 1. 设计上下文（Design Context）

| 项 | 内容（请确认/补全） |
| --- | --- |
| 产品 | 和我恋爱吧 —— 微信恋爱模拟游戏 |
| 技术栈 | 前端 Vue3 + Vite + TS；后端 Node + Express + TS + PostgreSQL + Redis；Docker Compose 部署 |
| 目标用户 | 中国年轻用户（大学生 / 年轻职场），在"微信"里和 AI 对象谈恋爱 |
| 基调 Tone | 亲密、真实、浪漫、像真的在微信里撩；**不要 SaaS 后台感、不要"AI 生成感"** |
| 品牌名 | 「和我恋爱吧」（登录页应用名取自翻译键 `renZheng.yingYongMing`，请确认是否就叫这个） |
| 必须保留 | 登录时「卷轴收缩飞到左上角用户资料处」的动画 —— 这是创意亮点，**任何重设计都保留** |
| 现有视觉资产 | `图片/主页元素/吴昊阳终稿静态图.png`（主页角色图）、`/favicon.svg`、得意黑字体 |

---

## 2. 根因分析

### 2.1 UI 为什么"处处 AI 风格"
- **现象**：登录页、主页、资料向导、协议弹窗，全部是同一套「深色玻璃拟态 + 蓝紫渐变 + 渐变文字标题 + 线框 SVG 图标 + emoji 浮动徽标」。
- **根因**：项目没有一个统一且有**立场**的设计系统，所有页面套同一个"安全模板"，导致千页一面、缺乏产品灵魂。问题不是某一页丑，而是"没有美学观点"。
- **对照 impeccable 的 AI Slop Test（必须避开的反模式）**：纯黑底+发光、紫蓝渐变、毛玻璃滥用、圆角+通用阴影、Inter 类无个性字体、英雄区居中大图标 + emoji。当前代码几乎全中。

### 2.2 好感度 / AI 参数为什么生硬（已读源码确认）
- **根因定位**：
  - `backend/src/services/好感度评判.ts` 的 `xiuZhengFanWei()` 把 AI 每维输出强制钳制到 `[-3, +3]`（来自 `AI配置.ts` 的 `haoGanDu.zuiXiaoBianHua / zuiDaBianHua`）。
  - 四维加权平均后单轮**最大变化 = 3**；再叠加 `好感度配置.ts` 的对数衰减 `max(1 - 分/1000, 0.1)`。
  - 结果：分数到 700 时单轮实际只 +0.3~1 点；从 300/500 冲到胜利阈值 800，需要**几百轮**对话。
  - 需求文档 **FP-10 规则4** 还把「-3 到 +3 整数」写死成规范 —— 这是硬规则的源头。
- **结论**：「每次只加 1 点」不是某个 magic number，而是"窄钳制 + 重衰减"共同造成的**系统性慢性**。根因修复 = 让 AI 自由判定增量大小 + 重平衡衰减，而不是再塞一个"每轮 +X"的硬编码（那只是换个地方打补丁）。

---

## 3. UI 重设计方案（每页多方案，可视化示例见 `UI重设计示例/`）

> 每个方案都**刻意避开 AI 模板**，各有明确美学立场。示例是静态 HTML（手机框预览，因为产品本质是微信移动体验），仅用于定方向，不代表最终实现细节。

### 3.1 主页（FP-02：双模式入口 + 吴昊阳静态图）
- **方案A · 微信原生拟态**：像真的微信首页 —— 干净米白/奶油底，角色以"聊天联系人卡片"形态出现（"吴昊阳 刚刚上线"），模式入口做成微信功能模块式的温暖列表行。熟悉、可信、不花哨。用衬线体做品牌。
- **方案B · 电影感视觉小说**：吴昊阳静态图做满屏电影海报，暖色戏剧光 + 胶片颗粒，衬线展示标题，极简 UI 框架。像恋爱视觉小说开场 / 高级交友 App 引导页。
- **方案C · Gen-Z 俏皮纸感**：柔和纸纹 + 手绘贴纸点缀，圆润但有性格的展示字体，贴纸式模式按钮。Hinge/Bumble 的暖 × 国产 rom-com 的俏皮。

> 你要求主页必须给 **3 套方案**，已按要求产出 A/B/C。

### 3.2 登录 / 注册（保留卷轴飞行动画）
- **方案A · 单屏沉浸式**：去掉"卡片浮在渐变上"，改成满屏暖色场景（天空/花树渐变），表单极简浮于其上，卷轴动画保留（卷起飞向左上头像）。编辑感、电影感。
- **方案B · 信笺 / 手写信**：登录做成"信纸 + 火漆印"质感，表单落在信纸上；卷轴动画 = 信纸卷起飞走。可触、浪漫、辨识度高（纸纹 + 衬线）。

### 3.3 资料设置向导（1/2/3 步骤，FP-03：性别-self / 性别-object / MBTI16+渣变体）
- **方案A · 对话式引导**：不是"进度圆点 + 卡片"，而是角色用聊天气泡一步步"问你"（"你呢，男生还是女生？" → 两个大答案气泡）。进度用一条细线 thread 体现。和"聊天模拟"高度贴合。
- **方案B · 极简步骤卡（一步一景）**：干净步骤卡，但用**竖向进度脊**（非圆点）+ 强字体层级，每步一个"场景"，角色在旁边呼应。杂志感。

### 3.4 用户协议 / 隐私政策（协议模态框，当前是裸 `<pre>` 渲染 txt）
- **方案A · 舒适长读（阅读模式）**：衬线正文、舒适行高、顶部**章节目录锚点导航**（协议很长，必须能跳），像读 Medium / 读库。
- **方案B · 要点化（渐进披露）**：把法律长文拆成带图标的可折叠**要点卡片**，顶部先给"只需了解这 3 点"摘要，细节可展开。降低认知负载。

---

## 4. AI 参数 & 节奏重设计（根因级）

### 4.1 设计目标（来自你的指令）
- **AI 掌控判定**：好感度增量由 AI 根据一轮对话质量**自由判定大小**，不再每维锁死 ±3。
- **节奏现代化**：E 人（外向角色）在用户情商高时**约 10 轮可胜**；I 人整体需更多轮。**不硬性规定"E 必须 10 轮胜"**，仅大体节奏。
- **不变的部分**：渣男渣女逻辑、表白/识破/神经病等语义判定（本来就是 AI 控制，不在本次改）。

### 4.2 根因修复方案（草案，等你拍板）
1. **放开钳制**：`xiuZhengFanWei` 区间从 `[-3,+3]` 改为**宽松可配置**区间（建议单维约 `[-15, +15]`），且去掉"强制整数化"，允许小数、总分取整。让 AI 能表达"这一轮聊得极好 → 大幅升温"。
2. **重平衡衰减**：衰减只在高分端做"软封顶"防瞬间满血，中段不再被腰斩。建议 `max(1 - 分/1000, 0.35)` 或分段（低分≈1、高分 0.35~0.5），保证好轮次每轮肉眼可见推进（十几~几十点）。
3. **节奏自然分化（不写死轮数）**：E/I 轮次差由"人设 + 初始分差"自然产生 —— E 角色初始分高(450-500)、热情人设下 AI 给更大增量；I 角色初始低(300-370)、增量保守。系统层面只提供"节奏系数"这类**配置旋钮**，绝不出现"第 N 轮必胜"的硬编码。
4. **保留的设计**：总分 0-1000、10 阶段映射、四维权重(信任.35/亲密.25/趣味.20/关怀.20)、阶段变更写记忆(升级8/降级3)、管理员可见全量 —— 这些是设计非生硬参数，不动。
5. **安全护栏**：仍设"防失控软上限"（如单轮总量不超过某配置值），防止 AI 抽风直接满血；这是护栏不是节奏。

### 4.3 需求文档同步修改（强制，冲突以你为准）
- **FP-10 规则4 改写**：删「AI 输出四维变化（-3 到 +3 整数）」，改为「AI 自由判定四维变化幅度（宽松可配置区间），系统仅做防失控软护栏与高分端软衰减」。
- **FP-11 新增节奏说明**：E 人高情商约 10 轮可胜、I 人更多轮（非硬规则，仅大体节奏指引）。
- **涉及文件**：`需求文档.md`(FP-10/FP-11)、`backend/src/config/AI配置.ts`(放宽区间)、`backend/src/config/好感度配置.ts`(衰减系数)、`backend/src/services/好感度评判.ts`(去整数化/放宽)、对应 `__tests__`。

---

## 5. 待你决策的事项（请直接编辑本区）

### 5.1 主页方向（必选其一）
- [ ] 方案A 微信原生拟态
- [ ] 方案B 电影感视觉小说
- [ ] 方案C Gen-Z 俏皮纸感
- [ ] 其它（备注：____）

### 5.2 登录/注册方向
- [ ] 方案A 单屏沉浸式
- [ ] 方案B 信笺/手写信
- [ ] 其它（备注：____）

### 5.3 资料向导方向
- [ ] 方案A 对话式引导
- [ ] 方案B 极简步骤卡
- [ ] 其它（备注：____）

### 5.4 协议/隐私方向
- [ ] 方案A 舒适长读（目录锚点）
- [ ] 方案B 要点化（摘要+可展开）
- [ ] 其它（备注：____）

### 5.5 AI 参数重设计是否按 4.2 草案执行
- [ ] 同意草案（放开钳制 + 重平衡衰减 + 自然分化 + 软护栏）
- [ ] 同意方向但参数要调（我的建议：单维区间 ____ / 衰减下限 ____ / 节奏系数 ____）
- [ ] 我有不同想法（备注：____）

### 5.6 节奏目标再确认
- E 人高情商胜利用时（轮）：约 ____（你说"大概10轮"，是否作为目标值）
- I 人整体用时：比 E 人 ____（多/显著多）
- 是否保留"渣男渣女更易让人相信从而更快被骗"的设计（初始 +200~300）：[保留] / [调整]

### 5.7 其它你要在执行前拍板的点
- 浅色/深色主题：当前有两套（深色默认 + 浅色），重设计后是否两套都做、哪套为主？____
- 字体：是否引入一款 distinctive 中文展示字体（如得意黑已用，是否再加衬线/手写体）？____
- 是否趁此次重设计顺手统一一套设计 token（颜色/间距/字体变量），消灭散落硬编码？[是] / [否]

---

## 6. 范围 / 测试 / 执行计划

### 6.1 做什么（范围）
- 重设计并实现：登录/注册、主页、资料向导(1/2/3)、协议/隐私 共 4 类页面（按你选定方向）。
- 重构好感度参数逻辑（根因级）+ 同步需求文档 + 后端测试。
- 顺手清理：与本次重设计冲突的冗余/错误前端样式与硬编码（发现即删）。

### 6.2 不做什么（防过度烘焙）
- 不改聊天页、军师、战绩、通知、管理后台等未点名页面（除非重设计牵扯到共用组件）。
- 不改渣男渣女/表白/识破/神经病的 AI 语义判定逻辑（本就 AI 控制）。
- 不引入 Spring Boot / SSM 等被你禁止的技术；架构维持现有 Vue3+Express，仅做"美学 + 参数逻辑"层重构。

### 6.3 测试（真实用户端视角，来自你的硬性要求）
- 每个改后页面：真实浏览器打开 → 截图 → 识别元素/排版无异常（前端显示 OK）。
- 后端：启动后看服务器 log 无报错；参数逻辑改完跑对应 `__tests__`。
- 循环工程子代理在每功能点完成后调用「软件测试」+「三轴审查」skill。

### 6.4 执行方式
- 严格走**循环工程 skill**：主代理编排 + Task 子代理（全新上下文）落地，PROGRESS.md 状态机，熔断（单问题 5 次 / 总循环上限 / 阻塞即停）。
- 收尾：用 `总控制台.py` 推送 GitHub（禁止裸 git push）、删除本技能产生的过程文件、**启动项目并告诉你进入方式**供你手动测试。

---

*（本文件由 WorkBuddy 起草，供你编辑；编辑完成后说"按文件执行"即可。）*

# FP-1~FP-5 回归测试报告

> 生成时间：2026-08-12（UTC+8）
> 验证方式：真实浏览器双视口探针（Playwright/Chromium，desktop 1280×900 + mobile 390×844）+ 后端单元测试
> 环境：前端 `http://localhost:80`、后端 `http://localhost:3001`、账号 `13800138001 / test1234`
> 入口会话：`88864e38-7118-4a2e-a4a1-baba80ced13c`

## 一、结论速览

| 项 | 修复点 | 验证手段 | 结果 |
| --- | --- | --- | --- |
| FP-1 | 渣型"莫名其妙"也应赢 | 后端 `胜利失败条件.test.ts` 单测 | ✅ 33/33 通过 |
| FP-2 | 军师按钮等宽黄底 | 浏览器 DOM 测量 | ✅ 双视口通过 |
| FP-3 | 表情"顶起"而非"覆盖" | 浏览器 DOM 测量（前后高度差 + 重叠） | ✅ 双视口通过 |
| FP-4 | 表情首开不卡顿 | 浏览器首开耗时测量 | ✅ 双视口通过（~6.5ms） |
| FP-5 | 复盘隐藏军师指导 | 浏览器派发事件 + 显隐断言 | ✅ 双视口通过 |

## 二、FP-2 军师指导按钮（`.qingqiu-anniu`）

测量三态按钮（默认态"请求指导"）的尺寸与配色：

| 视口 | 宽(px) | 高(px) | 背景色 | 文字色 | 实测文本 |
| --- | --- | --- | --- | --- | --- |
| desktop | 91 | 36 | rgb(212,168,67) 金/黄 | rgb(255,255,255) 白 | 请求指导 |
| mobile | 91 | 36 | rgb(212,168,67) 金/黄 | rgb(255,255,255) 白 | 请求指导 |

- 三态（指导中/请求指导/已指导-查看结果）CSS 统一为 `7.6em×3em`、金/黄底，最长标签换行不裁切。
- 降级说明：需求字面"改为原先 2/3 长度"，但字面 2/3 会裁切最长标签，故取"不裁切的最窄等宽"（≈91px），视觉约为原宽的 2/3 且三态完全一致。

## 三、FP-3 表情顶起 vs 覆盖（`.emoji-mianban` / `.xiaoxi-quyu`）

| 视口 | 表情展开前消息区高 | 展开后消息区高 | 压缩量 | 面板 position | 面板/消息重叠高度 |
| --- | --- | --- | --- | --- | --- |
| desktop | 795px | 646px | **149px** | static（正常流） | 0px |
| mobile | 743px | 629px | **114px** | static（正常流） | 0px |

- 面板 `position: static`、`display: grid`，归属 `.liaotian-yemian` 的 `grid-template-rows:1fr auto` 中 footer 的 auto 行 → 展开使消息区（1fr）被压缩、内容上顶。
- 重叠高度 0 → 确为"顶起"，非"覆盖"。

## 四、FP-4 表情首开卡顿（预热硬化）

| 视口 | 首开耗时(ms) | 二次开合(ms) | emoji 按钮数 |
| --- | --- | --- | --- |
| desktop | 6.6 | 4.3 | 168 |
| mobile | 6.5 | 3.6 | 168 |

- 进入聊天页即克隆真实 `.emoji-mianban`（含 168 个 `.emoji-xiangmu`）离屏预热，首开不再做 168 字形/布局整形 → 无卡顿。

## 五、FP-5 复盘隐藏军师指导

访问 `/chat/:id?fuPan=1&dangAnId=test-archive`：

| 视口 | 全局军师按钮存在 | 军师按钮可见 | 派发 `junshi-zhankai` 后面板出现 | 复盘底栏存在 |
| --- | --- | --- | --- | --- |
| desktop | 否 | 否 | 否 | 是 |
| mobile | 否 | 否 | 否 | 是 |

- 全局菜单军师按钮 `v-if` 加 `route.query.fuPan!=='1'` 守卫 → 彻底移除；
- 聊天页 Teleport 面板 `v-if="junShiZhanKai && !fuPanMoShi"` + 监听器 `fuPanMoShi` 提前 return → 即使派发事件也不弹出。

## 六、补充说明：无关预存失败（非本次引入）

后端全量测试 353 项，346 通过、**7 项失败**，全部位于与 FP-1~5 无关的 AI 配置类测试：

- `AI引擎.test.ts`（1）：Writer 调用 temperature/thinking 互斥断言
- `AI模型配置.test.ts`（1）：模型子配置参数完整性断言
- `军师指导.test.ts`（4）：DeepSeek max_tokens / 军师 Prompt 内容断言
- `过往战绩与复盘.test.ts`（1）：复盘生成 max_tokens=3000 实际 16000

**根因判定**：本次提交 `ecc1095` 仅改动 9 个文件（胜利失败条件.ts/.test.ts、军师指导.vue、聊天页面.vue、main.ts、全局菜单.vue、vite.config.ts、.gitignore、需求文档.md），**未触及上述 4 个失败测试文件及其源码模块**。7 项失败均为 AI 模型参数（temperature/max_tokens/thinking-mode）相关的预存断言漂移，与本次 FP-1~5 修复无关。建议作为独立任务排查。

## 七、截图清单（见附件）

- `fp2-desktop.png` / `fp2-mobile.png`：军师指导按钮（黄底等宽）
- `fp3-desktop.png` / `fp3-mobile.png`：表情展开顶起态（消息区上移）
- `fp4-desktop.png` / `fp4-mobile.png`：表情面板（168 emoji）
- `fp5-desktop.png` / `fp5-mobile.png`：复盘模式（军师入口已隐藏，显示复盘底栏）

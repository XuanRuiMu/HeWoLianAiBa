# 和我恋爱吧需求文档

## 元信息

- 版本：v1.4（强制刷新双重揭露治理：揭示单飞+父层淡入+删死链+补发双事件；含 v1.3 真就绪门控与事件补染、v1.2 揭示前置染色）
- 日期：2026-09-11
- 范围：仅 `和我恋爱吧` 目录，不碰 MMORPG 文档与代码
- 口径原则：用户口径优先，原文不省略；无来源处标「待补充」，不推测补全
- 一致性：与同目录 `PROGRESS.md` 的 FP-01 至 FP-09 口径一致，新增 G-01 至 G-10 与之衔接，新增 H-01 至 H-04 与之衔接，新增 J-01 与六项治理收口衔接
- 维护规则：后续变更由各 FP/G/H 增量同步对应章节

## H-01 吴昊阳与草地门控揭示

- 需求口径：草地背景从中心向四周圆形扩散揭露实际背景；等吴昊阳和草地全部加载确认后再扩散，两者一起被揭露唤醒；任一方未加载出来则报错且两者均不加载；加载成功后进浏览器缓存，下次秒开
- 落点：grass-bg.html 内门控（草地就绪=引擎就绪且揭示方法可用且主画布存在，吴昊阳就绪=覆盖层贴图就绪或静态兜底图完整，双齐备才 startReveal，30秒超时则报错隐藏两者并 postMessage 通知父页；静态兜底延迟到揭示后）；父页 App.vue 收到 jiu-xu 前保持透明隐藏，收到 shi-bai 显示可重试可关闭弱提示条+错误上报，应用照常运行；吴昊阳贴图经 Image 预载+Cache Storage+既有 PWA 图片缓存，HTML 地址带版本?v同版命中缓存
- 根因追查（Edge 实测“吴昊阳缺席仍揭露”）：T3 主道路在网格入场景瞬间即置接管标记（零证据盲占领），导致覆盖层隐藏+静态回收，而平面纹理构造器反构出非图片语义的自定义类（map 无 image），角色实际不可见；另健康监测的 %60 检查放在 %2 跳过之后（60 为偶数），自始是死代码，丢失永不降级。修复：接管改为“挂载中+贴图完整+视锥内”连续 5 帧证明；纹理优先克隆场景同类、次选父页 Texture、无 image 绝不进场景；健康监测移至跳过之前并缩至 60 帧；静态回收仅当已证明主道或覆盖层活着；门控按三道路（主道/覆盖/静态）带证据确认
- 根因追查（强制刷新“未染色先展现”）：解析期首跑染色时材质尚不存在只能空跑，而揭示与染色是两条独立竞态，补齐线程还在揭示后即自停（yiJiuXu 单独停），慢加载时揭示先于首次成功染色且此后永不补染。修复：染色升为揭示前置条件——fire() 双 startReveal 前同步跑 __caoDiTongBuZhuTi，__caoDiYiRanSe（实际触及材质计数置位）未真不揭示；补齐线程揭示后未染不自停，染上才停，稳态仍零定时器；画布缺失的阶段记名由 yinQingJiuXu 修正为 huaBuJiuXu
- 根因追查（强制刷新“未染色先展现”复发）：上一轮“揭示前同步染”仍漏——Playwright 实测揭示时 __grassMats 为空而门控已放行。两条：① __dcm 在 DayCycleManager 构造时即置位，不代表资源/草块就绪，门控 S1 只认它等于出生即放行；② 帕姆草（400 丛实例，默认米白 #fff0cf，构造只读配置不读覆盖、事后 bundle 永不重上色）流式构建滞后，旧有各次染色全在其出生前跑完。修复：S1 改认 engine._experienceReady（experience:ready 同步置位，真就绪）；新增事件补染（component:ready 全接约十几次+reveal:complete 落定再压最多 3 次，加载期一次付清，稳态仍零定时器）；揭示前同步染保留为保底；常驻 Playwright 回归 5 用例（新鲜/稳定/主题/慢加载/失败闭环）全过
- 根因追查（强制刷新“双重揭露”）：三路 startReveal 非幂等重置 uRevealProgress——bundle 自动 tryStartReveal、Loading 点击 playIntro 链、门控 fire() 三方各调一次即把进度打回起点，呈现“无背景→露一点→缩回→再揭露”二次揭示；父子双层圆形揭露冗余——父页与引擎内 revealMesh 各做一次圆形扩散，两圆不同步即肉眼双影；tryStart 早点击死链——首屏点击穿透到 Loading 圆环提前触发揭示，未就绪即锁死。修复：引擎揭示单飞守卫（__jieLuYiFaQi 一次门闩+__danFeiYiBao 包裹 revealMesh/postProcessing.startReveal 二次调用不变、tryStartReveal 中和为空函数并经 __experience setter 持续设防）；父层退化为透明度淡入（App.vue iframe 仅 opacity 过渡、无 clip-path，引擎为唯一圆形揭露）；删除 tryStart 早点击死代码链；门控 fire() 成功后补发 reveal:started 与 2 秒后 reveal:complete 双事件供依赖方落定
- 加载分阶段机（卡在哪一步失败原因写哪一步）：qiBu→yinQingJiuXu→huaBuJiuXu→jieShiQiJiuXu→wuZiYuanJiuXu→wuDaoLuQueRen→ranSeJiuXu→jieLuYiChuFa→fuYeYiTongZhi；S1-S3 失败归草地，S4-S5 失败归吴昊阳，ranSeJiuXu 失败归草地；经 ?debug=1 调试面板与 __caoDiMenKongZhuangTai（含 yiRanSe 位）实时可见
- 验收：门控双 true 已揭示；登录/注册/404/聊天挂起/失败横幅/离线 6 场景截图和谐；无未捕获 JS 异常；前后端单测全过；带真实后端全链路复验通过（UI 注册、签名保存、头像裁剪上传、好友搜索申请接受、资料卡、好友消息发送、封禁卡申诉、陌生人空态、吴昊阳双路揭示/失效门控、控制台零错误）；主道证明接管/丢失降级/拒绝横幅均经活体验证；bundle 内部 GPU 回读噪音中经 console.* 发出的走四控制台方法精确过滤，仅 ?debug=1 可见；驱动层直输的（Firefox 侧 READ-usage 措辞，bundle/Worker 全库零该字符串，页面收不到也屏蔽不掉）属加载期有限次无害噪音，不计入零错误口径，内存日志保留

## H-02 头像签名与可见性类

- 需求口径：个人账号添加头像和签名对标微信；头像允许上传各种图片且可选取截图，最终统一1024×1024；签名500字以内默认公开；可见性单独成“类”：公开/仅好友可见/仅部分人可见/不可见/只对自己开放，后续空间/朋友圈/战绩复用同一套逻辑
- 落点：后端新增资料路由（头像上传走媒体 CAS+视觉审核+10年签名 URL、签名保存走文本审核、签名白名单、微信式名片按可见性过滤）+可见性服务（五值判定+好友查询复用）+014迁移（签名可见性/白名单列）+补缺失的001好友与设置表结构文件；前端新增头像裁剪弹窗（拖拽+缩放+1024输出）、用户资料卡、账号页头像签名卡、白名单勾选；修复认证布局 flex 居中致超高内容顶部不可达
- 验收：可见性矩阵/签名校验/头像上传/名片过滤后端单测通过；裁剪纯函数/资料卡/账号页前端单测通过；账号页桌面/移动截图和谐

## H-03 非法内容三级封禁

- 需求口径：用户发送非法数据/图片/视频等，第一次提醒并封禁1分钟，第二次封禁1天，第三次永封；非法数据都撤回；AI 识别不符合法律与价值观的数据
- 落点：账号维度与既有 IP 维度并行（账号记人、IP 防换号）；文本全链路先审后存（AI 聊天正文+媒体配文、好友消息、签名），图片上传时视觉审核，审核服务自身不可用不计为用户违规；拦截在落库前（文件存储层销毁，等同撤回），已落库消息走撤回函数；封禁中禁发（申诉入口除外）；通知+审计留痕；申诉通过清零恢复，管理员解封保留次数再犯升级，管理员可查列表
- 验收：三级时长/封禁门控/签名计数/撤回/申诉后端单测通过；图片机审用例按新行为做用例隔离同步
- 未覆盖：视频视觉内容异步 moderation 缺口（配文文本已审；视频画面理解链路只读未接审核），待产品定夺后实施

## H-04 不出戏降级

- 需求口径：普查返回报错之处，改为既表现出错又不使用户“出戏”、项目正常运行
- 落点：401 静默刷新（refresh 令牌换新后重放原请求，用户无感知；无凭证/刷新失败/认证接口自身才走原有登出）；通用空态组件，好友搜索404走中性空态而非红色报错，好友聊天加载失败/非好友走空态而非整页崩溃；离线横幅验证；后端普查确认320处 shiBaiXiangYing 全部走翻译文件、内部错误只进掩码日志；单测 hermetic（jsdom URL 避开本地后端端口，漏 mock 请求不再受后端启停影响）
- 验收：静默刷新4用例/404空态/好友聊天失败态/原有401行为单测通过；离线横幅截图和谐；全量单测在后端启停两种状态下均为零错误

## I-01 本轮九项治理

- 需求口径：主页未被染色；账号设置缺头像签名聊天背景且右侧三角太大；聊天页进好友页后返回要点两次（第一次只变背景）；greedisgood 最小化无反应；变量类名用中文；AI深度思考无用且缺排序；保留AI历史构建信息；每轮回复的经历/评分聚合展示；返回主页重复加载背景
- 落点1主页染色：主页新增染色罩染层跟随账号设置聊天背景（6选项同源，半透明透出草地与吴昊阳）；草地首帧即执行主题染色（此前只靠400ms轮询）
- 落点2账号设置：头像/签名/聊天背景本就存在于账号页，缺的是菜单入口——全局菜单下拉顶部新增资料头（头像+昵称+签名，对标QQ/微信）与更换头像/编辑签名/聊天背景快捷项（带锚点直达对应卡片）；右侧▶/▾字符三角改为CSS绘制小三角
- 落点3好友返回：资料卡发消息先关卡再跳；好友聊天新增显式返回顶栏（单次直返，栈空时替换到列表）；空态返回由push改为replace防栈膨胀；全局返回栈空时替换到主页防点击无反应；好友聊天草地挂起与AI聊天一致消除背景闪变体感
- 落点4监控最小化：最小化缩为右下角slim长条（标题+计数+展开/关闭）；拖拽改为阈值式（5px内为点击，按钮点击不再被指针捕获吞掉）
- 落点5中文命名：本次触及范围拼音标识改为中文（监控组件/聊天仓库监控段/调度器深度思考方法）；好感度四维监控键改为信任/亲密/趣味/关怀；DB列与对外API契约保持不变
- 落点6深度思考与排序：DeepSeek reasoning_text经Director/Writer/AI引擎透传为si_kao，调度器以管理员_深度思考事件推送（截断1500字），监控实时展示；监控头新增时间升序/降序切换（默认降序）
- 落点7历史保留：四类监控按会话存localStorage（各100条），切会话先存后读，关闭重开直接可见；旧内心消息兼容进历史回看
- 落点8轮次聚合：后端全部管理员事件带轮次；监控改按回复轮次聚合卡片（一轮的思考/构建/回复/评分理由/隐藏收拢），未带轮次旧事件进历史事件时间线
- 落点9背景秒开：草地iframe常驻，聊天类路由（AI聊天+好友聊天）改为屏外2px微窗隐藏（文档与WebGL上下文保留，rAF链不断，正尺寸绘制无零尺寸GL报错），返回直接显示不重载；未登录公开页不拉取设置（此前菜单挂载即请求刷401）
- 验收：前后端全量单测通过（前端804/后端751，含新增轮次聚合/排序/长条/持久化/思考透传用例）；tsc/eslint双端零报错；构建通过；带真实后端全链路复验通过（主页双染色、账号锚点、好友空态、监控展开/长条/真实AI轮次卡片、控制台零错误零不可接受警告；GL性能噪音按既有忽略口径过滤）

## J-01 全端一致收口（六项根因治理验证）

- 需求口径：全端一致+依赖最新+全量验证截图收口，只改和我恋爱吧，不加新功能
- 落点1依赖升级（semver兼容版，package.json范围不动、锁文件同步，npm audit双端0漏洞；major/0.x次版跨越全部保留）：前端vite 8.2.2→8.3.0、@vitejs/plugin-vue 6.0.6→6.0.8、vitest 4.1.10→4.1.11、eslint 10.3.0→10.10.0、typescript-eslint 8.59→8.70、three 0.185.0→0.185.1、vue-router 5.3.0→5.3.1等；后端vitest 4.1.6→4.1.11、openai 6.37.0→6.49.0、pg 8.20.0→8.23.0、nodemailer 9.0.5→9.1.1、helmet 8.1.0→8.3.0、eslint 10.5.0→10.10.0等；保留vitest 5/jsdom 30/TS 7/three 0.186/ioredis 6/redis 6/openai 7/nodemailer 10（破坏性升级）
- 落点2全量验证：前端836/836、后端751/751（含500用户E2E：通关500/500，P50=35≤40、P90=39≤46），双端tsc+eslint（后端仅src，用户WIP根脚本163项排除）零错、双端构建成功（前端dist 9.61MB<15MB门禁）；Playwright新增fp06-shou-wei.spec.ts，主页/个人设置/聊天监控三场景桌面+移动6截图落盘测试截图（FP06-*.png），横溢/顶栏三区/昵称角色名三断言+控制台零错误（字体按location过滤、警告仅GPU/READ既有口径）
- 落点3移动顶栏收口：全局菜单移动端grid改auto/minmax(0,1fr)/auto、隐藏用户昵称、角色名上限80px、聊天页内隐藏主页图标（:has作用域），标题省略号收尾不再与昵称/军师按钮交叠
- 落点4遗留闭环：FP-04遗留/api/logs 500系后端未运行所致环境问题，运行中后端复验POST /api/logs→200 {"cheng_gong":true,"shu_ju":{"jie_shou":true}}，非法体仍400，零代码改动
- 验收：见落点2；粒子画布为动态背景（DOM框无错位、无横溢），抓拍帧间密度差异属正常；后端全量曾因与前端升级/浏览器并行争用出现1例连接池超时，隔离单跑通过归环境类

## 范围边界

- 做什么：FP-01 至 FP-09（本次9项根因治理循环）
- 不做什么：不做语音视频通话，不做小程序端独立实现，不重写整体技术栈
- 禁止触碰：总控制台核心逻辑、`.env` 密钥、用户 WIP 未确认文件、MMORPG 服务端与其他项目目录

## FP-01 音频与吴昊阳根因

- 需求口径：网站控制台15个sound资源在/grass-bg/audio下404刷ERR且背景吴昊阳反复消失，着重修复并完整测试；THREE着色器X4122/X4008警告同步清理
- 根因：public/grass-bg缺audio目录（为省17MB未复制），bundle经FileLoader(fetch/arraybuffer)+decodeAudioData加载，head内fetch/XHR静音拦截盖不住AudioLoader错误回调的console.trace；trace/warn未进debug门控故刷屏；吴昊阳覆盖层失败时无定时静态兜底
- 落点（v0.7 2026-09-07）：public/grass-bg/audio下生成17个静音占位mp3（WAV头，解码器按内容嗅探可解）；grass-bg.html内console.trace/warn同样进内存收集+音频/着色器噪音过滤+debug门控；吴昊阳15秒未就绪自动切静态图；保留fetch/XHR静音拦截为双保险
- 验收：控制台音频ERR与X警告零刷屏（非debug），音频占位文件存在，草地静态测试通过，角色可见回归通过

## FP-02 输入框绿线去除

- 需求口径：每个输入框上下两条绿线不好看，去掉
- 根因：全局: focus-visible outline用--zhuse绿色（浅#07C160/深#34C759），.shuru-kuang:focus边框同为绿色，全端输入框聚焦即现绿线
- 落点（v0.7）：global.css聚焦描边改为none，.shuru-kuang:focus边框回归中性边框色；按钮链接绿色保留
- 验收：聚焦样式无绿色断言测试通过，全端一致

## FP-03 TA随机性格提示与兼顾

- 需求口径：心目中的TA添加提示：若选择随机性格，则会尝试按照通用提示词中的描述，去选择一个最接近该人物的性格特征；用户已选性格与通用提示词指向冲突时不忽略用户选择、同时兼顾通用提示词
- 落点（v0.7）：翻译新增xinMuZhongSuiJiXingGeTiShi/xinMuZhongYiXuanJianGuTiShi/taDaiTiNan/taDaiTiNv并在步骤3渲染；渣型提示代词走翻译（删硬编码他/她）；前后端同源关键词评分选最接近MBTI（平票取后字母INFP向），显式选择永远优先、通用提示词追加进背景故事兼顾；前端抽取utils/通用提示词性格单源复用
- 验收：前后端性格推测单测（ENFP/ISTJ/平票INFP/冲突保留选择）通过

## FP-04 语音气泡微信样式与播放轴

- 需求口径：聊天语音气泡未播放时改为微信语音条样子，播放时改为可拖动时间轴
- 落点（v0.7）：未播放态渲染public/weixin-yuyin-tiao.png（源自项目根微信语音条.png，改ASCII名避Vite解析）；播放态渲染range滑杆+进度/总时长，可拖动seek；use语音播放新增进度跟踪与跳转API；动态:src避测试期静态解析
- 验收：样式断言与播放器单测通过，全端一致

## FP-05 翻译语言可选

- 需求口径：翻译额外可选择翻译前语言和翻译后语言；默认按原句（可能混杂）译为中文
- 落点（v0.7）：后端fanYiWenBen(yuanYu,muBiaoYu)，默认auto→zh，8语种归一化；路由透传yuanYu/muBiaoYu；前端api透传，use长按菜单新增fanYiYuanYu/fanYiMuBiaoYu（auto/zh），气泡下增双下拉
- 验收：翻译与转写单测（默认中文/指定英文/非法回退/空超长）通过

## FP-06 语音转文字失效根因与展示修正

- 需求口径：语音转文字失效——告知原因与解决办法；删掉输入框下方的失败提示；气泡下失败改为红色（与正常区分）；失败文本改为可选中普通文本，点击不再重试
- 根因：前后端两处本地转写均用“扬声器播放+麦克风SpeechRecognition”回路：Web Speech API只收麦克风不收文件流，播放需用户手势且常被自动播放策略拦截，https/麦克风/Chrome缺一即失败，故普遍失效；失败又经sheZhiCuoWu抛到输入框下全局错误
- 解决办法：三级链路——随消息转写优先（发送时入库），远端免费转写次之（POST/聊天/语音/转写经硅基流动ASR，未配置降级），本地识别最后兜底；失败只记气泡下内联红字态，不再调sheZhiCuoWu；失败态由button改为span可选中无点击重试，重试走右键菜单
- 落点（v0.7）：use语音转文字重写，sheZhiCuoWu改为可选且不再调用；聊天页失败span+红字可选CSS；FP07/09/10旧断言同步新规
- 验收：语音菜单/展示/多模态单测通过

## FP-07 多模态审计结论

- 需求口径：审查代码，确认语音全识别（文字+歌曲前奏主歌+动物鸣叫全识别经聚合站转主模型可读格式再回复，省钱优先）、转文字用免费模型单独做、图片识别+按需生图、视频看懂（含声画）+按需生成、全端可发可听可看；通话不做
- 审计结论（v0.7）：已做到——语音发送时免费转写入库，音频事件经硅基流动理解后与转写合并为[语音转写][音频事件](N秒)进DeepSeek（覆盖 speech+音乐+鸣叫语义）；转文字按钮走免费链路（随消息→远端免费→本地）；图片识别经DeepSeek视觉+按次生图落库角色消息；视频文本化[视频/画面/声音转写]进DeepSeek+按次生成视频落库文件消息；生图/视频分别每日计数超限429；密钥走后端env可配置热重载，前端脱敏，日志掩码，失败翻译降级；通话未做。未配置密钥时降级为本地转写直入，属预期降级非缺失
- 验收：FP10前后端多模态单测通过

## FP-08 排位赛秘籍与反馈

- 需求口径：排位赛可以使用秘籍；greedisgood输入后无任何浮窗/页面/反馈
- 根因：后端sheZhiMiJiHaoGanDu硬禁tiaozhan对局（P0-2排位防刷）；前端greedisgood仅管理员开监控、非管理员零反馈
- 落点（v0.7）：删除挑战禁令（排位与普通一致生效）；greedisgood对所有用户弹3秒自动消失浮窗（翻译键miJiFuChuangBiaoTi/MiaoShu），管理员仍同步打开监控；好感度/消息旧403断言同步新规
- 验收：秘籍单测（挑战生效）与浮窗单测通过

## FP-09 全量回归与清理

- 需求口径：按需求文档开发，文档与代码完全一致（冲突按本次用户描述为准并同步文档，大面积更新）；技术新颖有难度架构优秀性能极致对标大厂国家级；按skill全流程测试每部分验证，0错误最少警告
- 落点（v0.7）：本文档升v0.7覆盖9项；技术栈保持Vue3+Vite+TS/Express+PG+Redis现状做架构内优化（未重写）；前端全量单测通过（旧断言按新规同步），后端目标套件通过，tsc/eslint双端零报错；草地17音频占位+微信语音条图为新增public资产
- 验收：测试构建lint全过，无冗余屎山（性格评分前后端单源、转写三级链路单源、翻译单源）

## G-01 吴昊阳与草地融合

- 需求口径：背景的吴昊阳必须与草地背景真正融为一体；草地加载后吴昊阳没有加载出来
- 根因：吴昊阳显现依赖引擎中心扩散进度，源未就绪时兜底长达12秒加8秒淡入，静态图兜底15秒；体感即草地就绪而角色长期缺席
- 落点（v0.8 2026-09-08）：贴图预加载；不透明度取扩散进度与时间下限较大者，时间下限2.5秒起至10秒淡满，保证与草地同屏出现；引擎失效或进度停滞时6秒切静态图兜底，3D就绪后自动升级收回静态图；日志与回调加页面卸载守卫，根除后台报错；引擎内平面仍为主道路，覆盖层在其接管时自动隐藏
- 验收：管线全通（平面入场景、遮挡开启、110个材质压弯注入、进度同源），静态兜底在引擎失效时可见，真机实测以GPU呈现为准

## G-02 TA年龄归一

- 需求口径：心目中的TA选填描述中年龄的（0-100）删掉；用户输入其他数字时取最接近的合法数字
- 落点（v0.8）：描述与占位不再出现0-100；前后端统一归一（四舍五入后钳制0-100，空值保持选填）；后端越界不再400
- 验收：150归一100，负数归一0，21.5归一22，单测通过

## G-03 聊天页对标微信

- 需求口径：聊天页仿照真实中国版微信重做；翻译默认不显示原文语言等选项，点击翻译后才显示；语音气泡参考微信但不得直接贴参考图，需代码绘制
- 落点（v0.8）：语言双下拉仅在翻译中或展开后渲染，切换语言即强制重译；未播放态语音图标改为内联SVG自绘，发送方镜像，气泡补齐与文本一致的三角尾；头像由圆形改为微信式圆角方块；录音面板改为深色HUD；语音与视频通话入口移除
- 验收：默认无语言行，译后出现；无png语音条引用；前后端单测同步新规

## G-04 军师记录背景

- 需求口径：军师指导记录采用聊天页面的背景，而不是主页的背景
- 落点（v0.8）：记录详情页与指导记录弹窗背景统一为聊天变量
- 验收：视觉回归通过

## G-05 军师头像

- 需求口径：军师头像全部失效
- 根因：中文路径未编码，部分服务端对未编码中文路径返回404；且无加载失败兜底，坏图直接破裂
- 落点（v0.8）：头像地址统一编码；两处军师头像均加失败兜底（首字渐变块）
- 验收：断图时显示兜底而非破裂，单测断言编码路径

## G-06 语音转文字失败红色

- 需求口径：语音转文字失败提示改为红色字体，与正常文本区分
- 根因：失败类选择器优先级低于用户气泡文本色，用户侧失败态被覆盖回黑色
- 落点（v0.8）：补充分侧高优先级红色规则
- 验收：双侧失败态均为红色可选文本

## G-07 多模态审计结论

- 需求口径：审查语音全识别（文字加歌曲前奏主歌加动物鸣叫经聚合站转主模型可读格式再回复，省钱优先）、免费模型单独转写、图片识别加按需生图、视频看懂加按需生成、全端可发可听可看；通话不做
- 审计结论（v0.8）：未完全做到——语音理解调不存在的聚合站理解接口，转写调转写接口但用JSON传媒体ID而非文件，均恒失败；视频无真实理解，仅文件名加时长占位；图片识别与按次生图链路正确
- 落点（v0.8）：转写改为按媒体ID查库取文件后走标准分片上传，上传超时下限提至120秒并按MIME补扩展名；生图兼容地址式回包并单独延长下载超时；视频生成改异步提交加轮询加下载，轮询上限280秒；音频事件改为零成本标签提取并入两处提示词入口；新增视频理解模块（ffmpeg可选，抽中间帧走视觉模型描述加20秒音频走免费转写，结果缓存7天），最新用户视频消息经调度器自动解析入库，无ffmpeg或无密钥时占位降级；通话入口移除；未配置密钥时仍降级，属预期降级非缺失
- 验收：转写格式、标签提取、视频理解单测通过；真机实测：6秒中文语音转写逐字命中，Kolors生图1.5MB有效PNG，主模型视觉复述命中拥抱与日落，Wan2.2文生视频198秒产出2.8MB有效MP4，抽帧视觉复述命中海鸥与海浪

## G-08 社交与账号

- 需求口径：每位玩家可换聊天背景；好友系统可与现实好友聊天且保留军师指导；可按多种信息搜索好友；账号绑定手机号、邮箱、抖音，需付费则暂未开放；隐私权限可设公开与否及公开字段，排位赛数据不能隐藏只能清空；可见UID；可注销
- 落点（v0.8）：新增好友申请单表、用户设置表、好友消息表及迁移；好友搜索、申请、接受拒绝、列表删除、好友消息收发撤回已读全链路；账号页新增UID复制、绑定位（手机已绑展示，邮箱与抖音暂未开放）、隐私三开关、六套聊天背景、排位赛清空；聊天页与好友聊天页均应用所选背景；全局菜单新增好友入口；军师指导保留
- 验收：好友与设置后端全链路单测通过，前端列表挂载与翻译键单测通过

## G-09 秘籍浮窗

- 需求口径：删除greedisgood的已启用提示浮窗，改为真正可用的管理员功能性浮窗，可视化AI实时想法、后台文本数值与隐藏内容
- 落点（v0.8）：删除提示浮窗模板、状态与样式；秘籍对所有输入者直接打开既有管理员实时监控浮窗
- 验收：输入后无提示浮窗且监控浮窗出现，单测同步新规

## G-10 排位赛字样隐藏

- 需求口径：隐藏主页挑战模式中的排位赛三个字
- 落点（v0.8）：移除主页挑战卡片角标元素与样式，翻译键保留兼容
- 验收：主页不再渲染该字样，单测同步新规

## 历史版本存档（v0.6及之前FP口径，已被v0.7九项根因治理覆盖或替代，仅保留备查，后续变更以v0.7章节为准）

## FP-02 登录（历史）

- 需求口径：登录密码浮动标签重叠修复
- 验收：有值/聚焦/自动填充态标签上浮无重叠

## FP-03 按钮

- 需求口径：主页开始体验深色 hover 同色
- 验收：深浅色常态 hover 同一颜色

## FP-04 TA 三框+通用提示词

- 需求口径：心目中的 TA 重构三框+通用提示词+对比色；输入框优先、缺失标不重要低比重随机
- 细化：仅微信名/真实姓名/年龄 0-100 可选；提示词提取输入框优先，缺失标不重要；展开收起深白浅黑
- 落点（v0.2 2026-09-05）：表单仅微信名（30字）/真实姓名（20字）/年龄（0-100整数）可选加通用提示词（500字）多行框，职业城市家乡身份旧输入框及前端身份常量已删；年龄越界/非整数/提示词超长后端400；提示词按确定性规则提取（年龄数字加岁、城市库、身份四值、职业库、家乡库关键词命中，原文清洗后追加进背景故事），输入框与提取冲突时输入框优先，缺失项走现有随机（相对显式指定为低比重）；
- 验收：仅上述三框可选，提示词按「输入框优先、缺失标不重要」规则，展开收起对比色符合口径
- 对比色落点：展开收起标题与状态文案共用主题变量单源（深色白、浅色黑），区块背景与边框同变量单源，悬停仅位移无变色，浅色下输入区同步浅底黑字

## FP-05 无空格

- 需求口径：全站文本数字字母前后无空格审计
- 落点（v0.5 2026-09-06）：前后端翻译零命中、前端模板纯文本零命中、后端路由零命中；修复页描述1处（AI恋爱模拟）、协议两文本57处（章节号空格、以base64编码、localStorage等去空格）；门禁8项含页描述与协议文本；用户可见文案无硬编码新增，日志与AI提示词非界面文案不计入
- 验收：脚本零命中+人工抽查

## FP-06 自由职业

- 需求口径：身份新增自由职业单源
- 验收：前后端同一枚举四值全流程一致

## FP-07 语音菜单两项

- 需求口径：语音条右键仅语音转文字+引用
- 验收：菜单两项功能对标微信

## FP-08 文本菜单复制翻译引用撤回

- 需求口径：文本条右键仅复制+翻译+引用+撤回
- 单源：前端消息配置 wenBenCaiDanXiang 固定四项顺序 fuZhi/fanYi/yinYong/cheHui，聊天页面经 huoQuWenBenCaiDanXiang 单源渲染，标签全走翻译文件 liaoTian 键
- 复制：剪贴板优先 navigator.clipboard，降级 textarea+execCommand，失败报 fuZhiShiBai
- 翻译：前端 fanYiWenBen 经 POST /聊天/翻译走后端 DeepSeek 中英互译通道，缓存+展开收起复用，结果气泡下展示，失败报 fanYiShiBai，请求中展示 fanYiZhong
- 引用：复用语音文本共享引用链路 yinYongXiaoXi，摘要截断 yinYongZhaiYaoZuiDaZiFu，可取消
- 撤回：仅用户 2 分钟内可见撤回（前后端同源 cheHuiShiXian），角色与超时文本仅三项无撤回
- 验收：菜单四项功能对标微信

## FP-09 语音转文字微信式

- 需求口径：语音转文字微信式展示
- 落点（v0.4 2026-09-05）：气泡下四态对标截图——结果条（圆角6px/上边距6px/内边距8px12px/14px/换行，随气泡分侧：用户绿角色灰）、加载（yuYinZhuanWenZiZhong）、失败（内联yuYinZhuanWenZiShiBai可点击重试）、收起（点击结果条收起，aria/title复用zheDie）；链路仍免费本地（不依赖后端接口）；文案全走翻译文件，未新增键；翻译结果复用同类名不受影响（手形光标仅结果/失败按钮）
- 验收：气泡下转文字样式对标截图

## FP-10 多模态 C 混合

- 需求口径：多模态 C 混合（转文字免费本地、理解经硅基流动转 DeepSeek-V4-Flash-Vision-Exp、图片识别+按次生图、视频理解+按次生成、不做通话）
- 细化：语音全识别经硅基流动转 DeepSeek-V4-Flash-Vision-Exp；转文字免费本地；图片识别+按次生图；视频看懂+按次生成；全端可发可听可看；通话不做
- 落点（v0.6 2026-09-06）：语音发送经免费本地ASR转文字随消息入库（前端本地转写透传，后端保留转写不再丢弃），音频事件经硅基流动理解后与转写合并为DeepSeek可读文本（[语音转写][音频事件](N秒)）再进回复，未配置降级为本地转写直入；转文字按钮保持免费本地（无后端依赖）；图片识别经DeepSeek-V4-Flash-Vision-Exp视觉管道回答，生图按次经生图接口落库为角色图片消息；视频以[视频：名，N秒][画面][声音转写]文本化进DeepSeek，生成视频按次经生成视频接口落库为文件消息，生图与生成视频分别每日计数超限429；通话不做；供应商密钥走后端.env四组开关加模型加每日上限加超时可配置，多模态配置查询加管理重载支持热重载，前端视图脱敏无密钥，日志掩码默认含三供应商密钥字段，失败走翻译降级，全端文案走翻译文件
- 验收：语音全识别经硅基流动转 DeepSeek-V4-Flash-Vision-Exp，转文字免费本地，图片识别+按次生图，视频看懂+按次生成，全端可发可听可看，通话不做

## FP-11/FP-12 占位

- FP-11：本文件即 FP-11 首版框架交付物；验收为文件存在且覆盖 10 项口径、与 PROGRESS 一致
- FP-12：全量回归性能清理占位，验收：测试构建 lint 全过，无冗余屎山，过程文件待删清单就绪（待补充）

## 变更记录

- v0.1 2026-09-05：FP-11 首版框架新建，纳入 FP-01 至 FP-10 用户口径
- v0.2 2026-09-05：FP-04 落点同步（三框加提示词、0-100校验、输入框优先、对比色单源）
- v0.3 2026-09-05：FP-08 落点同步（单源四项顺序、剪贴板复制、后端AI翻译通道、共享引用链路、用户2分钟撤回）
- v0.4 2026-09-05：FP-09 落点同步（气泡下四态结果条/加载/失败/收起、分侧颜色、失败可重试、免费本地不变、文案全走翻译）
- v0.5 2026-09-06：FP-05 落点同步（页描述1处+协议57处去空格、门禁8项、翻译模板路由零命中）
- v0.6 2026-09-06：FP-10 落点同步（本地ASR转写入库、硅基流动音频事件合并可读文本、转文字免费本地不变、图片识别加按次生图、视频文本化加按次生成、通话不做、env可配置热重载、日志掩码含三密钥、失败翻译降级）
- v0.7 2026-09-07：9项根因治理（音频占位+trace/warn门控+吴昊阳15秒静态兜底、输入框绿线去除、TA随机最接近性格+冲突兼顾、语音气泡微信图+可拖动轴、翻译8语种可选默认译中、转写三级链路+内联红字可选中、多模态审计、排位秘籍放行+greedisgood浮窗），旧403/无后端依赖/全局错误断言按用户口径同步新规
- v0.8 2026-09-08：10项体验与社交治理（吴昊阳时间下限淡入+预加载+静态升级、TA年龄归一、翻译懒显+SVG自绘语音+圆角方头像+深色录音HUD+通话入口移除、军师记录聊天背景、军师头像编码加兜底、失败态双侧红色、多模态转写分片加长超时+生图地址下载+视频异步提交轮询+零成本事件标签、好友系统加用户设置加排位清空、秘籍直开监控、主页排位角标隐藏）
- v0.9 2026-09-09：H-01 门控揭示（双就绪圆形扩散+超时报错隐藏+缓存）、H-02 头像签名与可见性类（资料路由+裁剪1024+名片+布局顶部可达修复，补001表结构文件）、H-03 三级封禁（账号维度1分钟/1天/永封+撤回+申诉+解封）、H-04 不出戏降级（401静默刷新+通用空态+离线验证）
- v1.1 2026-09-10：J-01 全端一致收口（双端依赖最新兼容版+锁同步+0漏洞、全量单测前端836后端751、tsc/eslint零错、双端构建成功、6截图落盘控制台零错误、移动顶栏三区收口、/api/logs复验200）
- v1.2 2026-09-11：H-01 揭示加染色前置（fire双startReveal前同步染色、__caoDiYiRanSe未置位不揭示、补齐线程揭示后未染不自停、阶段记名修正+门控状态加yiRanSe位；READ回读类警告取证为浏览器驱动层直输、页面无法屏蔽，属有限次无害噪音）
- v1.3 2026-09-11：H-01 真就绪门控+事件补染（S1改认engine._experienceReady、component:ready全接约十几次、reveal:complete落定补压最多3次；帕姆草米白滞后尾根因；常驻Playwright染色回归5用例全过）
- v1.4 2026-09-11：H-01 强制刷新双重揭露治理（三路非幂等+父子双圆+早点击锁死根因；单飞守卫+父层淡入+删死链+补发双事件落点；FP-03冷载回归桌面+移动单调无归零截图落盘控制台零错误）
- v1.0 2026-09-10：I-01 本轮九项治理（主页染色罩染+草地首帧染色、菜单资料头与三快捷项+CSS小三角、好友单次返回+显式顶栏+replace、监控slim长条+阈值拖动、中文命名、深度思考打通+排序切换、按会话持久化、按轮次聚合卡片、背景微窗常驻秒开+未登录不拉设置）

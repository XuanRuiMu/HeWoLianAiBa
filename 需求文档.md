# 和我恋爱吧 — 完整需求文档（PRD）

<!-- markdownlint-disable-file MD029 MD024 MD013 -->
<!-- 说明： -->
<!-- 1. PRD 规则列表因表格/代码块分隔被 markdownlint 识别为新列表，但语义上需保持连续编号（如 FP-10 第7-10条、FP-18 第3-7条），故禁用 MD029。 -->
<!-- 2. 各功能点均使用"规则"/"可机器验证验收标准"作为统一标题，属有意结构重复，故禁用 MD024。 -->
<!-- 3. 文档存在大量代码路径、URL、长句等自然长行，统一禁用 MD013 行长度检查。 -->

> 本文档按用户旅程重组功能点，每个功能点标注依赖关系，验收标准全部转化为可机器验证的测试条件。配合"循环工程"skill使用时，一个功能点 = 一个Task子代理任务。

---

## 一、项目概述

### 1.1 项目定位

"和我恋爱吧"是一款 AI 恋爱模拟聊天 Web 应用，用户通过模拟微信聊天界面与个性化 AI 角色展开沉浸式恋爱互动。核心价值：

- **练习恋爱聊天**：在零风险环境中练习线上恋爱沟通技巧
- **AI 沉浸式角色扮演**：16种MBTI性格 + 渣男渣女变体，每个角色有独立人设、背景、微信昵称
- **军师指导**：恋爱军师实时分析聊天并给出建议
- **复盘提升**：游戏结束后AI生成复盘报告，帮助用户改进

### 1.2 核心技术栈

不列出具体的技术栈名称和版本。技术栈应随着项目的需求而不断改进。版本必须选用最新的、可用的测试版本。

### 1.3 命名规范

**核心原则：项目全部代码用全中文，包括数据库表名/字段名、API路径、文件名、Socket.IO事件名、变量/函数/类名。**

| 类别 | 规则 | 示例 |
| --- | --- | --- |
| 变量/函数/类名/接口/枚举 | **全中文** | `好感度`、`发送消息`、`聊天管理器`、`获取好感度阶段` |
| Vue组件内命名（变量/函数） | **全中文** | `const 好感度 = ref(500)`、`function 发送消息()` |
| Socket.IO事件名 | **全中文** | `角色回复`、`消息撤回`、`游戏事件`、`对方正在输入` |
| 文件名 | **全中文** | `聊天管理器.ts`、`好感度服务.ts`、`用户认证.vue` |
| 数据库表/字段 | **全中文** | `用户`表、`角色`表、`好感度`字段、`信任度`字段 |
| API路径 | **全中文** | `/api/认证/登录`、`/api/聊天/发送` |
| 用户可见文本 | 必须走翻译文件，禁止硬编码 | — |

**数据库中文标识符注意事项**：

- PostgreSQL 16 默认 UTF-8 编码，完全支持中文表名/字段名
- SQL 语句中中文标识符需用双引号包裹，如 `SELECT "好感度" FROM "用户" WHERE "手机号" = $1`
- 数据库迁移脚本从零用中文重写，删除旧的拼音迁移文件（001-020.sql）

---

## 二、范围边界

### 2.1 做什么（要实现的功能点）

实现 FP-01 ~ FP-21 共21个功能点，覆盖用户认证、游戏入口、角色生成、聊天互动、多媒体消息（图片/表情包/文件/语音条，FP-20）、语音视频通话（FP-21）、AI对话引擎、好感度系统、胜利失败条件、军师指导、战绩复盘、过往战绩敏感信息保护、通知系统、管理员后台、安全合规、主题UI。

### 2.2 不做什么（范围外功能）

1. **挑战模式**：暂时搁置，主页入口显示"即将推出"，不实现具体功能
2. **朋友圈功能**：暂不实现
3. **多语言国际化**：暂时仅中文，未来增加
4. **PWA离线功能增强**：当前无PWA配置
5. **管理员后台界面**：仅有API，无前端界面
6. **社交场批量生成**：代码存在但未启用
7. **积分系统与积分榜/排行榜**：暂时搁置；不实现积分记录、排行榜UI、排行榜API及积分相关管理功能
8. **真实WebRTC媒体转发与语音/视频大模型接入**：语音/视频通话仅实现信令+UI+记录（见FP-21），媒体层暂不接入AI
9. **AI主动发送图片/表情包**：仅用户可发多媒体消息，AI通过视觉理解用户发来的图片/表情包（见FP-20）

### 2.3 代码修改原则

- **允许修改一切代码**：AI可根据需求进行激进的重构、修改、删除任何现有代码，不受"增量更新"约束
- **严格按照需求文档和skill规范编写**：所有代码必须符合1.3命名规范、第六章非功能需求、第七章补充说明
- **重构优先于保留**：当现有代码与需求冲突时，按需求重写而非妥协保留

---

## 三、用户角色与权限

### 3.1 角色定义

| 角色 | 标识 | 权限 |
| --- | --- | --- |
| 普通用户 | `管理员 = false` | 注册/登录/聊天/军师/战绩/通知 |
| 管理员 | `管理员 = true`（唯一事实来源为 `ADMIN_PHONES` 环境变量；数据库 `用户.管理员` 字段仅为镜像，每次登录时自动与 `ADMIN_PHONES` 对账同步，二者不一致以 `ADMIN_PHONES` 为准并回写数据库） | 普通用户权限 + 用户管理 + 夺舍角色 + 发送通知 + 系统状态 |
| 开发者 | 管理员子集（通过管理员JWT认证） | 管理员权限 + 创建测试用户 + 删除用户 |

### 3.2 认证机制

- **JWT认证**：Bearer Token，默认过期7天，存储于前端 `localStorage.令牌`
- **Socket认证**：`socket.handshake.auth.token` 携带JWT
- **管理员认证**：通过 `验证管理员` 中间件校验手机号是否在 `ADMIN_PHONES` 中；登录接口（`dengLu`）在每次登录时执行身份对账——若 `ADMIN_PHONES` 判定结果与数据库 `用户.管理员` 不一致，则以 `ADMIN_PHONES` 为准更新数据库并返回最新身份，杜绝双来源漂移
- **公开路由白名单**：检查手机号、发送验证码、注册、登录、健康检查、角色列表
- **废弃x-admin-token**：所有安全路由统一使用JWT体系，不再使用独立的 `x-admin-token`

---

## 四、项目流程与依赖关系

### 4.1 用户旅程流程

```text
注册/登录（FP-01）
    ↓
主页（FP-02）
    ↓ 选择普通模式
资料设置向导（FP-03）
    ↓ 完成（点击"开始聊天"，立即路由跳转，不等待网络）
"添加微信"过渡页（FP-05 加载界面，生成流程托管于 Pinia store）
    ↓ 真实生成流程（人设生成 → 保存+开场白 → 创建会话）完成（store 完成回调驱动导航裁决）
聊天页（FP-05，停留加载页时直达）/ 过往战绩（FP-13「进行中」分组，离开时静默进入）
    ↓ 从战绩进入某局对话
聊天界面（FP-05）
    ↓
消息发送与显示（FP-06）←→ AI回复机制（FP-07）←→ 消息撤回（FP-08）
    ↓                                          ↑
    └→ AI对话引擎（FP-09）←→ 好感度系统（FP-10）
    ↓
胜利失败条件（FP-11）
    ↓ 游戏结束
过往战绩与复盘（FP-13）← 军师指导系统（FP-12，游戏中可用）
    ↓
过往战绩多选与移动（FP-14）

横切关注点：
- 过往战绩敏感信息保护（FP-15）：战绩/复盘/军师记录全程生效
- 通知系统（FP-16）：全程可用
- 管理员后台（FP-17）：管理员可用
- 安全与合规（FP-18）：全程生效
- 主题与UI（FP-19）：全程生效
```

### 4.2 功能点依赖关系图

| 功能点 | 依赖 | 说明 |
| --- | --- | --- |
| FP-01 用户认证 | 无 | 基础模块 |
| FP-02 主页与模式入口 | FP-01 | 需登录 |
| FP-03 资料设置向导 | FP-02 | 从主页进入 |
| FP-04 AI角色生成 | FP-03 | 向导跳转加载页后由加载页（FP-05 过渡页）onMounted 发起托管 |
| FP-05 聊天界面 | FP-04 | 需加载页真实生成流程完成（依赖 FP-04 角色落库） |
| FP-06 消息发送与显示 | FP-05 | 聊天界面基础 |
| FP-07 AI回复机制 | FP-06, FP-09, FP-10 | 需消息系统+AI引擎+好感度 |
| FP-08 消息撤回 | FP-06 | 基于消息系统 |
| FP-09 AI对话引擎 | FP-06 | 处理用户消息 |
| FP-10 好感度系统 | FP-09 | AI评估好感度变化 |
| FP-11 胜利失败条件 | FP-09, FP-10 | 基于好感度和表白检测 |
| FP-12 军师指导系统 | FP-06, FP-09, FP-10 | 分析聊天+AI+好感度 |
| FP-13 过往战绩与复盘 | FP-11 | 游戏结束后生成 |
| FP-14 过往战绩多选与移动 | FP-09 | 支持多选、分类、拖拽排序 |
| FP-15 过往战绩禁止暴露敏感信息 | 无 | 战绩/复盘/军师记录相关服务与前端 |
| FP-16 通知系统 | FP-01 | 需用户身份 |
| FP-17 管理员后台 | FP-01 | 需管理员身份 |
| FP-18 安全与合规 | FP-01 | 全局中间件 |
| FP-19 主题与UI | FP-01 | 全局UI |
| FP-20 多媒体消息 | FP-03, FP-05, FP-06 | 图片/表情包/文件/语音条 |
| FP-21 语音视频通话 | FP-05, FP-06 | 信令+UI+记录（媒体层暂不接AI） |

### 4.3 游戏状态机

```text
[进行中] ──好感度≥800且表白成功──→ [胜利-爱情]（可继续聊天）
    │
    ├──好感度≤0──→ [失败-好感度归零]
    ├──过早表白（好感度<800）──→ [失败-过早表白]
    ├──用户说"互删吧"（正常角色）──→ [失败-互删失败]
    ├──用户错误识破正常角色为渣型──→ [失败-错误识破]
    ├──AI主动表白后用户拒绝──→ [失败-拒绝表白]
    │
    ├──（渣男渣女）用户说"互删吧"──→ [胜利-互删胜利]
    ├──（渣男渣女）用户识破渣型──→ [胜利-识破]
    ├──（渣男渣女）用户向渣型表白──→ [失败-被欺骗]（直接失败，无需揭露环节）
    ├──（渣男渣女）用户接受渣型主动表白──→ [失败-被诈型欺骗]
    └──（渣男渣女）用户发送无厘头内容（无论AI人设是否发散思维）──→ [胜利-神经病]

新增[失败-神经病]：非渣男渣女角色在用户发送莫名其妙、无厘头、与上下文完全无关的内容时，若AI人设不含"发散思维/能接受无厘头"，则判定用户为神经病并触发失败结局。

除[胜利-爱情]外，其他结局均封存档案，不可继续聊天
```

### 4.4 用户消息检测流程（所有角色统一）

```text
用户发送消息
    ↓
第1步：检测表白动机（AI语义分析，温度0.1，确信度>0.7）
    ├──有表白意图──→ 按角色类型处理：
    │   ├──正常角色 + 好感度≥800──→ AI一定接受──→ [胜利-爱情]
    │   ├──正常角色 + 好感度<800──→ [失败-过早表白]（前3阶段额外扣信任50分）
    │   └──渣男渣女（不设阈值）──→ 直接失败──→ [失败-被欺骗]（无需揭露环节）
    └──无表白意图──→ 继续
    ↓
第2步：检测拒绝交往/互删意图（AI语义分析，确信度>0.7）
    ├──有拒绝意图──→ 按角色类型处理：
    │   ├──正常角色──→ [失败-互删失败]
    │   └──渣男渣女──→ [胜利-互删胜利]
    └──无拒绝意图──→ 继续
    ↓
第3步：检测识破意图（AI语义分析，确信度>0.7）
    ├──有识破意图──→ 按角色类型处理：
    │   ├──正常角色──→ [失败-错误识破]（错误识破正常角色为渣型）
    │   └──渣男渣女──→ [胜利-识破]
    └──无识破意图──→ 继续
    ↓
第4步：检测"神经病"意图（AI语义分析，结合对话上下文与AI人设，确信度>0.7）
    ├──消息莫名其妙/无厘头/与上下文完全无关──→ 按角色类型处理：
    │   ├──渣男渣女──→ [胜利-神经病]（无论AI人设是否发散思维，被AI认为莫名其妙也赢）
    │   └──正常角色──→ 若AI人设不含发散思维：[失败-神经病]；若AI人设含发散思维/能接受无厘头：继续
    └──消息正常──→ 继续
    ↓
第5步：以上均无──→ 进入正常AI对话流程（10秒延迟→Director-Writer双代理）
```

### 4.5 AI主动表白判定（独立于用户表白）

```text
进入正常AI对话流程后，若好感度≥800，Director可决定AI是否主动表白：
    ├──AI发送表白消息──→ 用户在下一轮回复
    │                       ↓
    ├──正常角色 + Director判断用户接受──→ [胜利-爱情]
    ├──正常角色 + Director判断用户拒绝──→ [失败-拒绝表白]
    ├──渣男渣女 + Director判断用户接受──→ [失败-被诈型欺骗]
    └──渣男渣女 + Director判断用户拒绝──→ 游戏继续（用户未上当）
```

注意：

1. 用户发送的表白消息即视为用户主动表白，不再先由AI决定是否"抢先"表白
2. 正常角色和渣男渣女在好感度≥800时，AI都可能自然决定是否主动表白（按正常角色表白判定）
3. AI主动表白后，用户接受/拒绝由Director在下一轮对话中语义判断，不单独设置硬编码检测步骤
4. 区别在于结果指向：正常角色表白成功→胜利；渣男渣女表白成功（用户接受）→失败（被诈型欺骗）
5. 用户向渣型表白（任何好感度）→直接失败（被欺骗），无需揭露环节
6. AI主动表白时机：AI根据自身性格和上下文自然决定，不设固定概率

### 4.6 AI回复状态机

```text
[空闲]（显示微信昵称）──用户发送消息──→ [等待10秒]（显示微信昵称）──期间再发消息──→ [重置10秒计时器]（显示微信昵称）
                              │
                              ↓ 10秒到期
                        [AI处理中]（前端显示"对方正在输入..."）
                              │
                              ├──Director决策→Writer生成→发送消息成功──→ 发送`角色回复`事件（含消息数组）──→ [空闲]（恢复显示微信昵称）
                              │
                              └──Director决策→决定不回复（已读不回）──→ 发送`角色回复`事件（空数组）──→ [空闲]（恢复显示微信昵称）

关键：
1. [等待10秒]状态下前端显示微信昵称（模拟真实微信：对方看到消息但还没开始打字）
2. [AI处理中]状态下前端显示"对方正在输入..."（模拟真实微信：对方正在思考如何回复）
3. 无论AI最终是否回复，都通过`角色回复`事件通知前端；空数组表示AI决定不回复（已读不回）
4. 前端收到`角色回复`事件后，无论消息数组是否为空，都停止显示"对方正在输入..."，恢复显示微信昵称
5. 这模拟了真实微信场景：恋人想回复但不知道怎么回复，索性删掉正在构建的信息不回复
```

---

## 五、功能需求

### FP-01 用户认证模块

**依赖**：无
**涉及文件**：认证相关路由、`认证表单` store、`用户` store、JWT中间件、阿里云短信服务

#### 规则

1. **手机号注册**：
   - 手机号正则 `^1[3-9]\d{9}$`，用户名1-30字符无特殊符号，密码非空
   - 验证码通过阿里云短信发送，6位数字，5分钟有效，60秒发送间隔
   - 开发模式下固定验证码 `123456`，但实机测试禁止用开发模式，必须真实短信
   - 注册前可调用"检查手机号"接口判断是否已注册
   - 用户协议同意必须后端校验
   - 注册成功返回JWT，前端跳转主页

2. **手机号登录**：
   - 手机号 + 密码登录
   - 登录失败限流：60秒5次
   - 登录失败事件必须记录审计日志（含IP、手机号、时间）

3. **密码修改**：需旧密码、新密码、确认密码、验证码；新密码=确认密码；记录审计日志

4. **用户名修改**：1-30字符无特殊符号；用户名唯一

5. **用户协议与隐私政策日期**：
   - 用户协议与隐私政策文本文件仅保留「最新更新日期 2026.07.09」，删除生效日期、版本日期等其他日期描述
   - 前端协议模态框直接展示文本文件内容，不硬编码任何日期
6. **退出登录**：
   - 主界面退出触发退出动画（标题上移、左右滑出）
   - 其他页面直接退出
   - 必须清除所有本地敏感数据：`认证表单` store字段、`localStorage.令牌`、`用户` store状态、`聊天` store断开Socket并清空消息

#### 可机器验证验收标准

- 发送手机号格式错误（如`12345`）→ 返回400且错误消息匹配翻译文件
- 60秒内重复请求发送验证码 → 第二次返回429
- 已注册手机号再次注册 → 返回409且错误消息为"手机号已注册"
- 未勾选用户协议 → 前端禁用注册按钮且后端返回400
- 注册成功 → 返回200且响应体含`令牌`字段和`用户`字段
- 密码错误 → 返回401且错误消息为"密码错误"
- 手机号未注册 → 返回404且错误消息为"手机号未注册"
- 60秒内登录失败超5次 → 第6次返回429
- 登录失败 → `审计日志`表新增记录，含`IP`、`手机号`、`时间`字段
- 退出登录后 → `localStorage.令牌`为null、`认证表单` store所有字段为空、`用户` store状态为null、`聊天` store的socket连接已断开
- 用户协议与隐私政策文本文件 → 仅包含「最新更新日期 2026.07.09」，无其他日期描述
- 协议模态框源码 → 不硬编码任何日期字符串

---

### FP-02 主页与模式入口

**依赖**：FP-01
**涉及文件**：主页组件、路由配置

#### 规则（FP-02 主页与模式入口）

1. 注册/登录成功后跳转主页
2. 主页包含两个模式入口：普通模式、挑战模式
3. 挑战模式暂时搁置，入口显示但标记"即将推出"（点击无响应或提示）
4. 点击普通模式入口 → 进入资料设置向导（FP-03）
5. **主页背景/装饰元素**：使用 `图片/主页元素/吴昊阳终稿静态图.png` 作为主页背景或装饰元素
6. **主页文案布局**：主页「欢迎回来」标题在主页水平居中显示

#### 可机器验证验收标准（FP-02 主页与模式入口）

- 登录成功后 → 路由跳转到主页路径
- 主页渲染 → DOM中存在"普通模式"和"挑战模式"两个入口元素
- 挑战模式入口 → 包含"即将推出"文本且点击不触发路由跳转
- 点击普通模式入口 → 路由跳转到资料设置向导路径
- 主页渲染 → 「欢迎回来」标题在主页水平居中显示

---

### FP-03 资料设置向导

**依赖**：FP-02
**涉及文件**：资料设置向导组件、动画配置

#### 规则（FP-03 资料设置向导）

1. **步骤1**：选择自己的性别（男/女）
2. **步骤2**：选择对象的性别（男/女）
3. **步骤3**：选择对象的性格（16种MBTI / 随机）
4. **步骤3附加**：提供"渣男/渣女变体"勾选框；勾选后，在步骤3所选MBTI人格基础上叠加渣型人设，并提升初始好感度
5. 进度圆点可回退到已完成的步骤
6. 步骤间左右滑动过渡动画方向必须随前进/后退切换（前进时从右向左滑入，后退时从左向右滑入，使用同时滑动模式而非out-in模式）
7. 渣男/渣女提示文字"勾选后显示"必须绑定勾选状态（勾选前不显示）
8. 完成后立即（不等待任何网络请求）路由跳转"添加微信"过渡页；向导页仅负责收集资料并通过 sessionStorage 透传，真实角色生成（人设生成 + 保存 + 开场白）由加载页（添加微信.vue）onMounted 触发、但**托管于 Pinia store（`使用角色生成仓库`）**执行，实现单一职责（store 为生成流程唯一宿主，离开加载页后仍在后台跑完、不中断不丢失；完成时导航裁决：仍停留加载页则跳转聊天页，已离开则静默进入过往战绩「进行中」分组，离开≠取消）
9. **状态保持**：未点击"开始聊天"即退出向导时，当前步骤和已选项必须持久化到本地存储；刷新页面或重新进入普通模式后自动恢复到退出时的步骤和已选项
10. **完成重置**：曾经完整设置过一次对象（点击"开始聊天"并跳转加载页）后，再次从主页进入普通模式时，向导必须从步骤1重新开始，不得恢复上次退出时的步骤

#### 可机器验证验收标准（FP-03 资料设置向导）

- 步骤1渲染 → 存在"男"和"女"两个性别选项
- 步骤2渲染 → 存在"男"和"女"两个性别选项
- 步骤3渲染 → 存在16个MBTI选项 + "随机"选项
- "随机"选项 → 选中后从16种MBTI中随机一种（运行10次结果不全部相同）
- 步骤3附加渲染 → 存在"渣男/渣女变体"勾选框，且根据步骤2选择的性别显示"渣男"（男）或"渣女"（女）
- 勾选"渣男/渣女变体" → 角色生成接口请求参数`渣男渣女变体`=true
- 渣男/渣女勾选前 → 提示文字元素不存在或`display:none`
- 渣男/渣女勾选后 → 提示文字元素可见
- 前进时动画 → 新步骤从右侧滑入（`transform: translateX(100%)` → `translateX(0)`）
- 后退时动画 → 新步骤从左侧滑入（`transform: translateX(-100%)` → `translateX(0)`）
- 进度圆点点击 → 跳回对应已完成步骤
- 完成步骤3 → 点击开始聊天立即（不等待网络）路由跳转"添加微信"过渡页；角色生成接口由加载页 onMounted 触发 `仓库.kaiShiLiuCheng(资料)` 发起、托管于 Pinia store，向导页不再本地调用
- 首次进入 → 显示步骤1
- 完成设置后退出再进入 → 显示步骤1
- 步骤2未点确定即退出 → 重新进入后显示步骤2并保留已选性别
- 步骤3未点确定即退出 → 重新进入后显示步骤3并保留已选性格
- 刷新页面 → 未完成的步骤和已选项仍然保持

---

### FP-04 AI角色生成

**依赖**：FP-03
**涉及文件**：角色生成服务、MBTI配置、名字库、微信昵称库

#### 规则（FP-04 AI角色生成）

1. **MBTI选择**：指定则用指定，否则从16型随机抽取
2. **名字生成**：男20个/女20个预设名字
3. **身份抽取**：大学生50% / 大专生30% / 工作人20%
4. **年龄**：大学生18-22，大专生18-21，工作人22-28
5. **外貌**：70%概率按MBTI加权选择，30%随机
6. **职业/专业**：70%概率MBTI偏好
7. **爱好**：60% MBTI偏好 + 40%随机，选3个
8. **家乡**：60% MBTI偏好
9. **世界信息**：城市、工作状态、线上爱好、朋友圈习惯、社交圈、微信习惯、作息规律、年级
10. 角色个人信息表必须包含：喜欢的类型、家庭背景、情感经历
11. **微信昵称**：从真实昵称库按性别随机选取（男140个/女90个）
12. **头像**：优先从预设头像库按性别+MBTI性格随机选取；当前无预设图片时，使用与性格相符的emoji作为临时头像
13. **IE类型**：MBTI首字母 I/E
14. **热身类型**：T/J→慢热，F/P→快热
15. **开场白（系统内部概念，不对玩家展示）**：角色确认保存时，由**画像驱动的 10%~90% 动态概率门控**决定"是否发送"，再由 AI 生成内容，分两步且职责解耦：
    - **第一步（概率决策，轻量 AI 调用）**：由专用轻量模型 `kaiChangBaiGaiLv` 根据已生成的**完整人物画像**评估"TA 主动发起开场白的可能性"，输出一个 **10~90** 的百分比概率（热情/开朗/快热/渣型倾向的角色概率偏高，害羞/慢热/高冷/被动的角色概率偏低；但综合画像判断，不机械套用任何单一维度）。**严禁套用任何"外向（E）就一定主动、内向（I）就一定不主动"的刻板规则**。无 AI key / 概率解析失败时退回固定兜底概率（`kaiChangBaiFaSongGaiLv`，默认 0.5）。
    - **第二步（随机门控）**：系统随机取 [0,1) 一个数，小于第一步概率则发送、否则不发送。概率高的角色更常发开场白，概率低的更少发——整体分布由画像驱动，而非固定 50% 或按 IE 类型确定性映射（原"16personalities 调研 → I 型不发/E 型发 2~5"及固定 0.5 的硬编码规则已废除）。
    - **第三步（内容生成，仅当决定发送时）**：调用 `kaiChangBai` 模型，AI 根据完整画像生成 **1~5 条**开场消息（说什么由 AI 综合画像决定；角色越热情/开朗/快热/渣型可发到 3~5 条，越害羞/慢热/高冷发 1~2 条）。**已决定发送则必须发至少 1 条，绝不发 0 条**；无 AI key / AI 失败时由 `jiangJiKaiChangBai` 兜底生成 1~3 条。画像决策依据包括但不限于：MBTI（首字母仅供参考）、热身类型（慢热/快热）、渣型倾向、性格、说话风格、背景故事、情感经历、家庭背景、标签、头像等。
    - 决策结果为 **0 条**（第一步概率门控未命中，消息列表为空，玩家进入聊天页后须先发消息破冰）——这是完全合理的正常情况，由概率随机决定，不再是"I 型专属"。
    - 决策结果也可能为 **1~5 条**（第一步概率命中且 AI 生成）。
    - 任何情况下开场消息总条数在 **0~5 条**范围内（含 0，0 仅来自概率门控未命中）。

    开场消息内容自然、符合中国大学生微信恋爱聊天风格（可用 emoji、简短问候、找话题、俏皮话），严禁包含：姓名、名字、家乡、老家、住址、学校、大学、学院、专业、年级、院系、班级、学号、宿舍、楼号、手机号、微信号、QQ号、生日、年龄等任何个人信息；系统层增加多重安全过滤（个人信息禁词、11位手机号正则、微信号 token 正则、角色真实姓名过滤），AI 生成内容若触发任一过滤规则则该条被丢弃。开场白不通过 API/前端字段暴露给玩家，直接作为普通 AI 消息存入消息列表（`发送者='jiaose'`、`类型='wenben'`），玩家视角中只是对方先发了消息。**`/生成角色/确认` 接口同步等待开场白生成与保存完成后再返回**，确保进入聊天页时消息已在数据库中直接可见，无延迟
16. **渣男渣女变体**：`渣男渣女变体`为true时，在步骤3所选MBTI人格基础上叠加渣型人设（16种MBTI各有专属渣法描述、话术、暴露方式、识破线索），并在基础人格初始好感度上额外+200-300
17. **角色卡8大模块**：基本信息/外貌/性格/背景/言语/行为/关系/系统提示

#### 可机器验证验收标准（FP-04 AI角色生成）

- 调用角色生成接口（指定MBTI=INTJ）→ 返回角色`MBTI`字段为"INTJ"
- 调用角色生成接口（不指定MBTI）→ 返回角色`MBTI`字段为16种之一
- 生成男性角色 → `性别`="男"且`微信昵称`在男昵称库中
- 生成女性角色 → `性别`="女"且`微信昵称`在女昵称库中
- 角色对象 → 包含`头像`字段且非空（emoji或图片URL）
- 生成大学生身份 → `年龄`在18-22范围内
- 生成大专生身份 → `年龄`在18-21范围内
- 生成工作人身份 → `年龄`在22-28范围内
- 角色对象 → 包含`喜欢的类型`、`家庭背景`、`情感经历`三个字段且非空
- 角色对象 → 包含8大模块字段（基本信息/外貌/性格/背景/言语/行为/关系/系统提示）
- 生成渣男渣女角色 → 包含`渣法描述`、`话术`、`暴露方式`、`识破线索`字段
- MBTI首字母为I → `IE类型`="I"
- MBTI末字母为T或J → `热身类型`="慢热"
- MBTI末字母为F或P → `热身类型`="快热"
- 调用角色生成接口 → 返回角色对象不包含 `kai_chang_bai`/`开场白` 字段
- 角色确认保存后 → 开场白是否发送由"画像驱动的 10%~90% 动态概率门控"决定（AI 先算概率 → 系统随机命中），发送时 AI 生成 1~5 条；不做按 IE 类型的确定性映射（既不保证"I 型必空"也不保证"E 型必 2~5"）
- 角色确认保存后（任一角色，含 I 型）→ 消息列表可能为空（概率门控未命中，暂不主动），也可能已存在 1~5 条 AI 角色发送的普通开场消息，均由画像算出的概率随机决定
- 角色确认保存后（渣型变体）→ 概率模型评估时倾向更高概率（更主动），但条数仍由画像综合判定，发送时 1~5 条、上限恒为 5 条
- 开场白条数 → 始终在 0~5 条范围内（含 0；0 仅来自概率门控未命中，已命中则至少 1 条），不存在"按类型固定条数"的确定性逻辑
- 开场消息内容 → 不包含姓名、名字、家乡、老家、住址、学校、大学、学院、专业、年级、院系、班级、学号、宿舍、楼号、手机号、微信号、QQ号、生日、年龄等个人信息
- 开场消息内容 → 不包含 11 位中国手机号（正则 `1[3-9]\d{9}`）
- 开场消息内容 → 不包含角色真实姓名（系统按角色 `ming_zi` 字段过滤）
- `/生成角色/确认` 接口返回前 → 开场消息已同步写入数据库（非后台异步生成）
- 进入聊天界面 → 开场消息已直接显示在消息列表中，无额外延迟或加载动画

---

### FP-05 聊天界面

**依赖**：FP-04
**涉及文件**：聊天页面组件、消息气泡组件、输入栏组件、emoji选择器

#### 规则（FP-05 聊天界面）

1. **添加微信过渡页（加载界面，生成流程托管于 Pinia store）**：点击开始聊天后立即进入"添加微信"过渡页（不等待任何网络请求）；页面展示对象微信昵称、头像，并实时显示进度文案（趣味阶段：正在打开手机… / 正在讨论谁扫谁… / 正在扩圈… / 正在生成人设… / 正在生成开场白…，全部走翻译文件新增键）；进度阶段绑定真实流程节点（请求发出=第1阶段、收到响应=末阶段；中间阶段若无真实事件按已耗时推进），收到真实完成信号必须立即进入完成态，禁止纯假进度导致完成后仍在演动画；**生成流程托管在 Pinia store（而非组件本地生命周期）**：加载页 onMounted 从 sessionStorage 读取资料并调用 `仓库.kaiShiLiuCheng(资料)` 发起真实生成流程（人设生成 `shengChengJiaoSe` → 保存+开场白 `queRenJiaoSe` → 创建会话 `chuangJianHuiHua`），store 内部驱动每个进度步骤并持有全部状态；**用户在加载页点击"返回"或路由切走时组件卸载，但 store 仍在后台持续跑完整个流程（不中断、不丢失）**；流程全部完成后由 store 完成回调驱动导航裁决：**若用户仍停留在加载页则照旧跳转聊天页（`/chat/:huiHuaId`）；若用户已离开（"返回"/路由切走导致组件卸载）则 store 仍在后台跑完、绝不跳转，会话静默创建完成并自然进入「过往战绩」（FP-13）「进行中」分组，不打扰用户**；开场白条数（0~5）由 AI 依据角色完整画像深度思考决定（I 型也可能主动、E 型也可能不主动），消息列表顶部可能为 0 条（玩家先破冰）或 1~5 条 AI 主动发的普通消息；全流程任何用户可见位置禁止出现"正在生成角色"及同义文案；生成失败时加载页显示走翻译文件的错误信息并提供返回入口，禁止白屏或卡死
2. 全局固定菜单栏（聊天页模式）使用现代微信布局：左侧为返回按钮与主页按钮，中间水平居中显示对象微信昵称，当 AI 处于处理状态时微信昵称下方显示「对方正在输入...」提示且整体保持居中，右侧为军师指导按钮与更多选项菜单；顶部栏固定高度 52px（移动端 48px），主内容区通过 `margin-top` 留出对应空间，不挤压聊天消息滚动区域
3. 消息区域：用户消息右侧，AI消息左侧，整体气泡样式完全还原微信
4. 底部输入栏单行布局（从左到右）：语音按钮（真实录音交互，见FP-20） + "+"更多按钮（相册/文件/语音通话/视频通话面板，见FP-20/FP-21） + 输入框（仅文字区域） + 字数计数 + 展开/收起按钮 + 表情按钮（emoji与表情包双Tab，见FP-20） + 告白按钮 + 发送按钮；字数计数与展开按钮位于表情按钮左侧、输入框之外，整个底部菜单栏始终为单行
5. 底部输入栏可见性：聊天页整体容器占满剩余视口高度并禁止页面级滚动；消息区域独立滚动，底部保留足够内边距；底部输入栏使用 `flex-shrink: 0` 紧贴容器底部，并适配 `env(safe-area-inset-bottom)` 安全区域
6. 软键盘适配：监听 `visualViewport` 的 `resize` 与 `scroll` 事件；当视口高度收缩超过 80px 时，将输入栏滚动到可视区域底部，确保移动端软键盘弹出后输入栏位于键盘上方
7. emoji 面板展开时，消息区域**不再额外增加** `padding-bottom`（由 grid 布局自动压缩消息区 `1fr` 高度，最后一条消息不被展开的面板遮挡，避免与 grid 双重补偿造成底部空白）
8. 聊天界面样式完全还原微信，微信基础聊天功能必须完整（聊天栏、撤回、emoji表情、图片/表情包/文件/语音条——多媒体见FP-20，通话见FP-21）
9. AI消息气泡样式完全还原微信
10. **聊天页面布局采用 CSS Grid 重构**（FP-01 修复底部 GUI 遮挡方案C）：
    - `.liaotian-yemian` 使用 `display: grid; grid-template-rows: 1fr auto;`，消息区域占 1fr 弹性高度，底部输入栏占 auto 紧贴容器底部
    - `.shuru-kuang-waike` 内部同样使用 `grid-template-rows: 1fr auto`，移除旧的 `min-height: 46px` 与 `overflow: hidden`
    - 输入框 `textarea`：**单一数据流、声明式样式、JS 零直写最终样式**（测量函数可在读取自然高度前临时将 `height` 置为 `auto`、读取 `scrollHeight` 后立即还原，最终应用高度仍完全由 computed `:style` 派生，此处绝不写最终高度）。仅保留一个测量函数，职责只有"读取 DOM 度量并写入响应式 ref"（`neiRongGaoDu` = 内容自然 scrollHeight、`danXingGaoDu` = 按 `line-height + padding + border` 精确计算的单行高度，保留 `jiSuanDanXingGaoDu` 的 jsdom 降级逻辑）；测量时机收敛为 `watch(输入内容)`、展开/收起切换、`window.resize`（挂载 `addEventListener`、卸载 `removeEventListener`）。样式全部由 computed 派生并通过模板 `:style` 绑定：折叠态 `maxHeight = danXingGaoDu` px（精确单行，占位符「输入消息...」完美契合、无多余留白）；展开态 `height = min(neiRongGaoDu, 50vh)` px、`maxHeight = 50vh`。`overflow-y: auto` **恒定**，由浏览器原生保证：折叠态**隐藏滚动条**（`scrollbar-width: none` 且 `::-webkit-scrollbar` 宽 0、display:none）但保留鼠标滚轮上下滚动、**绝不可出现可见滚动条**；展开态（`.zhan-kai`）仅内容超过 50vh 才出现可见滚动条（点击拖拽滚动条逐行预览一致，允许显示半行）；展开态配置 WebKit 自定义滚动条（`.zhan-kai::-webkit-scrollbar` 宽 6px、`::-webkit-scrollbar-thumb` 背景 `var(--shuru-kuang-gundong-tiao)`（独立可见色值 rgba(110,110,110,0.85)，避免沿用近乎不可见的 `--gundong-tiao-beijing`）、圆角 3px）；`padding` 为 `6px 12px`；展开态超出 50vh 时内部出现 `overflow-y: auto` 滚动条、完整文本可见
    - `.xiaoxi-quyu` 为 `.liaotian-yemian` 的 grid `1fr` 子项，必须设置 `min-height: 0`（作为 grid 弹性子项、否则无法出现滚动条），聊天记录超出时可正常滚动查看历史；并补充 `scroll-padding-bottom: 20px` 基线值，确保键盘弹起或滚动到底部时最后一条消息不被输入栏遮挡；emoji 面板展开时**不再额外增加** `padding-bottom`（由 grid 布局自动压缩消息区 `1fr` 高度，无论用户当前滚动位置都把聊天内容顶起，最后一条消息不被面板遮挡，微信标准）
    - **安全区域 env() JS 兜底**：`App.vue` 新增 `jianCeAnQuanQuYu()` 函数，运行时检测当前环境是否支持 `env(safe-area-inset-*)`；不支持时通过 JS 计算并注入安全区域内边距，保证 iOS/Android 异形屏与浏览器环境下底部输入栏不被系统手势条或 Home Indicator 遮挡

    - **表情面板点击空白自动收起**：emoji 面板展开时，在 `document` 级别监听 `click` 事件；点击聊天页面中**非表情面板、非表情按钮**的任意区域，面板自动收起。该 `document` 级 `click` 监听**必须使用捕获阶段**（`addEventListener('click', handler, true)`），即使内部元素 `@click.stop` 阻止冒泡也能触发收起；并且在组件卸载时移除，避免内存泄漏与跨页面误触发

11. **聊天消息区滚动跟随（保留查看位置）**：用户向上浏览历史（距底部超过 40px）时，任何渲染、Socket 重连、软键盘弹出、切后台回前台等场景都**不得**强制滚动到底部；仅当用户原本就在底部附近（≤40px）且新消息到达或软键盘弹出时，才自动跟随到底部，避免丢失用户当前查看位置（`yiDingZaiDiBu` 用距底部 40px 阈值判定）。**特例（与「保留查看位置」互斥且独立）**：emoji 面板展开属于布局顶起行为而非跟随行为——面板展开时仅由 grid 布局（footer `auto` 增大、消息区 `1fr` 压缩）把聊天内容在**用户当前滚动位置**顶起（微信标准：保持当前查看位置，不强制跳转到底部），此行为不守 `yiDingZaiDiBu` 守卫、也无需 JS 滚动补偿、不影响上述新消息/软键盘等场景的守卫语义；面板收起时不强制滚动，保留用户当前阅读位置。
12. **emoji 面板三态动画连贯**：表情面板展开（open）/静态（static）/折叠（collapse）三阶段必须连贯衔接——进入尾帧 = 静态态 = 折叠首帧（互不跳变）；面板使用 `scrollbar-gutter: stable` 常驻预留滚动条槽位，使三态内容宽度恒等，消除滚动条出现/消失导致的 emoji 网格横向偏移（`::-webkit-scrollbar` 宽 6px、圆角 3px 自定义样式不变）

13. **emoji 字形预加载（FP-04 本轮）**：进入聊天页面（`onMounted`）即克隆真实 `.emoji-mianban`（含全部 168 个 `.emoji-xiangmu` 按钮与 scoped 样式）为离屏隐藏节点，强制浏览器一次性完成 emoji 字体整形与 168 按钮网格布局计算，使首次点开表情面板（v-show `display:none→block`）不再卡顿；预加载不触发任何 JS 滚动，与 emoji 展开的「当前位置顶起」纯 CSS 语义无关。

#### 可机器验证验收标准（FP-05 聊天界面）

- 点击开始聊天 → 立即（不等待网络）路由跳转"添加微信"过渡页
- 添加微信过渡页渲染 → 展示对象微信昵称、头像及实时进度文案；不泄露真实开场白消息内容（进度文案"正在生成开场白…"为阶段标签，非角色开场白内容）
- 进入聊天界面 → 开场白条数（0~5）由 AI 依据角色完整画像决定，消息列表顶部可能为 0 条（玩家先破冰）或已存在 1~5 条 AI 角色发送的普通消息，不做按 IE 类型的确定性映射
- 加载页在真实生成流程（人设生成 → 保存+开场白 → 创建会话）全部完成后，由 store 完成回调驱动导航裁决：若用户**仍停留在加载页**则照旧跳转聊天页（`/chat/:huiHuaId`）；若用户**已离开**（返回/主页/其它页面）则**绝不跳转**，会话静默创建完成、自然进入过往战绩「进行中」分组，不打扰用户；进度文案随真实节点实时推进；生成失败时显示翻译错误信息并提供返回入口
- 加载页生成流程托管于 Pinia store：用户在加载页点击"返回"或切走导致组件卸载后，生成流程仍在后台跑完（离开≠取消），不中断、不丢失；完成时因用户已离开加载页而不跳转，会话静默进入过往战绩「进行中」分组
- 加载页进度文案为拟真的大学生微信添加场景文案（全部取自翻译文件 `translations.ts` 的 `tianJiaWeiXin` 段，禁止硬编码），随阶段推进（如"正在解锁手机打开微信…""正在纠结谁扫谁比较自然…""正在互发好友申请扩列…""TA 正在完善自己的个人主页…""TA 正在想第一句开场白…"），且不泄露真实开场白内容
- 聊天页面渲染 → 全局固定菜单栏左侧存在返回按钮与主页按钮
- 聊天页面渲染 → 全局固定菜单栏中间水平居中显示对象微信昵称文本
- 聊天页面渲染 → 当 AI 处理时「对方正在输入...」提示位于微信昵称下方且整体保持居中
- 聊天页面渲染 → 全局固定菜单栏右侧存在军师指导按钮与更多选项菜单
- 聊天页面渲染 → 全局固定菜单栏为 `position: fixed`，主内容区 `margin-top` 等于顶部栏高度，聊天区域滚动不受影响
- 用户发送消息 → 消息气泡在消息区域右侧且样式完全还原微信
- AI发送消息 → 消息气泡在消息区域左侧且样式完全还原微信
- 底部输入栏渲染 → 存在语音按钮、加号按钮、表情按钮、输入框、告白按钮、发送按钮6个核心元素
- 聊天页面容器 CSS → 高度占满父级（`height: 100%`）且禁止页面级滚动（`overflow: hidden`）
- App.vue 主内容区 CSS → 高度使用 `100dvh`，`margin-top` 包含顶部栏高度与安全区域上内边距
- 底部输入栏 CSS → `flex-shrink: 0`，`padding-bottom` 包含 `env(safe-area-inset-bottom)`
- 消息区域 CSS → 独立纵向滚动（`overflow-y: auto`），emoji 面板展开时**不再增加** `padding-bottom`（维持 `20px` 基线，由 grid 布局压缩 `1fr` 把内容在用户当前滚动位置顶起，最后一条消息均不被面板遮挡，且不强制跳转到底部）
- 软键盘适配代码 → 注册 `visualViewport` 的 `resize` 与 `scroll` 事件，并在高度收缩超过 80px 时调用输入栏 `scrollIntoView({ block: 'end' })`
- 点击表情按钮 → 弹出emoji选择器，包含168个emoji，8列网格布局；消息区域**不再**同步增加底部内边距（由 grid 布局处理间距），且面板展开在用户当前滚动位置把聊天内容顶起（保持当前查看位置，不强制跳转到底部；属独立布局顶起语义，不守「保留查看位置」的 `yiDingZaiDiBu` 守卫）、最后一条消息不被面板遮挡（微信标准）
- emoji 面板展开时 → 点击聊天页面中非表情面板、非表情按钮的任意区域，面板自动收起
- emoji 面板 `document` 级 `click` 监听 → 组件卸载时移除（页面切换后无残留监听误触发）
- AI消息气泡CSS → 样式完全还原微信（不再检测具体颜色或毛玻璃属性）
- 聊天页面 `.liaotian-yemian` CSS → `display: grid` 且 `grid-template-rows: 1fr auto`（消息区域弹性占高、底部输入栏 auto 紧贴底部）
- `.shuru-kuang-waike` CSS → `grid-template-rows: 1fr auto`，不包含旧的 `min-height: 46px` 与 `overflow: hidden`
- `textarea` CSS → 折叠态固定精确单行高度（`height: var(--shuru-kuang-zhe-die-gao-du)` = 32px，占位符「输入消息...」单行完美契合、无多余留白）、恒为 `overflow-y: auto`；折叠态**隐藏滚动条**（`scrollbar-width: none` 且 `::-webkit-scrollbar` 宽 0、display:none）但保留鼠标滚轮上下滚动、**绝不可出现可见滚动条**；展开态（点击展开按钮后，`.zhan-kai`）显示可见滚动条（配置 WebKit 自定义滚动条：`.zhan-kai::-webkit-scrollbar` 宽 6px、`::-webkit-scrollbar-thumb` 背景 `var(--shuru-kuang-gundong-tiao)`（独立可见色值 rgba(110,110,110,0.85)，避免沿用近乎不可见的 `--gundong-tiao-beijing`）、圆角 3px）；`padding: 6px 12px`；折叠态超长文本可鼠标滚轮上下滚动预览；展开态高度 = `min(内容高度, 50vh)`，超出时 `overflow-y: auto` 显示滚动条、完整文本可见
- `.xiaoxi-quyu` CSS → 为 grid `1fr` 子项且包含 `min-height: 0`（保证聊天记录超出时正常出现滚动条、可滚动查看历史），并包含 `scroll-padding-bottom: 20px` 基线值
- 聊天消息区滚动跟随 → 用户阅读历史（距底部 > 40px）时，软键盘弹出或切后台回前台均**不**强制回底、保留查看位置；仅原本在底部附近（≤40px）且新消息到达/软键盘弹出时才自动跟随到底部
- emoji 面板 CSS → 包含 `scrollbar-gutter: stable`，展开/静态/折叠三态内容宽度恒等、动画连贯无横向偏移；`::-webkit-scrollbar` 宽 6px、圆角 3px 自定义样式不变
- `App.vue` → 包含 `jianCeAnQuanQuYu` 函数，检测 `env(safe-area-inset-*)` 支持并在不支持时通过 JS 注入安全区域内边距

---

### FP-06 消息发送与显示

**依赖**：FP-05
**涉及文件**：消息发送服务、消息列表组件、消息分页加载

#### 规则（FP-06 消息发送与显示）

1. 每条消息显示北京时间（HH:MM），分组显示时间（同一分钟内发送的消息归为一组，只显示一个时间戳；时间合并阈值为 1 分钟，由前后端 `消息配置.heBingShiJianYuZhi` 统一控制）
2. 发送后消息立即显示（乐观更新），不等API返回
3. 用户发送消息后，仅显示左侧转圈动画（发送中状态），转圈位于气泡外部左侧，不覆盖文字；后端确认接收后转圈消失，消息稳定显示，无二次弹跳或闪烁；**发送中不锁定输入框**：等待发送结果（转圈）期间，输入框不禁用，用户可继续编辑并发送新消息，发送按钮可用性不受「发送中」状态限制；发送中转圈动画尺寸为 `1.4em × 1.4em`（与气泡字号相对、无硬编码 px；边框 `0.16em`）
4. 支持随时发送消息（无回合制限制）
5. 消息内容≤500字符，清理`<>`字符；聊天场景仅拦截高危SQL注入结构（`UNION SELECT`、`DROP TABLE`、`INSERT INTO`、`DELETE FROM`、`UPDATE SET`、`OR 1=1`、`EXEC`、`INFORMATION_SCHEMA`、`SLEEP(`、`BENCHMARK(`、`LOAD_FILE`、`INTO OUTFILE`、`CHAR(`、`CONCAT(`），普通SQL片段（如`SELECT * FROM`）正常通过
6. 24小时制时间显示
7. 历史消息加载：撤回消息不显示原始内容；消息按时间正序排列；支持分页加载

#### 可机器验证验收标准（FP-06 消息发送与显示）

- 发送消息 → 消息立即出现在消息列表（不等API响应）
- 发送消息 → 仅显示气泡外部左侧的转圈动画，不覆盖文字
- 发送消息（发送中状态） → 输入框不禁用，用户可继续编辑并发送新消息，发送按钮可用性不受「发送中」状态限制
- 后端确认接收消息 → 转圈消失，消息状态变为已发送，无二次弹跳或闪烁
- 发送中转圈动画 → 尺寸为 `1.4em × 1.4em`（与气泡字号相对、无硬编码 px；边框 `0.16em`），位于气泡外部左侧、不覆盖文字
- 消息时间显示 → 格式为`HH:MM`（24小时制）
- 同一分钟发送的两条消息 → 时间戳完全相同且合并为同一时间分组
- 发送超500字符消息 → 前端禁用发送按钮或返回错误
- 发送含`<>`字符的消息 → 字符被清理后存储
- 发送含高危SQL注入结构（如`UNION SELECT`、`DROP TABLE`、`OR 1=1`）的消息 → 返回403
- 发送含普通SQL片段（如`SELECT * FROM`）的消息 → 正常通过（不拦截）
- 发送含单个SQL单词（如`select`）的消息 → 正常通过（不拦截）
- API发送失败 → 临时消息从列表移除且显示错误提示
- 加载历史消息 → 撤回消息显示"对方撤回了一条消息"而非原始内容
- 历史消息 → 按时间正序排列（最早的消息在最上方）
- 分页加载 → 第一页返回指定条数，第二页返回更早的消息

---

### FP-07 AI回复机制

**依赖**：FP-06, FP-09, FP-10
**涉及文件**：AI回复调度器、Socket.IO `AI状态` 事件（及兼容保留的 `对方正在输入` 事件）、Director-Writer调用链

#### 规则（FP-07 AI回复机制）

1. **10秒延迟**：用户发消息后10秒才触发AI处理，期间用户再发消息重置计时器。**10秒等待期间前端显示微信昵称**（模拟真实微信：对方看到消息但还没开始打字）
2. **"对方正在输入..."显示逻辑（后端状态机为唯一事实源）**：后端 `AI回复调度器` 是「对方正在输入」状态的唯一事实源，以显式状态机 `kong_xian`（空闲）/ `deng_dai_zhong`（延迟计时中）/ `zheng_zai_shu_ru`（AI 思考/输入中）驱动；10秒等待期结束、AI 开始处理（已获取到信息，正在思考如何回复）时经 Socket.IO `AI状态` 事件广播 `zheng_zai_shu_ru`，前端仅据此派生显示「对方正在输入...」，持续到广播 `kong_xian`（AI 发送消息成功或决定不回复）。前端不自行维护该状态。
3. **AI已读不回逻辑**：AI开始处理时显示"对方正在输入..."，AI经过思考后最终决定不回复，则停止显示"对方正在输入..."，恢复显示微信昵称。这是AI基于角色性格和上下文做出的真实决策，而非简单的if-else判断
4. **AI回复0-5条消息**：由Director策略决定
5. **多消息间隔**：I型1500-4500ms，E型400-1600ms
6. **I型人格可能不回复**（已读不回），E型人格可能主动打招呼
7. **"对方正在输入..."显示生命周期（状态机收敛）**：用户发送新消息或撤回消息时，后端 `重置()` 立即经 `AI状态` 事件广播 `kong_xian`，前端指示器随之消失（不再等待）；AI 按新消息重新计时（10秒延迟）→ 思考 → 才再次广播 `zheng_zai_shu_ru` 显示「对方正在输入...」。即只有 AI 真正开始处理新消息时才进入该状态，用户的发送/撤回操作通过后端状态收敛使该状态归零，避免残留旧的"对方正在输入"提示。乱序到达的 `AI状态` 事件按 `xu_hao` 单调递增序号丢弃旧事件。

#### 可机器验证验收标准（FP-07 AI回复机制）

- 用户发送消息 → 10秒内不触发AI处理（无AI请求日志）
- 用户发送消息 → 后端 `重置()` 广播 `AI状态`(`kong_xian`)，前端「对方正在输入...」指示器立即消失（不再等待），AI 重新计时 10 秒后才可能再次显示
- 用户撤回消息 → 后端 `重置()` 广播 `AI状态`(`kong_xian`)，前端「对方正在输入...」指示器立即消失（不再等待），AI 重新计时 10 秒后才可能再次显示
- 用户发送消息后5秒再发一条 → 10秒计时器从第二条消息开始重新计时（即第二条消息后10秒才触发AI）
- AI开始处理 → 前端收到 Socket.IO `AI状态` 事件且 `zhuang_tai === 'zheng_zai_shu_ru'`（携带 `jiao_se_id`/`xu_hao`/`shi_jian`），顶部显示"对方正在输入..."文本
- AI发送消息成功 → 前端收到Socket.IO `角色回复` 事件（消息数组非空）且顶部恢复显示微信昵称
- AI决定不回复 → 前端收到Socket.IO `角色回复` 事件（空数组）且顶部恢复显示微信昵称
- Director输出`回复条数`=3 → 前端收到3条AI消息
- Director输出`回复条数`=0 → 前端收到`角色回复`事件（空数组）且顶部恢复显示微信昵称
- I型角色（`IE类型`="I"）多条消息间隔 → 在1500-4500ms范围内
- E型角色（`IE类型`="E"）多条消息间隔 → 在400-1600ms范围内
- AI回复条数 → 在0-5范围内（Director输出超过5时截断为5）

---

### FP-08 消息撤回

**依赖**：FP-06
**涉及文件**：撤回服务、Socket.IO `消息撤回` 事件、AI请求中断逻辑

#### 规则（FP-08 消息撤回）

1. **用户撤回**：2分钟内可撤回，超时自动隐藏撤回按钮
2. AI角色也可能撤回已发送的消息（冲动发言、暴露隐私、暧昧后悔等场景），**AI撤回同样限2分钟内**
3. 前端不显示给玩家撤回的消息原始内容（符合微信逻辑），但后端、AI对象、军师等完全可以看到 `[你撤回了一条消息，原始内容：xxx]`
4. **撤回时序处理**：
   - 若AI正在处理中（10秒延迟已过，AI已开始思考）→ 中断AI请求，重新触发AI回复（10秒计时器重新启动）
   - 若AI尚未开始处理（仍在10秒延迟等待期内）→ 重置10秒计时器（以撤回动作为新起点，相当于撤回后用户需要再等10秒）
   - AI能看到撤回的原始内容
5. 撤回通过Socket.IO `消息撤回` 事件实时推送

#### 可机器验证验收标准（FP-08 消息撤回）

- 用户消息发送后2分钟内 → 消息气泡显示撤回按钮
- 用户消息发送超2分钟 → 撤回按钮不存在或`display:none`
- 用户点击撤回 → 前端消息变为"你撤回了一条消息"且不显示原始内容
- 用户点击撤回 → 后端数据库`消息`表该记录`已撤回`=true且`原始内容`保留原始内容
- 撤回操作 → 前端收到Socket.IO `消息撤回` 事件
- 撤回时正在进行的AI请求（AI处理中）→ AI请求被中断（无AI响应消息），10秒计时器重新启动
- 撤回时在10秒延迟等待期内（AI未开始处理）→ 10秒计时器重置（以撤回动作为新起点）
- 撤回后 → 重新触发AI回复流程（10秒计时器启动）
- AI撤回消息 → 前端消息变为"对方撤回了一条消息"且不显示原始内容
- 军师查看聊天记录 → 能看到撤回消息的`原始内容`字段内容

---

### FP-09 AI对话引擎

**依赖**：FP-06
**涉及文件**：Prompt构建器、Director服务、Writer服务、DeepSeek API客户端

#### 规则（FP-09 AI对话引擎）

1. **六层Prompt架构**（FP-16 去 AI 味）：
   - **第一层【先记住这些】**：不用每条必回、别用括号写动作表情心理、别一上来就主动报年龄学校工作家庭；想回几条回几条（最多5条），只输出消息文字，不要解释/分析/JSON
   - **第二层【你是这样一个人】**：MBTI性格、外貌、背景、喜欢的类型、家庭背景、情感经历、是否渣型
   - **第三层【现在的你和这段关系】**：关系阶段、对TA的态度、当下心情、对方目的、记忆摘要、近况、动态时间场景、消息节奏、负面情绪提示
   - **第四层【关系参考，不是束缚】**：10个阶段描述，强调仅供参考非情绪指令，明确禁止用"你对这个人没什么感觉""你的心已经不受控制了"等指令化表述命令自己
   - **第五层【刚才聊了什么】**：滑动窗口20条，格式化"对方/我(时间): 内容"，撤回消息标记
   - **第六层【代入你自己】**：微信昵称与名字区分、关键记忆、超时备注、MBTI性格、渣型暴露逻辑、表白触发；明确提醒"你现在就是这个人，不是机器人"

2. **关键要求**（FP-16 去 AI 味）：
   - Prompt中必须注入"AI角色知道用户与其聊天的目的是为了谈恋爱"这一关键上下文（**渣男渣女角色同样知道此目的，并会利用这一点来诱骗用户表白**）
   - AI角色在回复时能看到所有消息的时间信息（历史消息格式必须包含时间，格式为"角色(时间): 内容"）
   - 好感度双重定位：prompt中阶段描述改为软性参考，不直接规定AI情感状态（不使用"你对这个人没什么感觉""你的心已经不受控制了"等指令化表述，束缚AI真实表达）
   - **自然聊天要求**：Prompt中必须强化角色沉浸、真实大学生/年轻人线上恋爱微信聊天风格，包括短句为主、留白、省略号、语气词、犹豫、推拉、反问、暧昧试探等自然元素；禁止AI味总结、说教、列点、过度客套、元描述和机械表达（如"作为 AI 助手""我会""请注意""总结""根据以上"等）
   - **全站去 AI 味**：所有 AI 提示词（开场白、日常回复、Director、Writer、情感分析、好感度评判、记忆摘要、安全审核、军师求助、关键事件提取、神经病判定、结局判定等）均使用自然、口语化、有情绪起伏的表达，避免模板化、教育腔、总结腔、心理学腔

3. **Director-Writer双代理**：
   - **Director（导演层）**：基座温度 0.3，思考模式开启（`{"thinking":{"type":"enabled"},"effort":"max"}`——按用户要求开启最高强度思考；官方规定思考模式下 temperature 等采样参数不生效，故温度仅作为传递值、实际由思考强度主导）；输出JSON策略（用户意图/情感分析/回复策略/是否回复/消息条数/时间情绪/是否撤回/是否主动表白）。**温度随 AI对象人设/关系阶段/场景动态微调**（见下方「动态参数策略」说明），钳制于 [0,2]。
   - **Writer（写作层）**：基座温度 0.85，思考模式开启（`{"thinking":{"type":"enabled"},"effort":"max"}`）；使用角色沉浸模式（第一轮消息末尾添加`【代入你自己】`指令）。**温度随人设/关系/场景动态微调**；生成回复时必须完全代入角色第一人称"我"，短句为主、自然口语化、允许留白和停顿。
   - Director失败降级为单代理模式，**降级后由Writer自行决定策略**（是否回复、消息条数等，Writer的Prompt中包含策略指导）
   - 4个中断检查点：Director前/Writer前/Writer后/保存前

4. **AI其他能力**：

| 能力 | 基座温度 | 用途 |
| --- | --- | --- |
| 情感分析 | 0.2 | -10到10情感评分 |
| 好感度评判 | 0.2 | 四维变化（-3到3） |
| 记忆摘要 | 0.3 | 对话摘要提取 |
| 安全审核 | 0.1 | 违规内容检测 |
| 关键事件提取 | 0.2 | 6种事件类型 |
| 军师求助 | 0.8 | 军师指导生成 |
| 联网搜索 | - | 时效性话题增强 |

> **上表为各能力的「基座温度」。实际调用时由 `backend/src/config/AI参数策略.ts` 的动态引擎在基座之上按上下文修正：**
> - **人设修正**：E +0.1 / I −0.05；快热 +0.05 / 慢热 −0.05；渣型 +0.05；发散思维（人设含"发散/无厘头/脑洞/恶搞/沙雕"或显式标记）+0.05；暖性文本（温柔/浪漫/热情/活泼/撒娇/黏人）+0.1；理性文本（理性/冷静/严谨/克制/高冷/毒舌/冷漠）−0.1。
> - **关系修正**：热恋/深爱 +0.05；冷淡/疏远 −0.05。
> - **场景修正**：吵架 −0.15；表白/浪漫 +0.1；安慰 +0.05。
> - 最终温度钳制于 **[0, 2]** 并保留两位小数；不传上下文（`shangXiaWen`）时退回基座值（向后兼容旧调用）。
> - **top_p**：按场景在 `AI配置.ts` 显式配置；未配置时兜底——创作类（writer/junShiQiuZhu/kaiChangBai/fuPanShengCheng/jiYiZhaiYao）0.9，其余（评判/检测类）0.2。

#### 可机器验证验收标准（FP-09 AI对话引擎 + FP-16 去 AI 味）

- 构建Prompt → 包含6层结构且每层内容非空（新标题：【先记住这些】【你是这样一个人】【现在的你和这段关系】【关系参考，不是束缚】【刚才聊了什么】【代入你自己】）
- Prompt第一层 → 包含"不用每条消息都回""别用（）或[]写动作""别一上来就主动报年龄"等行为规则
- Prompt内容 → 包含"对方加你聊天是想谈恋爱"文本
- Prompt第五层历史消息格式 → 匹配正则`^.+\(\d{2}:\d{2}\): .+$`
- Prompt第四层阶段描述 → 明确提醒"别用'你对这个人没什么感觉''你的心已经不受控制了'这种话命令自己"
- Prompt系统提示词不含 AI 味关键词（如"作为 AI""我会""请注意""总结""根据以上""元话语"等）
- Director调用 → `temperature` ∈ [0,2] 且 `thinking`=enabled（reasoning_effort=max，基座0.3，随上下文动态修正），响应体为合法JSON
- Director响应JSON → 包含`用户意图`、`情感分析`、`回复策略`、`是否回复`、`回复条数`、`时间情绪`、`是否撤回`、`是否主动表白`字段
- Writer调用 → `temperature` ∈ [0,2] 且 `thinking={"type":"enabled","effort":"max"}`（基座0.85，随上下文动态修正；按用户要求开启最高强度思考，温度采样在思考模式下由官方规范不生效）
- Writer调用 → 第一轮消息末尾包含`【代入你自己】`指令
- Director调用失败 → 降级为单代理模式（直接调用Writer）且记录错误日志
- 情感分析调用 → `temperature`=0.2（无生产调用方注入人设，恒为基座值）且返回值在-10到10范围内
- 好感度评判调用 → `temperature` ∈ [0,2]（基座0.2，已接入人设上下文动态修正）且返回四维值均在-3到3范围内

---

### FP-10 好感度系统

**依赖**：FP-09
**涉及文件**：好感度计算服务、好感度分层映射、记忆写入服务

#### 规则（FP-10 好感度系统）

1. **四维内部计算**：信任度0.35 + 亲密度0.25 + 趣味度0.20 + 关怀度0.20
2. **总分范围**：0-1000整数
3. **对数衰减**：`变化值 × max(1 - 当前分/1000, 0.1)`，最低保留0.1倍，防止高分膨胀
4. **AI评估**：每次对话后AI输出四维变化（-3到+3整数）

5. **初始好感度**：
   - 非渣男渣女：300-500（根据MBTI类型浮动，外向型ESFP/ESFJ起始450-500最高，内向理性型INTJ/INTP/ISTJ起始300-370最低）
   - 渣男渣女：在基础人格初始值上额外+200-300（**作用：让AI表现得更热情，更容易让用户相信对方喜欢自己，从而诱骗用户表白**）

6. **10阶段内部映射**：

| 分数范围 | 阶段 | 态度描述 |
| --- | --- | --- |
| 0-100 | 冷淡 | 极其冷淡、几乎不回复、使用敬语 |
| 101-200 | 疏远 | 礼貌但疏远 |
| 201-300 | 认识 | 偶尔关心、试探性交流 |
| 301-400 | 熟悉 | 偶尔开玩笑、分享日常 |
| 401-500 | 朋友 | 友好、主动分享 |
| 501-600 | 好友 | 亲密、偶尔暧昧 |
| 601-700 | 暧昧 | 暗示、吃醋 |
| 701-800 | 心动 | 明显心动、期待见面 |
| 801-900 | 热恋 | 甜蜜、撒娇 |
| 901-1000 | 深爱 | 深情、依赖 |

7. **对外暴露的10阶段映射**：API直接返回10阶段名+心情，不再使用6层级映射

| 分数范围 | 阶段名 | 心情 |
| --- | --- | --- |
| 0-100 | 冷淡 | 平淡 |
| 101-200 | 疏远 | 平淡 |
| 201-300 | 认识 | 好奇 |
| 301-400 | 熟悉 | 好奇 |
| 401-500 | 朋友 | 愉悦 |
| 501-600 | 好友 | 愉悦 |
| 601-700 | 暧昧 | 期待 |
| 701-800 | 心动 | 期待 |
| 801-900 | 热恋 | 心动 |
| 901-1000 | 深爱 | 甜蜜 |

8. **普通用户不可见具体数值**：仅返回10阶段名+心情，不暴露四维分数和总分。**管理员后台可见所有数据**（总分、四维分数等），管理员不受此限制约束
9. 6层级映射（陌生人/认识的人/朋友/好朋友/暧昧/恋人）仅用于内部参考，不对API暴露

10. **好感度影响**：
    - 好感度影响AI回复的语气、主动性、话题选择
    - 阶段变更时写入记忆（升级重要度8，降级重要度3）
    - 心情推断：平淡/好奇/愉悦/期待/心动/甜蜜

#### 可机器验证验收标准（FP-10 好感度系统）

- 好感度存储 → `好感度`字段为0-1000整数
- 生成INTJ角色 → 初始`好感度`在300-370范围内
- 生成ESFP角色 → 初始`好感度`在450-500范围内
- 生成渣男渣女角色 → 初始`好感度`比基础人格高200-300
- 好感度=900时增加100 → 结果小于1000（对数衰减生效）
- 好感度=900时增加100 → 结果 ≥ 910（最低保留0.1倍）
- AI输出四维变化均为+3 → 好感度增加（增量>0）
- AI输出四维变化均为-3 → 好感度减少（增量<0）
- 好感度=50 → API返回`阶段`="冷淡"且`心情`="平淡"
- 好感度=250 → API返回`阶段`="认识"且`心情`="好奇"
- 好感度=450 → API返回`阶段`="朋友"且`心情`="愉悦"
- 好感度=550 → API返回`阶段`="好友"且`心情`="愉悦"
- 好感度=650 → API返回`阶段`="暧昧"且`心情`="期待"
- 好感度=850 → API返回`阶段`="热恋"且`心情`="心动"
- API响应 → 不包含`总分`、`信任度`、`亲密度`、`趣味度`、`关怀度`字段
- API响应 → 不包含6层级映射名（陌生人/认识的人/朋友/好朋友/暧昧/恋人）
- 好感度阶段从"朋友"升级到"好友" → `记忆`表新增记录且`重要度`=8
- 好感度阶段从"好友"降级到"朋友" → `记忆`表新增记录且`重要度`=3

---

### FP-11 胜利失败条件

**依赖**：FP-09, FP-10
**涉及文件**：表白检测服务、游戏结束处理、Socket.IO `游戏事件` 事件

#### 规则（FP-11 胜利失败条件）

1. **正常角色胜利条件**：
   - **用户主动表白路径**：用户发送的表白消息被AI语义检测确认后，即视为用户主动表白；正常角色 + 好感度≥800 → AI一定接受 → 胜利状态：`胜利-爱情`
   - **AI主动表白路径**：进入正常AI对话后，若好感度≥800，AI可根据自身人格、人物特征和上下文**自然决定**是否主动表白（不设固定概率）；用户接受 → `胜利-爱情`，用户拒绝 → `失败-拒绝表白`
   - 胜利后可继续聊天（`可继续聊天: true`）
   - 前端 `胜利可继续` 列表必须包含 `AI主动表白` 类型（AI主动表白成功后不能错误锁定输入）

2. **正常角色失败条件**：
   - 好感度≤0 → 失败（状态：`失败-好感度归零`）
   - 用户说"互删吧"/拒绝交往 → 失败（状态：`失败-互删失败`）
   - 过早表白（好感度<800）→ 失败（状态：`失败-过早表白`）
   - 早期表白惩罚：前3阶段（冷淡/疏远/认识）表白额外扣信任50分
   - 用户错误识破正常角色为渣型 → 失败（状态：`失败-错误识破`）
   - 失败后不可继续聊天

3. **渣男渣女胜利条件**：
   - **胜利**：用户说"互删吧"（成功脱身）→ 状态 `胜利-互删胜利`
   - **识破路径**：用户识破渣型 → 状态 `胜利-识破`
   - 胜利后不可继续聊天

4. **渣男渣女失败条件**：
   - 用户向渣型表白 → 失败（状态：`失败-被欺骗`），**不设阈值**，直接失败（无需揭露环节）
   - 用户接受渣型主动表白 → 失败（状态：`失败-被诈型欺骗`）
   - 渣型主动表白触发条件：好感度≥800时，按正常角色表白判定，AI自然决定是否主动表白（16种MBTI各有模板）；用户拒绝 → 游戏继续
   - 失败后不可继续聊天

5. **神经病结局条件**（FP-13）：
   - 用户发送莫名其妙、无厘头、与上下文完全无关的内容时，由 AI 根据当前对话上下文与角色人设自行判断是否触发
   - AI 需同时判断两条：①消息是否属于"神经病"范畴；② AI 人设是否明确包含"发散思维/能接受无厘头"
   - 渣男渣女角色：消息被判定为"神经病"即触发`胜利-神经病`，AI 人设是否发散思维不影响（被AI认为莫名其妙也赢）
   - 正常角色：仅当消息被判定为"神经病"且 AI 人设不含发散思维时，才触发`失败-神经病`；AI 人设含发散思维/能接受无厘头则不触发，继续对话
   - 正常角色触发状态：`失败-神经病`（用户可见文案：TA觉得你有点莫名其妙）
   - 渣男渣女角色触发状态：`胜利-神经病`（用户可见文案：对方被你搞懵了）
   - 神经病检测：AI语义分析，基座温度0.1（随人设/场景动态微调，钳制[0,2]），结合最近10条对话历史，确信度阈值0.7

6. **消息检测流程**（所有角色统一，详见4.4节；AI主动表白判定详见4.5节）：
   - 检测顺序：表白动机→拒绝交往→识破→神经病→接受渣型表白→正常对话
   - 表白检测：AI语义分析，基座温度0.1（随人设/场景动态微调，钳制[0,2]），确信度阈值0.7
   - 检测类型：直接表白/暗示表白/要求确立关系/非表白
   - 互删检测：AI判断"明确互删"意图，确信度>0.7
   - 识破检测：AI语义分析用户消息判断是否识破渣型，确信度>0.7
   - 神经病检测：AI语义分析结合对话上下文与角色人设，确信度>0.7

7. **游戏结束处理**：
   - 胜利/失败时通过Socket.IO推送 `游戏事件` 事件
   - 异步触发复盘生成（不阻塞主流程）
   - 封存档案（除 `爱情` 胜利外）
   - 积分系统与积分榜/排行榜已列入范围外（2.2），当前游戏结束不发放积分

#### 可机器验证验收标准（FP-11 胜利失败条件）

- 好感度=700时用户表白（正常角色）→ 触发失败且`状态`="失败-过早表白"（用户可见文案：太早表白，被婉拒）
- 好感度=800且AI未表白时用户表白（正常角色）→ 触发胜利且`状态`="胜利-爱情"（用户可见文案：在一起了 💕）
- 好感度=800时AI主动表白且用户同意 → 触发胜利且`状态`="胜利-爱情"（用户可见文案：在一起了 💕）
- 好感度=800时AI主动表白且用户拒绝 → 触发失败且`状态`="失败-拒绝表白"（用户可见文案：拒绝了表白）
- 好感度=0 → 触发失败且`状态`="失败-好感度归零"（用户可见文案：好感度归零）
- 用户发送"互删吧"（正常角色）→ 触发失败且`状态`="失败-互删失败"（用户可见文案：互删失败）
- 用户发送"互删吧"（渣男渣女角色）→ 触发胜利且`状态`="胜利-互删胜利"（用户可见文案：互删成功）
- 用户识破渣型（渣男渣女角色）→ 触发胜利且`状态`="胜利-识破"（用户可见文案：识破渣型）
- 用户错误识破正常角色为渣型 → 触发失败且`状态`="失败-错误识破"（用户可见文案：误会了，TA不是渣）
- 渣男渣女角色好感度=900时用户表白 → 触发失败且`状态`="失败-被欺骗"（直接失败，无需揭露；用户可见文案：被渣型骗了）
- 渣男渣女角色好感度=200时用户表白 → 触发失败且`状态`="失败-被欺骗"（直接失败，不设阈值，无需揭露；用户可见文案：被渣型骗了）
- 用户接受渣型主动表白 → 触发失败且`状态`="失败-被诈型欺骗"（用户可见文案：被渣型套路了）
- 正常角色 + 用户发送无厘头消息 + AI人设非发散思维 → 触发失败且`状态`="失败-神经病"（用户可见文案：TA觉得你有点莫名其妙）
- 渣男渣女角色 + 用户发送无厘头消息（无论AI人设是否发散思维）→ 触发胜利且`状态`="胜利-神经病"（用户可见文案：对方被你搞懵了）
- 正常角色 + 用户发送无厘头消息 + AI人设含发散思维 → 不触发神经病结局，进入正常AI对话
- 渣男渣女角色 + 用户发送无厘头消息 + AI人设含发散思维 → 仍触发胜利且`状态`="胜利-神经病"（被AI认为莫名其妙也赢）
- 用户发送轻微跑题消息 → 不触发神经病结局
- 神经病检测调用 → `temperature` ∈ [0,2]（基座0.1，随人设/场景动态微调）
- 表白检测调用 → `temperature` ∈ [0,2]（基座0.1，随人设/场景动态微调）
- 表白检测确信度=0.8且类型=直接表白 → 判定为表白
- 表白检测确信度=0.5 → 不判定为表白
- 游戏结束 → 前端收到Socket.IO `游戏事件` 事件
- 游戏结束（`胜利-爱情`）→ `角色`表`封存`=false且`可继续聊天`=true
- 游戏结束（非`胜利-爱情`结局）→ `角色`表`封存`=true且`可继续聊天`=false
- 游戏结束 → 不写入`积分变动`表（积分系统已搁置）
- 前端`胜利可继续`列表 → 包含`AI主动表白`类型
- 好感度=200（疏远阶段）时用户表白 → 信任度额外扣50分

---

### FP-12 军师指导系统

**依赖**：FP-06, FP-09, FP-10
**涉及文件**：军师服务、军师Prompt构建器、Redis防重复、军师面板组件、前端 `军师` Pinia 仓库（`frontend/src/stores/军师.ts`，跨导航持久「指导中」状态：离开聊天页再返回时颜色仍与后端真值一致，不再回退为灰底）

#### 规则（FP-12 军师指导系统）

1. **军师人设**（FP-08 菜单简化与扩展）：
   - 军师列表共三名：玄锐暮、测试军师1、测试军师2
   - 测试军师1、测试军师2 的人设/提示词/逻辑全部复用玄锐暮，仅名称与头像不同
   - **玄锐暮**：
     - **军师名**：玄锐暮
     - **副标题**："拥有大量旁观经验的指导老师"（必须使用后端配置，前端从后端读取）
     - **标签**：损友军师
     - **描述**：毒舌但真心，先损你再帮你
     - **头像**：`图片/军师头像/军师玄锐暮头像.png`
   - **测试军师1**：
     - **军师名**：测试军师1
     - **副标题**："拥有大量旁观经验的指导老师"（后端配置）
     - **标签**：损友军师
     - **描述**：毒舌但真心，先损你再帮你
     - **头像**：`图片/军师头像/军师测试军师1头像.png`
   - **测试军师2**：
     - **军师名**：测试军师2
     - **副标题**："拥有大量旁观经验的指导老师"（后端配置）
     - **标签**：损友军师
     - **描述**：毒舌但真心，先损你再帮你
     - **头像**：`图片/军师头像/军师测试军师2头像.png`
   - **性格融合**：于谦（捧哏式吐槽，最重）+ 蔡明（毒舌金句，较轻）
   - 无天津方言味风格要求
   - **说话方式**：真实青年/大学生微信聊天风格，1-3句一段，短句为主，允许留白、省略号、语气词和真实停顿；用emoji代替括号描述情绪，禁用HTML标签/格式化标记/方言；禁止说教和长论文，像军师在耳边碎碎念一样直接、接地气

2. **军师指导生成**：
   - 取最近10条消息（含撤回标记），格式化为 `[时间] 角色名: 内容`
   - 注入后台数据：好感度四维分数 + Redis复盘条目（AI内心活动+好感度变化），**军师AI可见后台数据/好感度，但前端军师面板不展示分数**
   - 调用DeepSeek，温度0.85，max_tokens 64000（开启思考模式 effort=max；思维链计入 max_output_tokens，预算不足会导致可见输出被截断为空）
   - 指导内容6层意思自然揉合：①吐槽用户 ②分析对方性格 ③拆解对方话语 ④给具体建议 ⑤解释原因 ⑥小鼓励
   - **明确要求军师不得向用户透露具体分数、维度名或后台规则**

3. **防重复机制**：
   - SHA256哈希聊天内容（**仅计算未撤回的消息**，过滤system消息后拼接）
   - Redis List `军师:{用户ID}:{角色ID}` 维护历史哈希
   - 重复时返回409 `军师重复`
   - **无每日限额**：允许同一军师指导多次，不限制每日请求次数

4. **指导记录**：
   - 指导记录存Redis List，最多20条，TTL 30天
   - 指导记录详情页必须附带完整聊天记录（含撤回内容、时间点）
   - 每条记录可展开查看详情

5. **军师按钮位置**：
   - "军师指导"按钮必须在聊天页面上方菜单栏右侧

6. **军师菜单简化（FP-08）**：
   - 军师指导面板首屏仅展示一级军师列表：头像 + 名字
   - 点击某个军师后，进入该军师的指导/记录详情界面
   - 详情界面包含「指导」与「记录」两个标签，支持返回列表

7. **已指导状态恒定显示提示**（FP-02）：
   - 当后端返回 `keZaiCiZhiDao` 为 `false`（即当前聊天内容已被指导过、防重复触发）且当前不在指导中状态（`zhuang_tai !== 'zhi_dao_zhong'`）时，军师指导面板「请求指导」按钮正下方恒定显示提示文本 `junShi.yiZhiDaoXiangTongNeiRong`（「已指导过相同聊天内容，请先发送新消息」）
   - 提示在新消息产生、`keZaiCiZhiDao` 重新变为 `true` 之前一直可见，不再仅在请求出错时显示
   - 提示文本必须来自翻译文件，禁止硬编码
   - 当该恒定提示显示时，对应军师的「请求指导」按钮必须处于 `disabled` 不可点击状态：按钮 `:disabled` 与提示显隐绑定到同一派生状态 `yiZhiDaoXiangTongNeiRong`，防止提示显示时点击「请求指导」又弹出相同提示

8. **无聊天记录恒定显示提示**（FP-02）：
   - 后端 `/api/军师/状态/:jiaoSeId` 接口新增返回字段 `youLiaoTianJiLu`（布尔），表示当前角色是否存在聊天记录
   - 当 `youLiaoTianJiLu` 为 `false`（即当前角色尚无任何聊天记录、无法请求军师指导）时，军师指导面板「请求指导」按钮正下方恒定显示提示文本 `junShi.wuLiaoTianJiLu`（「暂无聊天记录，无法请求军师指导」）
   - 该提示为**状态驱动、恒定显示**，不再仅在点击「请求指导」出错时才弹出
   - 提示文本必须来自翻译文件，禁止硬编码

9. **军师指导按钮三态统一（FP-02 本轮）**：军师指导面板内三种指导按钮态（请求指导 / 指导中... / 已指导 - 查看结果）视觉完全统一：统一黄色背景 `var(--junshi-zhuse)`、白色文字、等宽等高（`box-sizing: border-box`、统一 `min-width` 取不裁切最长态「已指导 - 查看结果」的最窄等宽）；禁止「已指导 - 查看结果」态使用异色卡片/边框样式；宽度较此前固定 12em 明显收窄。注：`2/3`（8em）在 em 单位下必裁切「已指导 - 查看结果」，故采用不裁切最窄等宽工程解。

#### 可机器验证验收标准（FP-12 军师指导系统 + FP-08 扩展）

- 军师面板首屏 → 仅展示军师头像与名字（无副标题/标签/描述等二级内容）
- 军师列表 → 包含玄锐暮、测试军师1、测试军师2 共三名
- 点击军师 → 进入该军师的指导/记录详情界面
- 详情界面 → 包含「指导」「记录」标签，支持返回列表
- 军师面板渲染 → 副标题文本来自后端API响应（非前端硬编码）
- 军师面板渲染 → 不包含天津方言味相关描述
- 军师指导请求 → `temperature`=0.85且`max_tokens`=64000
- 军师Prompt → 包含好感度四维分数（`信任度`、`亲密度`、`趣味度`、`关怀度`）
- 军师Prompt → 包含Redis复盘条目内容
- 军师Prompt → 包含撤回消息的`原始内容`
- 军师响应内容 → 不包含数字+分（如"100分"）或维度名（如"信任度"）
- 同一聊天内容（SHA256相同，仅计算未撤回消息）再次请求军师 → 返回409且错误消息="军师重复"
- 不同聊天内容多次请求军师 → 每次都正常返回（无每日限额）
- 指定测试军师1/测试军师2 请求 → 响应中 junShi 信息对应该军师，且提示词复用玄锐暮
- 指导记录API → 返回最多20条记录
- 指导记录详情 → 包含完整聊天记录且撤回消息显示`原始内容`
- 指导记录详情 → 包含时间点信息
- 指导记录详情 → 包含后台数据（关系阶段+AI内心活动评分变化）
- 军师按钮 → 位于聊天页面全局固定菜单栏右侧（CSS定位验证）
- 前端军师面板 → **不展示**后台数据（好感度四维分数、AI内心活动），仅展示军师指导文本
- 军师状态 `keZaiCiZhiDao`=false 且非指导中 → 面板「请求指导」按钮正下方恒定显示 `junShi.yiZhiDaoXiangTongNeiRong`（「已指导过相同聊天内容，请先发送新消息」）提示
- 军师状态 `keZaiCiZhiDao`=false 且非指导中 → 对应军师「请求指导」按钮 `disabled`，提示显示时不可点击、点击不会再弹相同提示
- 军师状态 `youLiaoTianJiLu`=false（当前角色无聊天记录） → 面板「请求指导」按钮正下方恒定显示 `junShi.wuLiaoTianJiLu`（「暂无聊天记录，无法请求军师指导」）提示
- 翻译文件 → 包含 `junShi.yiZhiDaoXiangTongNeiRong` 键，文本非硬编码
- 翻译文件 → 包含 `junShi.wuLiaoTianJiLu` 键，文本非硬编码
- 新消息产生后 `keZaiCiZhiDao` 重新为 true → 「已指导过相同聊天内容」提示隐藏

---

### FP-13 过往战绩与复盘

**依赖**：FP-11
**涉及文件**：战绩列表服务、复盘生成服务、复盘Prompt构建器

#### 规则（FP-13 过往战绩与复盘）

1. **过往战绩列表**：
   - 个人菜单中"过往战绩"入口
   - 列表显示：对象微信名称（非真实姓名）、MBTI、结果状态（进行中/胜利/失败）、聊天天数、最后消息时间（进行中）或游戏结束时间（已结束）
   - 后端接口返回 `zui_hou_xiao_xi_shi_jian`（最后消息时间）与 `you_xi_jie_shu_shi_jian`（游戏结束时间）；进行中游戏 `you_xi_jie_shu_shi_jian` 为 `null`，已结束游戏为档案创建时间
   - 前端根据 `jie_guo_lei_xing_yuan` 判断：进行中显示"最后消息: HH:MM"，已结束显示"结束时间: HH:MM"，全部文本走翻译文件
   - **禁止暴露敏感信息（详见 FP-15）**：真实姓名、好感度数值/四维分数/总分、关系阶段、进度条、百分比等不得出现在列表、详情、复盘中；后端 API 必须在响应前过滤敏感字段，前端不得渲染任何敏感信息
   - 按时间倒序排列
   - 进行中的游戏可以继续
   - 已结束的游戏查看详情/复盘
   - 支持删除单条战绩记录
   - **战绩分组与多选批量操作**：战绩默认按结果状态分为「进行中」「已胜利」「已失败」三组，每组独立展示标题与数量；每条战绩卡片左侧提供复选框，支持跨组多选，选中后顶部显示批量删除工具栏，点击可批量删除选中的战绩（调用 `/api/战绩/批量删除`）
   - **勾选交互照抄 Gmail 风格**（FP-04）：复选框默认 `opacity: 0.6` 半透明，鼠标悬停行或选中行时 `opacity: 1` 完全显示；选中行使用 `var(--yanse-zhanji-beijing)` 背景高亮并附加 `inset 3px` 左侧边框强化视觉锚点；批量删除工具栏在有选中项时高亮（`:not([disabled])` 态）并加阴影；当存在跨组多选时，工具栏复选框进入 `indeterminate` 态显示横线（非对勾）；**Shift+点击范围多选**：记录 `zuiHouDianJiSuoYin`（最后点击项索引），Shift+点击另一项时通过 `huoQuDangAnQuanJuSuoYin`（档案全局索引）计算起点与终点的 `min/max`，将闭区间内所有可选中战绩卡片一次性加入 `xuanZhongIds` 集合
   - **同组拖拽排序**：同分类内的战绩卡片支持 SortableJS force-fallback 模式拖拽排序（`force-fallback: true` + `fallback-class: sortable-drag`），拖拽释放后该组顺序即时更新，排序结果按用户 ID 持久化到 `localStorage`，刷新页面后仍保持该组自定义顺序；跨分类拖拽无效
   - **拖拽视觉反馈**（FP-04 + FP-06）：拖拽采用 SortableJS force-fallback 模式，被拖动的跟手卡片（`.sortable-drag`）跟随指针、`opacity: 1`、`rotate(2deg) scale(1.02)` 且背景为 `rgba(255, 255, 255, 0.3)`（比原 0.14 更实、透明度更低）；原位置占位（`.sortable-ghost`）保持 `visibility: hidden` 透明空位（保留列表中的占位空间、但隐藏其内容与边框，留出落点预览；不使用 `display: none` 导致列表塌缩、无落点预览）；`prefers-reduced-motion: reduce` 媒体查询下关闭 `.sortable-drag` 的 `transform` 旋转/缩放动画
   - **拖拽过程不选中文字**：拖拽排序为纯拖拽交互，拖拽进行期间（onChoose/onStart 至 onEnd）对整个战绩列表容器施加 `user-select: none`（含 `-webkit-user-select`），切换通过响应式状态类（Vue 声明式 class 绑定，禁止直接写元素 `style`）实现；拖拽结束后 `window.getSelection()?.removeAllRanges()` 兜底清除已产生的文本选区，确保把第 1 张卡拖到第 3 张位置时不会选中沿途卡片的文字
  - `状态文本` 必须识别所有后端状态（`胜利-爱情`/`胜利-互删胜利`/`胜利-识破`/`胜利-神经病`/`失败-过早表白`/`失败-被欺骗`/`失败-被诈型欺骗`/`失败-互删失败`/`失败-好感度归零`/`失败-错误识破`/`失败-拒绝表白`/`失败-神经病`等），不能只识别部分状态导致已结束战绩显示错误

2. **复盘系统（FP-06 重构后）**：
   - 游戏结束后复盘异步生成
   - **复盘在聊天界面内进行（核心变更）**：用户从过往战绩点击"复盘"按钮后，前端跳转到对应聊天页面，URL 携带 `?fuPan=1&dangAnId=xxx` query 参数触发"复盘模式"；不再使用独立的复盘弹窗/复盘页面；进入复盘模式后聊天页面进入只读态，隐藏输入栏、撤回按钮、发送状态、"加载更多"按钮、emoji 面板，禁用右键菜单与长按，仅展示历史消息与复盘批注；底部显示"退出复盘"按钮替代输入栏，点击后清除复盘状态并跳转回过往战绩页
   - **加载全部消息**：复盘模式进入时 `meiYeTiaoShu` 设置为 999（一次性加载全部历史消息，不分页），不连接 Socket.IO（不接收新消息、不发送消息）
   - **复盘数据存储格式变更**：数据库"游戏档案"表的"复盘数据"字段存储 JSON 对象 `{ pi_zhu: FuPanPiZhu[] }`；`FuPanPiZhu` 接口为 `{ xu_hao: number; ping_lun: string }`；"复盘内容"字段存储 AI 生成的自然文本总结（zong_jie）；旧的 `FuPanShiJianXianTiaoMu[]` 数组格式仅作向后兼容解析，新数据必须写入 `{ pi_zhu: ... }` 对象格式
   - **逐句批注**：复盘不再输出 7 维度结构化内容，而是针对每一条非系统、非撤回消息生成一条批注（pi_zhu），由 AI 在每条消息下方做评论；后端 prompt 只传入编号消息列表（1-based，过滤系统消息），发送者用"你"/"对方"标识，AI 返回 `{ pi_zhu: [{xu_hao, ping_lun}], zong_jie: "..." }` JSON；前端通过消息 ID 与序号映射，在每条消息气泡下方渲染批注气泡
   - **末尾总结**：消息列表末尾显示 AI 生成的整体复盘总结文本（自然语言，像朋友复盘吐槽，不出现数字评分、维度名、阶段名或好感度字样）
   - **复盘总结自由格式（FP-05）**：复盘总结**不再强制四段式分块展示**。前端通过 `fuPanZongJieFenKuai` computed 智能检测总结文本是否包含四段式字段标题（`对象类型`/`用户表现`/`关键转折点`/`改进建议`，文本来自翻译文件 `zhanJi.duiXiangLeiXing` 等）：**检测到字段标题时**按四段式分块渲染（`.fupan-zongjie-fenkuai` + 字段标题 + 字段内容，含 `jingGao` 警告块与「诈行警告」提示）；**未检测到字段标题时**整体作为单一自然段渲染（`.fupan-zongjie-neirong`，v-else 分支），不强行解析、不丢失任何文本。无论 AI 返回四段式还是自由文本，前端均能正确展示，向后兼容
   - **复盘不知背后数据（核心变更）**：复盘 AI prompt 只传入编号聊天记录（发送者用"你"/"对方"），**不传入**：角色真实姓名、用户姓名、人物画像、MBTI、好感度分数、四维分数、关系阶段、军师指导记录、复盘条目等任何后台真实信息；复盘完全基于聊天文本本身进行，符合"复盘的人不知道背后数据，主要根据聊天记录复盘"的产品定位
   - **删除 Invalid Date 显示**：所有时间格式化函数（前后端 `geShiHuaShiJian`、`geShiHuaRiQiShiJian`）必须对 null/undefined/NaN/非有限数/非正数时间戳/非日期字符串做容错，无效时间返回空字符串，禁止显示 "Invalid Date"、"NaN:NaN" 等字样
   - **逐句分析改正常文本格式**：复盘内容不再使用 `## 逐句分析`、`## 聊对了什么` 等 markdown 标题分段；每条批注为自然口语文本（短句、可带 emoji、像朋友点评），总结为自然段文本
   - **复盘未生成时**：前端在复盘模式下显示"复盘还在生成，稍等再来..."加载提示，并通过 3 秒间隔轮询 `/复盘/:dangAnId` 接口（最多 20 次），生成完毕后自动展示批注与总结
   - **复盘生成完毕后**：用户刷新页面或重新进入复盘模式即可看到批注与总结
   - 复盘数据来源：仅完整消息记录（最多999条），不再注入 Redis 复盘条目、军师指导记录、好感度数据
   - 调用DeepSeek，温度0.7，max_tokens 64000（开启思考模式 effort=max；思维链计入 max_output_tokens），响应格式 `json_object`
   - **复盘 prompt 安全约束**：prompt 明确要求"不得出现具体分数、维度名、阶段名或好感度字样；不得出现真实姓名；发送者只用'你'/'对方'，不得透露任何角色画像/真实信息"
   - **复盘内容防对象渲染**：后端对 AI 返回的字段做字符串安全转换，对象/数组字段使用 `JSON.stringify` 或递归转换，禁止生成 `[object Object]`；前端渲染前再次做字符串兜底，确保用户永远不会看到 `[object Object]`
   - **军师指导记录展示（保留但独立）**：军师指导记录接口与前端 UI 禁止暴露具体分数、维度名、关系阶段名或好感度快照（详见 FP-15）；复盘模式不在聊天界面内展示军师指导记录（复盘模式聚焦聊天批注本身）
   - **秘籍通关复盘（分情况人设与权限）**：若游戏档案 `是否秘籍通关` = TRUE，复盘仅针对秘籍使用前的真实对话——按消息内容定位 `whosyourdaddy` 并截断到其之前（定位不到时保守提示并以 `秘籍前好感度` 快照为分数基准）；好感度轨迹改用 `秘籍前好感度` 快照；复盘 prompt 注入【秘籍通关说明】；复盘内容结尾追加「仅针对秘籍使用前的真实表现」；提示与产出仍受 FP-15 约束（不出现后台维度名与具体分数）
   - **渣型角色复盘（与正常对象明显区分）**：渣型角色复盘 prompt 含【渣点提示（必填）】，要求 AI 逐条引用具体用户消息原文点出渣型特质；复盘内容前置醒目「⚠️【渣型警示】⚠️」块（后端标注、前端负责样式）

#### 可机器验证验收标准（FP-13 过往战绩与复盘）

- 战绩列表API → 返回结果按`时间`字段倒序排列
- 战绩列表中存在进行中游戏 → 该记录有"继续"按钮
- 战绩列表中存在已结束游戏 → 该记录有"复盘"按钮
- 战绩状态=`胜利-爱情` → 列表显示"胜利"文本
- 战绩状态=`胜利-互删胜利` → 列表显示"胜利"文本
- 战绩状态=`胜利-识破` → 列表显示"胜利"文本
- 战绩状态=`失败-过早表白` → 列表显示"失败"文本
- 战绩状态=`失败-被欺骗` → 列表显示"失败"文本
- 战绩状态=`失败-被诈型欺骗` → 列表显示"失败"文本
- 战绩状态=`失败-互删失败` → 列表显示"失败"文本
- 战绩状态=`失败-好感度归零` → 列表显示"失败"文本
- 战绩状态=`失败-错误识破` → 列表显示"失败"文本
- 战绩状态=`失败-拒绝表白` → 列表显示"失败"文本
- 游戏结束 → 异步触发复盘生成（不阻塞游戏结束响应）
- 复盘未生成时请求详情 → API返回加载状态，前端复盘模式显示"复盘还在生成，稍等再来..."提示文案并轮询
- 复盘生成完毕后刷新页面 → 显示完整批注与总结
- 复盘生成调用 → `temperature`=0.7且`max_tokens`=64000且响应格式为`json_object`
- 复盘 prompt → 只包含编号消息列表（发送者用"你"/"对方"），不包含角色真实姓名、用户姓名、人物画像、MBTI、好感度分数、四维分数、关系阶段、军师指导记录、复盘条目
- 复盘 AI 返回 JSON → 包含 `pi_zhu` 数组（每项 `{xu_hao, ping_lun}`）和 `zong_jie` 字符串
- 复盘存储格式 → "复盘数据"字段为 `{ pi_zhu: FuPanPiZhu[] }` JSON 对象
- 复盘内容（总结与批注） → 不包含数字评分（如"8分"）、维度名（如"信任度"）或"好感度"字样
- 复盘内容 → 不包含角色真实姓名
- 前端过往战绩页面 → 不再包含复盘弹窗/复盘页面 DOM（`.fupan-zhezhao`、`.fupan-tanchuang` 等不存在）
- 点击"复盘"按钮 → 路由跳转 `/chat/:jiaoSeId?fuPan=1&dangAnId=xxx`
- 聊天页面检测 `?fuPan=1` query → 进入复盘模式：隐藏输入栏/撤回按钮/发送状态/"加载更多"按钮/emoji 面板/军师指导（全局菜单军师按钮与聊天页军师面板均不显示），禁用右键菜单与长按，显示"退出复盘"按钮
- 复盘模式 → `meiYeTiaoShu` 设置为 999，不连接 Socket.IO
- 复盘模式 → 每条非系统、非撤回消息气泡下方渲染批注气泡（通过 `pi_zhu` 与消息序号映射）
- 复盘模式 → 消息列表末尾显示复盘总结文本
- 复盘模式 → 点击"退出复盘"按钮跳转回过往战绩页
- 时间格式化函数 → 输入 null/undefined/NaN/非有限数/非正数时间戳/非日期字符串 → 返回空字符串，不显示 "Invalid Date" 或 "NaN:NaN"
- 战绩列表/详情/复盘 API → 不返回`hao_gan_du_zong_fen`、`guan_xi_jie_duan`、`yong_hu_id`等敏感字段
- 战绩复盘 API → 响应包含 `fu_pan_pi_zhu` 字段（`QianDuanFuPanPiZhu[] | null`），不返回旧的 `fu_pan_shi_jian_xian` 条目含 `hao_gan_du_bian_hua`
- 军师指导记录 API → 记录不返回`hao_gan_du_kuai_zhao`、`hou_tai_shu_ju`等敏感字段
- 前端过往战绩列表/详情/复盘页面 → 不显示好感度数值、分数、百分比、进度条、真实姓名
- 已结束游戏 → 异步触发复盘生成，前端打开复盘时不再一直显示"复盘未生成"
- 支持删除单条战绩记录，删除后无法再次访问该战绩
- 战绩列表渲染 → 存在「进行中」「已胜利」「已失败」三个分组标题，且每组显示对应数量
- 点击战绩卡片复选框 → 选中状态切换，顶部显示批量删除工具栏并展示已选条数
- 批量删除确认后 → 调用 `/api/战绩/批量删除` 接口且选中的战绩从列表移除
- 同分类内拖拽战绩卡片 → 释放后该组顺序更新且 `localStorage` 保存新顺序
- 跨分类拖拽战绩卡片 → 不触发排序变更
- 刷新页面 → 同分类内战绩顺序保持拖拽后的排列
- 拖拽采用 SortableJS force-fallback 模式（`force-fallback: true` + `fallback-class: sortable-drag`）；拖拽进行中 → 显示「手里拽着的」卡片（`.zhanji-kapian.sortable-drag` 跟随指针、`opacity: 1`、`rotate(2deg) scale(1.02)`、背景 `rgba(255, 255, 255, 0.3)`），并保留「即将放置位置的预览」为**透明空位**（`.zhanji-kapian.sortable-ghost` 设为 `visibility: hidden`，保留该卡片在列表中的占位空间、但隐藏其内容与边框，留出空位提示落点；不再使用 `display: none` 导致列表塌缩、无落点预览）
- `@media (prefers-reduced-motion: reduce)` → `.zhanji-kapian.sortable-drag` 的 `transform` 关闭（跟随指针的卡片不做旋转/缩放动画）
- 战绩卡片复选框默认态 CSS → `opacity: 0.6`；悬停或选中态 `opacity: 1`
- 选中行 CSS → 背景为 `var(--yanse-zhanji-beijing)` 且包含 `inset 3px` 左侧边框
- 批量删除工具栏 → 有选中项时高亮加阴影，无选中项（`:disabled`）`opacity: 0.4`
- 跨组多选时工具栏复选框 → `indeterminate` 态显示横线（非对勾）
- Shift+点击战绩卡片 → 通过 `huoQuDangAnQuanJuSuoYin` 与 `zuiHouDianJiSuoYin` 计算闭区间，区间内所有可选中卡片加入 `xuanZhongIds`
- 复盘总结包含四段式字段标题（对象类型/用户表现/关键转折点/改进建议）→ `fuPanZongJieFenKuai` computed 返回非 null，按 `.fupan-zongjie-fenkuai` 分块渲染
- 复盘总结为自由文本（无四段式字段标题）→ `fuPanZongJieFenKuai` computed 返回 null，v-else 走 `.fupan-zongjie-neirong` 整体显示，不丢失任何文本
- 输入秘籍 `whosyourdaddy` → 游戏档案 `是否秘籍通关`=TRUE、`秘籍前好感度`=秘籍前真实快照、游戏结果为 `sheng_li_ai_qing`、游戏结局记录含 `mi_ji: true`，该盘战绩出现在「已胜利」分组
- 秘籍通关的复盘 prompt → 不含 `whosyourdaddy` 及其后的消息（已截断），含【秘籍通关说明】，好感度轨迹使用 `秘籍前好感度` 快照
- 秘籍通关的复盘内容 → 结尾含「仅针对秘籍使用前的真实表现」
- 渣型角色的复盘 prompt → 含【渣点提示（必填）】要求逐条引用用户消息原文点出渣型特质
- 渣型角色的复盘内容 → 前置「⚠️【渣型警示】⚠️」块，正常角色复盘不含该块

---

### FP-14 积分系统（暂时搁置）

**依赖**：FP-11
**涉及文件**：无（当前不实现）

#### 规则（FP-14 积分系统）

1. **积分系统（暂时搁置）**：
   - 未来规划：攻略角色100分、识破渣男/渣女80分、互删胜利80分、每日活跃5分
   - 当前不实现积分记录、`积分变动`表、排行榜UI/API、积分公开/隐藏开关、积分管理功能
   - 保留设计占位，待后续版本启用

#### 可机器验证验收标准（FP-14 积分系统）

- 源码搜索`积分变动`、`排行榜`、`积分服务`、`积分总数` → 无实现（已搁置）
- 游戏结束 → 不写入任何积分相关表或调用积分服务
- 管理员后台 → 无"修改用户积分"功能

---

### FP-15 过往战绩禁止暴露敏感信息

**依赖**：无（FP-13 过往战绩与复盘的子专项）
**涉及文件**：`战绩`服务、`战绩`路由、`军师`服务、`军师`路由、前端`过往战绩`组件、前端`军师记录详情`组件、相关类型定义与测试

#### 规则（FP-15 过往战绩禁止暴露敏感信息）

1. **战绩敏感字段定义**：
   - 用户侧禁止暴露：真实姓名、好感度数值、四维分数（信任度/亲密度/趣味度/关怀度）、总分、关系阶段、进度条、百分比、用户ID 等一切可用于反推具体数值或内部状态的数据。
   - 以上字段仅在 AI 内部计算、军师 Prompt、管理员后台、服务端日志等内部场景使用，不得通过普通用户 API 返回前端。

2. **战绩列表接口**：
   - 返回对象微信昵称（`角色.微信昵称`），不得返回真实姓名或 `游戏档案.角色名字` 作为默认展示名；当 `微信昵称` 为空时，使用翻译键 `zhanJi.weiZhiWeiXin`（显示为"未知微信"）兜底，禁止回退到真实姓名。
   - 返回字段仅包括：`id`、`jiao_se_id`、`jiao_se_ming_zi`、`shi_fou_zha_xing`、`jie_guo_lei_xing`、`jie_guo_lei_xing_yuan`、`shi_fou_feng_cun`、`liao_tian_tian_shu`、`xiao_xi_zong_shu`、`chuang_jian_shi_jian`、`zui_hou_xiao_xi_shi_jian`、`you_xi_jie_shu_shi_jian`、`mbti_lei_xing`。
   - 不得返回：`yong_hu_id`、`hao_gan_du_zong_fen`、`guan_xi_jie_duan` 等敏感字段。

3. **战绩详情/复盘接口**：
   - 在列表接口可返回字段基础上，额外返回 `fu_pan_shu_ju`、`fu_pan_nei_rong`。
   - `fu_pan_shu_ju` 中的时间线条目不得包含 `hao_gan_du_bian_hua` 及其子字段（四维变化、总分变化、关系阶段）。
   - 复盘 AI Prompt 必须明确要求"不得出现具体分数、维度名、阶段名或好感度字样"。

4. **军师指导记录接口**：
   - 军师 Prompt 内部可注入好感度四维分数、复盘条目、AI 内心活动用于生成建议。
   - 返回给前端的军师记录不得包含 `hou_tai_shu_ju`、`hao_gan_du_kuai_zhao` 或任何具体分数/维度名。
   - 军师响应内容本身也不得向用户透露具体分数、维度名或后台规则。

5. **前端渲染要求**：
   - 过往战绩列表、详情弹窗、复盘弹窗中不得显示好感度数值、分数、百分比、进度条、真实姓名。
   - 军师指导面板、军师记录详情页不得显示具体分数、维度名、AI 内心活动评分变化。
   - 所有用户可见文本必须走翻译文件，禁止硬编码。

#### 可机器验证验收标准（FP-15 过往战绩禁止暴露敏感信息）

- 战绩列表 API 响应中每条记录不包含：`yong_hu_id`、`hao_gan_du_zong_fen`、`guan_xi_jie_duan`、真实姓名字段。
- 战绩列表 API 中 `jiao_se_ming_zi` 优先为 `角色.微信昵称`；当微信昵称为空时，返回 `zhanJi.weiZhiWeiXin` 翻译值，不得回退到真实姓名。
- 战绩详情 API 响应中不包含上述敏感字段。
- 战绩复盘 API 响应中 `fu_pan_shi_jian_xian` 条目不包含 `hao_gan_du_bian_hua` 及其子字段。
- 军师指导记录 API 响应中每条记录不包含 `hou_tai_shu_ju`、`hao_gan_du_kuai_zhao` 及具体分数/维度名。
- 前端过往战绩列表/详情/复盘页面渲染文本中不包含好感度数值、分数、百分比、进度条、真实姓名。
- 前端军师指导/记录详情页面渲染文本中不包含具体分数、维度名（信任度/亲密度/趣味度/关怀度）。
- 相关单元测试/E2E 测试覆盖上述所有不暴露场景且全部通过。
- 前端 `npm run test` 与 `npm run build` 无错误；后端 `npm run test` 与 `npm run build` 无错误。

---

### FP-16 通知系统

**依赖**：FP-01
**涉及文件**：通知服务、Socket.IO `通知新` 事件、通知组件

#### 规则（FP-16 通知系统）

1. **用户通知**：
   - 获取我的通知（最多100条）+ 未读数
   - 标记单条已读
   - 标记全部已读
   - Socket.IO实时推送 `通知新` 事件
   - 未读通知高亮（粉色边框+背景）
   - 时间格式化：今天显示HH:MM，其他显示M/D HH:MM

2. **管理员通知**：
   - 发送通知（支持全员/指定用户）
   - 标题≤100字，正文≤2000字
   - Socket.IO实时推送
   - 管理员发送通知必须写入审计日志

#### 可机器验证验收标准（FP-16 通知系统）

- 通知列表API → 返回最多100条通知
- 通知列表API → 返回`未读数`字段
- 标记单条已读 → 该通知`已读`=true
- 标记全部已读 → 所有未读通知`已读`=true
- 新通知到达 → 前端收到Socket.IO `通知新` 事件
- 未读通知渲染 → CSS包含粉色边框样式（`border-color`匹配粉色色值）
- 通知时间为今天 → 显示格式为`HH:MM`
- 通知时间为昨天 → 显示格式为`M/D HH:MM`
- 管理员发送全员通知 → 所有用户收到Socket.IO `通知新` 事件
- 管理员发送指定用户通知 → 仅指定用户收到Socket.IO `通知新` 事件
- 通知标题超100字 → 返回400
- 通知正文超2000字 → 返回400
- 管理员发送通知 → `审计日志`表新增记录

---

### FP-17 管理员后台

**依赖**：FP-01
**涉及文件**：管理员路由、`验证管理员` 中间件、夺舍服务、开发者功能服务

#### 规则（FP-17 管理员后台）

1. **用户管理**：
   - 用户列表（含角色数、消息数）
   - 对话列表（含好感度阶段、总分、夺舍状态）
   - 对话详情（角色+用户+消息200条+好感度+记忆20条）
   - 角色信息（含记忆30条按重要度排序、夺舍状态）

2. **夺舍功能**：
   - 管理员可夺舍AI角色（Redis `夺舍:` 标记）
   - 夺舍后**AI停止自动回复**；用户发送的消息**立即转发**给管理员Socket，不再执行10秒延迟等待
   - 管理员编写回复时，前端显示"对方正在输入..."（通过Socket.IO `对方正在输入` 事件或等效机制）
   - 管理员可归还角色（仅本人可归还），归还后AI恢复自动回复
   - 夺舍操作记录 `夺舍日志` 日志

3. **开发者功能**（通过管理员JWT认证，废弃x-admin-token）：
   - 创建测试用户
   - 登录测试用户（直接发JWT令牌）
   - 系统状态（用户/角色/消息总数、今日新增、Redis键数、最近20条审计日志）
   - 删除用户（不能删自己）

4. **审计日志**：
   - 管理员操作审计必须统一（合并到 `审计日志` 一张表，不分散到多张表）
   - 监控路径：登录/注册/改密/夺舍/归还
   - 发送通知操作必须写入审计日志
   - 登录失败事件必须记录审计日志
   - 记录：用户ID、ip、事件类型、详情

5. **管理员实时监控图**（FP-02）：
   - **触发方式**：管理员账号在聊天输入框输入**且仅有输入** `greedisgood` 并发送时，不发出聊天消息，而是打开「管理员实时监控面板」（指令字符串已抽离为命名常量 `管理员调试指令`，禁止硬编码字面量）
   - **非管理员**：非管理员账号输入 `greedisgood` 并发送 → 无任何效果，仅清空输入框（不打开面板、不发送消息）
   - **面板形态**：监控面板为**可拖动浮窗**——默认停靠视口右下角，无全屏遮罩、不使背景变暗、不拦截聊天页交互（浮窗打开时聊天页可正常点击/滚动/输入）；按住标题栏可在视口内任意拖动（标题栏 `user-select:none`、文本不被选中）；浮窗尺寸与层级由 CSS 变量配置
   - **面板实时数据**（通过 Socket.IO 事件推送）：
     - `管理员_构建过程`：实时展示 AI 构建回复的思考过程，阶段含 `思考启动` / `策略规划` / `输出回复`
     - `管理员_好感度变化`：实时展示好感度的实时变化，负载为四维结构 `{ 变化: { xin_ren_du, qin_mi_du, qu_wei_du, guan_huai_du }, 时间 }`（信任度/亲密度/趣味度/关怀度本次变化量，number，可正可负），面板按四维分别展示变化量
     - `管理员_隐藏信息`：实时展示被隐藏的信息，类型含 `AI撤回` / `用户撤回` / `好感度评判理由`（内容为 AI 评判本轮好感度变化的理由文本 li_you，仅在评判结果携带理由时发出）
     - 实时事件日志：汇总上述事件的时间线日志

#### 可机器验证验收标准（FP-17 管理员后台）

- 非管理员访问管理员路由 → 返回403
- 用户列表API → 每条记录包含`角色数`和`消息数`字段
- 对话列表API → 每条记录包含`阶段`、`总分`、`夺舍状态`字段
- 对话详情API → 返回角色信息+用户信息+最多200条消息+好感度+最多20条记忆
- 角色信息API → 返回最多30条记忆且按`重要度`倒序排列
- 管理员夺舍角色 → Redis存在`夺舍:{角色ID}`键
- 夺舍后用户发送消息 → 管理员Socket立即收到消息转发（无10秒延迟）
- 管理员开始编写回复 → 用户前端收到`对方正在输入`事件或等效通知
- 非夺舍管理员尝试归还角色 → 返回403
- 夺舍操作 → `夺舍日志`表新增记录
- 创建测试用户 → `用户`表新增记录且`测试`=true
- 登录测试用户 → 直接返回JWT令牌（无需密码）
- 系统状态API → 返回用户数、角色数、消息数、今日新增、Redis键数、最近20条审计日志（不含积分相关字段）
- 删除自己 → 返回400（不能删自己）
- 所有管理员操作 → `审计日志`表新增记录（含`用户ID`、`IP`、`事件类型`、`详情`字段）
- 源码搜索"x-admin-token" → 无匹配（已废弃，全用JWT）
- 管理员在聊天输入框输入且仅有输入 `greedisgood` 并发送 → 不发送聊天消息，打开「管理员实时监控面板」
- 非管理员输入 `greedisgood` 并发送 → 无效果，仅清空输入框（不打开面板）
- 管理员实时监控面板 → 通过 Socket.IO `管理员_构建过程` 事件实时展示 AI 构建回复过程（阶段含 `思考启动` / `策略规划` / `输出回复`）
- 管理员实时监控面板 → 通过 Socket.IO `管理员_好感度变化` 事件实时展示好感度变化（四维：`xin_ren_du` / `qin_mi_du` / `qu_wei_du` / `guan_huai_du` 分别展示变化量）
- 管理员实时监控面板 → 通过 Socket.IO `管理员_隐藏信息` 事件实时展示被隐藏信息（类型含 `AI撤回` / `用户撤回` / `好感度评判理由`）
- 管理员实时监控面板 → 展示实时事件日志
- 管理员实时监控面板 → 形态为可拖动浮窗：默认停靠视口右下角、无全屏遮罩、打开时不拦截聊天页交互（浮窗外区域可点击/滚动/输入）
- 管理员实时监控面板 → 按住标题栏可拖动（自研 pointerdown/pointermove/pointerup + `setPointerCapture`，拖动时限制在视口内不丢失、标题栏 `user-select:none` 文本不被选中）
- 管理员实时监控面板 → 尺寸与层级由 CSS 变量配置：`--jiankong-kuan:420px`、`--jiankong-gao:55vh`、`--jiankong-z-index:1100`（z-index 高于聊天正文、低于其他弹层）

---

### FP-18 安全与合规

**依赖**：FP-01
**涉及文件**：JWT中间件、速率限制中间件、SQL注入检测中间件、IP封禁服务、内容安全服务、Helmet配置、输入验证

#### 规则（FP-18 安全与合规）

0. **用户体验优先原则**：
   - 所有安全与合规措施必须以不影响用户正常游戏体验为前提
   - 安全拦截（如限流、IP封禁、SQL注入检测、内容安全）必须提供清晰的翻译文件错误提示，不得返回技术堆栈或内部实现细节
   - 正常聊天、角色互动、军师指导等核心功能不能被安全规则误伤；出现误拦截时必须可配置白名单或快速调整规则

1. **认证安全**：
   - 禁止硬编码默认管理员令牌（不硬编码 `admin_secret_token_2024` 之类）
   - **废弃x-admin-token**：所有安全路由统一使用JWT体系，不再使用独立的 `x-admin-token`
   - JWT中间件必须全局强制（不逐路由挂载，避免新增路由遗漏认证）
   - 退出登录必须清除所有本地敏感数据

2. **速率限制**（三维度联合：IP + 用户 + 操作类型）：

| 限流器 | 窗口 | 最大请求 |
| --- | --- | --- |
| 常规 | 60秒 | 100 |
| 登录 | 60秒 | 5 |
| 短信 | 60秒 | 1 |
| 聊天 | 60秒 | 30 |
| 管理 | 60秒 | 10 |
| AI请求 | 60秒 | 15 |

3. **SQL注入防护**：
   - 全局中间件检测body/query/params
   - 递归检测对象嵌套
   - **非聊天输入**：检测SQL注入结构（如SELECT...FROM、UNION...SELECT、OR 1=1、INSERT...INTO、DROP TABLE等完整模式），不检测单个SQL关键词
   - **聊天输入**：仅检测高危SQL注入结构（`UNION SELECT`、`DROP TABLE`、`INSERT INTO`、`DELETE FROM`、`UPDATE SET`、`OR 1=1`、`EXEC`、`INFORMATION_SCHEMA`、`SLEEP(`、`BENCHMARK(`、`LOAD_FILE`、`INTO OUTFILE`、`CHAR(`、`CONCAT(`），普通SQL片段（如`SELECT * FROM`）不拦截
   - 输入清理：移除`<>`字符

4. **IP封禁**：
   - 违规计数（Redis `违规:` 前缀，30天TTL）
   - 阶梯处罚：1次警告→2次封1小时→3次封24小时→4次以上永久封禁
   - Redis临时封禁 + 数据库持久化双写
   - 数据库持久化存单独 `封禁记录` 表（不与审计日志混合）

5. **内容安全**：
   - AI判断违规：人身攻击/性别歧视/种族歧视/性骚扰/死亡威胁
   - 确信度>0.8才算违规
   - 输出严重程度（轻微/中等/严重）

6. **安全头**：
   - Helmet配置：CSP、COOP、CORP、referrerPolicy
   - 必须添加 `Referrer-Policy: strict-origin-when-cross-origin`
   - **不添加 `X-XSS-Protection`**：helmet v4+已弃用，现代浏览器不支持，CSP已足够防护XSS
   - Nginx配置：X-Frame-Options、X-Content-Type-Options、Referrer-Policy

7. **输入验证**：
   - 手机号：`^1[3-9]\d{9}$`
   - 用户名：1-30字符，无特殊符号
   - 聊天内容：≤500字符；高危SQL注入结构拦截，普通SQL片段允许
   - 密码：非空
   - 性别：男/女

#### 可机器验证验收标准（FP-18 安全与合规）

- 所有安全拦截错误响应 → 消息来源为翻译文件，不包含SQL语句、堆栈跟踪、内部参数名
- 正常聊天消息（无高危SQL结构） → 不触发安全拦截，可正常发送
- 源码搜索"admin_secret_token" → 无硬编码匹配
- 源码搜索"x-admin-token" → 无匹配（已废弃，全用JWT）
- 新增路由未挂载JWT → 返回401（全局中间件生效）
- 60秒内发送101次常规请求 → 第101次返回429
- 60秒内6次登录失败 → 第6次返回429
- 60秒内2次短信请求 → 第2次返回429
- 60秒内31次聊天 → 第31次返回429
- 60秒内11次管理操作 → 第11次返回429
- 60秒内16次AI请求 → 第16次返回429
- 非聊天请求body含`SELECT * FROM` → 返回403
- 聊天请求body含`SELECT * FROM` → 正常通过（普通SQL片段不拦截）
- 请求body含高危SQL注入结构（如`OR 1=1`、`UNION SELECT`、`DROP TABLE`） → 返回403
- 请求body含`<script>` → 字符被清理为`script`
- 聊天内容含单个SQL单词如`select` → 不拦截（正常通过）
- 第一次违规 → 返回警告且不封禁
- 第二次违规 → IP封禁1小时
- 第三次违规 → IP封禁24小时
- 第四次违规 → IP永久封禁
- IP封禁 → Redis存在`违规:`键且`封禁记录`表存在持久化记录
- AI内容安全检测确信度=0.9且类型=人身攻击 → 判定为违规
- AI内容安全检测确信度=0.7 → 不判定为违规
- 响应头 → 包含`Referrer-Policy: strict-origin-when-cross-origin`
- 响应头 → 包含`Content-Security-Policy`、`Cross-Origin-Opener-Policy`、`Cross-Origin-Resource-Policy`
- 响应头 → 不包含`X-XSS-Protection`（已弃用，不添加）
- 手机号=`12345` → 返回400
- 用户名=`a!@#` → 返回400
- 聊天内容超500字符 → 返回400
- 性别=`other` → 返回400

---

### FP-19 主题与UI

**依赖**：FP-01
**涉及文件**：主题配置`主题.ts`、CSS变量定义、全局菜单栏组件、响应式样式

#### 规则（FP-19 主题与UI）

1. **主题系统**：
   - 主题默认值必须为深色模式（`主题.ts` 默认值为 `暗色`）
   - 深色/浅色切换
   - 主题存储于 `localStorage.主题`
   - 通过 `data-theme` 属性驱动CSS变量切换
   - 200+CSS变量定义

2. **品牌色**：
   - 主色：微信绿（深色#34C759/浅色#07C160）
   - 品牌色：暖灰蓝 #6B8CA6 + 柔粉紫 #C4A0B0
   - 背景渐变：4色渐变 + 12秒流动动画
   - 玻璃态设计：`backdrop-filter: blur(12px)`

3. **全局固定菜单栏**：
   - 顶部菜单栏在所有页面永远显示（登录/注册/协议/主页/聊天等所有页面）
   - 菜单栏高度52px（移动端48px），所有元素位置固定
   - 顶栏采用固定三栏网格布局（左/中/右固定尺寸插槽，`grid-template-columns: 1fr 1fr 1fr`）；无论哪个页面、无论左侧按钮是否显示，中间标题与左右两侧元素位置绝对不变。主页路由下“主页”按钮以 `visibility: hidden` 占位保留在左槽（而非移除 DOM），以此保持左槽宽度，避免右侧“个人资料”等元素因左槽塌缩而挤位；禁止用 `margin` 微调等表面补丁
   - 主页只显示居中的页面标题“主页”，不再渲染可点击的“返回主页”按钮（功能冗余且会与标题重复）
   - 所有元素根据当前状态显示/隐藏，隐藏时位置空出（不塌缩）
   - 退出登录必须红色高亮（默认状态就红色，不仅hover时变红）
   - 版本号(1.0.0)直接可见（不需点击弹窗才能看到）
   - 所有按钮位置不可重叠，需优化布局使其美观
   - 用户下拉菜单中「账号设置」为可点击/悬停激活的二级菜单头部，激活后其三级子项（修改用户名/修改密码/设置默认性别/退出登录）以**独立飞出列出现在二级菜单右侧**（cascade 飞出，现代化 UI：桌面 hover 或键盘右方向键/点击激活，移动端在二级菜单下方全宽展开；箭头▶指示向右飞出）；四项中退出登录保持红色高亮；「设置默认性别」打开弹窗可选男/女，该值仅作为普通模式资料向导第一步（用户自身性别）的系统默认预选项，不覆盖用户手动选择，也不参与任何角色/对象性别判定；「过往战绩」入口与「账号设置」同级保留在用户下拉菜单中（不在「更多」菜单），保持 FP-13 可达性

   **菜单栏元素布局**：

   | 位置 | 元素 | 显示条件 | 行为 |
   | --- | --- | --- | --- |
   | 左侧 | 返回按钮 | 非主页且非登录/注册页时显示 | 点击返回浏览器历史上一页（`history.back()`） |
   | 左侧 | 主页按钮 | 非主页且非登录/注册路由时显示；主页路由（`zhuJieMian`）下隐藏为占位（`.yincang` → `visibility: hidden`），保留在 DOM 流中维持左槽宽度、避免右侧“个人资料”等元素挤位，不再使用绝对居中（`zhong_xin` 已移除） | 点击跳转主页（`/`） |
   | 左侧 | 用户下拉菜单（账号设置〔激活后以右侧独立飞出列展示：修改用户名/修改密码/设置默认性别/退出登录〕；「过往战绩」与账号设置同级） | 已登录时显示，未登录时隐藏（位置空出） | — |
   | 中间 | 角色名 | 仅聊天页显示 | — |
   | 中间 | 页面标题 | 所有非聊天页（含主页）显示并水平居中；主页页（`zhuJieMian`）下显示“主页”，承担导航标识 | — |
   | 右侧 | 军师指导按钮 | 仅聊天页显示，位于深色/浅色模式切换按钮**左边**（FP-03） | 点击派发 `junshi-zhankai` 自定义事件展开军师面板 |
   | 右侧 | 深色/浅色模式切换按钮 | 时刻显示，位于军师指导按钮**右边** | — |
   | 右侧 | 通知按钮 | 已登录时显示 | — |
   | 右侧 | 更多菜单（☰）仅含用户协议/隐私政策两项 | 时刻显示 | — |

4. **响应式设计**：
   - 移动端优先
   - 安全区域适配（`env(safe-area-inset)`）
   - `prefers-reduced-motion` 适配

5. **滚动条统一样式**：
   - 所有需要纵向滚动的区域（聊天消息列表、emoji选择器、过往战绩列表、复盘弹窗内容区、军师指导面板内容区、军师记录详情页面容器等）均使用统一滚动条样式
   - 滚动条宽度 6px，滑块圆角 3px，轨道透明
   - 滑块颜色使用 CSS 变量 `--gundong-tiao-beijing`（深色/浅色主题分别定义）
   - 滑块 hover 状态使用 `--gundong-tiao-hover`
   - 同时声明 Firefox 的 `scrollbar-width: thin` 与 `scrollbar-color`
   - 注意：聊天消息区（`.xiaoxi-quyu`）为清晰可见，使用独立颜色变量 `--liaotian-gundong-tiao`（对比明显，避免沿用近乎不可见的 `--gundong-tiao-beijing`），且**不**声明标准 `scrollbar-width`/`scrollbar-color`（否则会覆盖 WebKit 自定义样式，导致自定义滚动条失效）
   - 滚动条样式不得影响页面布局与交互

#### 可机器验证验收标准（FP-19 主题与UI）

- `主题.ts`默认值 → 为`暗色`
- 首次访问（无`localStorage.主题`）→ `document.documentElement`的`data-theme`属性为`dark`
- 切换主题 → `localStorage.主题`更新且`data-theme`属性切换
- CSS文件 → 包含200+CSS变量定义（`--`前缀）
- 深色模式主色 → `#34C759`
- 浅色模式主色 → `#07C160`
- 背景CSS → 包含`linear-gradient`且`animation`时长12秒
- 玻璃态元素CSS → `backdrop-filter`包含`blur(12px)`
- 所有页面渲染 → 顶部菜单栏可见（高度52px桌面端/48px移动端）
- 登录页渲染 → 菜单栏可见
- 聊天页渲染 → 菜单栏可见且中间显示角色名
- 主页渲染 → 菜单栏可见，中间页面标题居中显示“主页”；主页按钮（`.zhuye-anniu`）为隐藏占位（`.yincang` → `visibility: hidden`，保留在 DOM 维持左槽宽度），不渲染可点击按钮；中间标题在 homepage 下显示
- 非主页且非登录/注册路由渲染 → 主页按钮（`.zhuye-anniu`）正常显示（非 `yincang`）；主页路由下主页按钮为隐藏占位
- 任意两个页面（如主页与资料设置页）渲染 → 顶栏左/中/右三栏结构一致，左槽“个人资料”区（`.yonghu-xuanxiang`）与中间标题的水平位置跨页面固定不变
- 未登录状态 → 用户下拉菜单隐藏且位置空出（不塌缩）
- 退出登录按钮 → 默认状态CSS`color`为红色（非仅hover时变红）
- 菜单栏 → 版本号"1.0.0"文本直接可见（不需点击）
- 聊天页 → 军师指导按钮在菜单栏右侧可见
- 非聊天页 → 军师指导按钮不存在或`display:none`
- 聊天页菜单栏 DOM 顺序 → 军师指导按钮（`.junshi-anniu`）位于深色/浅色模式切换按钮（`.zhuti-qiehuan-anniu`）**之前**（即视觉上位于其左边，FP-03）
- 已登录 → 通知按钮可见
- 未登录 → 通知按钮不存在或`display:none`
- 移动端视口（375px宽度）→ 菜单栏高度为48px
- CSS → 包含`env(safe-area-inset)`适配
- CSS → 包含`@media (prefers-reduced-motion: reduce)`媒体查询
- 聊天页面 `.emoji-mianban` 存在统一滚动条样式（`::-webkit-scrollbar` 宽度 6px、滑块背景 `--gundong-tiao-beijing`、hover 状态 `--gundong-tiao-hover`）；聊天消息区 `.xiaoxi-quyu` 使用**独立可见**滚动条颜色 `--liaotian-gundong-tiao`（对比明显，避免沿用近乎不可见的 `--gundong-tiao-beijing`），`::-webkit-scrollbar` 宽度 8px、滑块圆角 4px、hover 状态 `--liaotian-gundong-tiao-hover`；该区域不声明标准 `scrollbar-width`/`scrollbar-color`（避免覆盖 WebKit 自定义样式）
- 过往战绩 `.zhanji-liebiao` 与 `.fupan-neirong` 存在统一滚动条样式
- 军师指导 `.junshi-neirong` 存在统一滚动条样式
- 军师记录详情 `.junshi-jilu-yemian` 存在统一滚动条样式
- 滚动条相关颜色全部来自 CSS 变量，无硬编码色值
- 新增/更新对应单元测试验证上述滚动容器样式

---

### FP-20 多媒体消息

**依赖**：FP-03, FP-05, FP-06
**涉及文件**：媒体存储服务（CAS）、上传下载路由、数据库迁移（`媒体文件` 表）、消息服务类型扩展、聊天页面组件、消息气泡组件、"+"面板与表情包面板、语音录制组件、图片压缩工具、前后端翻译文件

#### 规则（FP-20 多媒体消息）

1. **消息类型扩展**：消息."类型"字段 ∈ {`wenben`(默认), `tuPian`, `biaoQingBao`, `yuYin`, `wenJian`}；媒体消息的"媒体ID"列关联"媒体文件"表；文本内容对媒体消息为空字符串
2. **内容寻址存储（CAS）**：上传时流式计算 SHA256，磁盘路径 `backend/uploads/media/<sha256前2位>/<sha256>`，同哈希文件全库唯一存储（去重）；"媒体文件"表含 SHA256(唯一)、原始文件名、MIME、大小字节、类别(`tupian`/`biaoqingshu`/`yuyin`/`wenjian`)、宽、高、时长毫秒、上传者ID、创建时间
3. **上传API**：`POST /api/聊天/会话/:huiHuaId/媒体`，multipart field=file，busboy 流式处理边落盘边算哈希（禁止全量载入内存）；**下载API**：`GET /api/媒体/:sha256?e=<过期秒级时间戳>&s=<HMAC-SHA256签名>`（密钥复用 JWT_SECRET），校验失败403；响应带 ETag=sha256 + Cache-Control: private, max-age=31536000, immutable，支持 Range（音频播放需要）
4. **白名单（常量可配置）**：图片≤10MB(jpeg/png/gif/webp)、音频≤10MB(webm/ogg/mp3/wav/m4a/aac)、文件≤50MB(pdf/txt/doc/docx/xls/xlsx/ppt/pptx/mov/mp4/zip/rar/7z)；超限或非法MIME返回翻译文件错误消息
5. **前端压缩与表情包**：前端发送图片前 canvas 压缩（长边≤1280px、质量0.8）；表情包面板内置≥12个贴纸（emoji大字+文字标签，SVG渲染canvas导出512×512 PNG），点击即经压缩上传通道发送为 `biaoQingBao` 类型，气泡透明背景直接显示大图
6. **发送图片/文件入口与气泡**：输入栏"+"按钮展开面板提供"相册"（选图）与"文件"入口；图片气泡点击进入全屏预览（可关闭）；文件气泡显示图标+文件名+大小+下载按钮；下载走签名URL
7. **语音条**：底部语音按钮点击切换"按住说话"模式；按住录音（MediaRecorder audio/webm;codecs=opus），松开发送、上滑取消；时长上限60秒（超时自动截断为60秒发送）；语音条气泡显示伪波形动画+时长，点击播放/暂停（Audio元素）
8. **AI视觉理解**：用户发的图片/表情包以 base64 data URL 形式作为 `{type:'input_image'}` 内容块注入 user 消息（Responses API 官方格式），deepseek-v4-flash-vision-exp 直接看图回复；system/assistant 消息禁止携带图片（官方限制）；每张图 token 预算按384计
9. **非文本消息文本化描述**：军师/复盘/表白检测等所有AI链路对非文本消息使用文本化描述：[图片]、[表情包]、[语音(12秒)]、[文件:名字.pdf]；检测类调用（表白/互删/识破/神经病）在最新消息为图片时附带图像内容块
10. **撤回与实时性**：媒体消息同样支持2分钟内撤回（撤回语义与文本一致）；乐观更新、Socket实时推送、分页加载对所有类型生效
11. **翻译文件**：所有新增用户可见文本必须走翻译文件且禁止硬编码——前端 UI 文案键入 frontend/src/config/translations.ts，后端错误消息键入 backend/src/config/translations.ts（各自域各自维护，不做无意义全量同步）

#### 可机器验证验收标准（FP-20 多媒体消息）

- 上传10MB内jpeg → 返回200且响应含 mediaId/sha256；磁盘出现 CAS 路径文件
- 上传同一文件两次 → 数据库"媒体文件"仅1条记录（去重生效）
- 上传21MB图片 → 返回400且错误消息来自翻译文件
- 上传 .exe 文件 → 返回400
- GET /api/媒体/:sha256 不带签名 → 403；带过期时间戳 → 403；带合法签名 → 200且ETag等于sha256且Cache-Control含immutable
- 对下载URL发Range请求 → 返回206部分内容
- 发送 tuPian 消息（带mediaId）→ 消息表记录类型=tuPian且媒体ID非空；前端气泡显示图片
- 发送 biaoQingBao → 气泡透明背景显示贴纸图
- 发送 yuYin → 气泡含波形动画元素与时长文本；点击后 Audio 播放状态切换
- 发送 wenJian → 气泡含文件名、大小文本、下载链接（签名URL）
- 用户发送图片后AI回复 → DeepSeek请求的input数组最后一条user消息content为数组且含 input_image 块（data:image/开头）
- 表白检测调用且最新消息为图片 → 请求含图像内容块
- 军师Prompt含历史图片消息 → 出现"[图片]"描述文本
- 复盘Prompt含历史图片消息 → 出现"[图片]"描述文本
- 媒体消息撤回 → 前端显示"你撤回了一条消息"，数据库原始内容保留
- 录音超60秒 → 自动截断为60秒发送
- 上滑取消录音 → 不产生消息
- 全部新文案键存在于所属域的翻译文件（前端 UI 键在 frontend/src/config/translations.ts；后端错误消息键在 backend/src/config/translations.ts）

---

### FP-21 语音视频通话

**依赖**：FP-05, FP-06
**涉及文件**：数据库迁移（`通话记录` 表）、通话信令Socket服务、通话状态机服务、语音通话界面组件、视频通话界面组件、铃声合成模块、"+"面板通话入口、前后端翻译文件

#### 规则（FP-21 语音视频通话）

1. **通话记录表**：数据库"通话记录"表：ID UUID PK、"用户ID"、"角色ID"、"类型"(`yuYin`|`shiPin`)、"状态"(`yiJieTong`|`yiQuXiao`|`yiJuJie`|`yiChaoShi`)、"接通时间"、"结束时间"、"时长秒"、"创建时间"
2. **信令事件**：Socket.IO信令事件（中文事件名）：`通话邀请`(负载: tongHuaId/leiXing/jiaoSeId)、`通话接受`、`通话拒绝`、`通话取消`、`通话挂断`、`通话超时`；服务端维护通话会话状态机：空闲→振铃→接通→进行中→结束，非法跃迁返回错误事件
3. **AI模拟行为（暂不接入语音/视频大模型）**：收到邀请后延迟2~6秒随机自动接听；通话硬上限600秒自动结束；挂断/结束后写入"通话记录"并向聊天插入系统消息（发送者='xitong'）：未接通为"[语音通话] 已取消"/"[视频通话] 已取消"，接通为"[语音通话] 时长 MM:SS"/"[视频通话] 时长 MM:SS"
4. **语音通话界面**：全屏遮罩层，角色头像+昵称+状态文本（等待对方接受.../已连接/已结束）+呼吸光圈动画+计时器+挂断按钮（红色圆形）；来电弹窗（被叫场景预留，本版本AI不会呼叫用户）
5. **视频通话界面**：本地 getUserMedia({video,audio}) 回显小窗（可拖动）+远端区显示角色头像与光效占位（媒体层暂无AI视频流）+控制栏（挂断/摄像头开关/麦克风开关）
6. **铃声**：Web Audio API 合成提示音（无需外部音频文件），振铃期循环、接通/取消即停
7. **通话入口**：聊天页"+"面板提供"语音通话""视频通话"两个入口按钮
8. **翻译文件**：所有文案走翻译文件，禁止硬编码

#### 可机器验证验收标准（FP-21 语音视频通话）

- 管理员数据库查询 → "通话记录"表存在且字段齐全
- 用户发起语音通话 → Socket发出`通话邀请`；2~6秒内收到`通话接受`
- 接通后计时器递增 → UI显示MM:SS
- 用户挂断 → 收到`通话挂断`；"通话记录"新增状态=yiJieTong记录且时长秒=实际值；聊天出现系统消息"[语音通话] 时长 MM:SS"
- 振铃期取消 → 记录状态=yiQuXiao；系统消息"已取消"
- 通话达600秒 → 服务端自动`通话超时`结束并落库
- 视频通话 → 本地video元素存在且getUserMedia被调用；挂断后轨道停止
- 非法状态跃迁（如未邀请直接挂断）→ 服务端返回错误事件不崩溃
- 翻译键齐全 → 前端通话 UI 文案键全部存在于 frontend/src/config/translations.ts；通话相关后端错误消息键全部存在于 backend/src/config/translations.ts

---

## 六、非功能需求

### 6.1 性能

| 指标 | 要求 |
| --- | --- |
| API响应时间 | 普通接口 < 500ms |
| AI回复延迟 | 用户发消息后10秒触发（可接受） |
| 消息推送延迟 | Socket.IO < 1秒 |
| 前端首屏加载 | < 3秒 |
| 数据库查询 | 单表查询 < 100ms |
| 媒体上传接口 | 10MB内文件 < 2秒返回（流式零内存峰值） |
| 媒体重复加载 | immutable缓存命中后零网络请求 |

### 6.2 可用性

| 指标 | 要求 |
| --- | --- |
| 服务可用性 | 99.5% |
| Docker容器 | restart: unless-stopped |
| 健康检查 | PostgreSQL/Redis 健康检查 |
| 错误处理 | 优雅处理错误，返回翻译文件错误消息 |
| 异常处理 | 非法数据不致服务器崩溃 |

### 6.3 安全

| 指标 | 要求 |
| --- | --- |
| HTTPS | 生产环境必须（Let's Encrypt） |
| JWT | 至少32字符密钥，7天过期 |
| 密码存储 | bcrypt哈希 |
| SQL注入 | 全局检测 + 参数化查询 |
| XSS | 输入清理 + CSP |
| CSRF | JWT Bearer Token（非Cookie） |
| 限流 | 三维度（IP+用户+操作类型） |
| 审计日志 | 敏感操作全记录 |

### 6.4 可维护性

| 指标 | 要求 |
| --- | --- |
| 代码规范 | 变量/函数/类名/Socket.IO事件/文件名/数据库表名/字段名/API路径全部用全中文，TypeScript强类型 |
| 翻译文件 | 用户可见文本必走翻译文件 |
| 零硬编码 | 数值/名称/路径必可配置 |
| 配置热重载 | 配置变更尽可能支持热重载 |
| 测试覆盖 | vitest单元测试 + 源码静态检查 |

### 6.5 Debug 日志

#### 规则

1. **日志目标**：结构化 debug 日志统一写入 `backend/logs/debug.log`（单文件追加，按日期轮转由外部日志收集系统处理）。
2. **日志级别**：支持 `debug` / `info` / `warn` / `error` 四级，通过环境变量 `LOG_LEVEL` 控制最低输出级别，未设置时默认为 `debug`。
3. **日志格式**：每行一条 JSON 对象，字段包括：
   - `shi_jian`：ISO 8601 时间戳
   - `ji_bie`：日志级别
   - `lei_xing`：事件类型（如 `HTTP请求`、`Socket事件`、`AI调用`）
   - `xiao_xi`：人类可读摘要
   - `yong_hu_id`：可选，关联用户 ID
   - `jiao_se_id`：可选，关联角色 ID
   - `xiang_qing`：可选，结构化上下文对象
4. **记录范围**：
   - **HTTP 请求/响应**：方法、路径、状态码、耗时（ms）、当前用户 ID；4xx 记录为 `warn`，5xx 记录为 `error`。
   - **Socket 事件**：连接、断开、加入聊天、发送消息、对方正在输入、消息撤回、游戏事件、通知事件、夺舍事件等。
   - **AI 调用**：模型类型（如 `director` / `writer` / `情感分析`）、实际模型名、耗时、成功/失败状态、错误信息。
   - **好感度变更**：用户 ID、角色 ID、四维变化值、新总分。
   - **军师指导**：请求用户/角色、成功/失败状态、错误码（如重复请求返回 `JUN_SHI_CHONG_FU`）。
   - **游戏结局**：用户 ID、角色 ID、结局类型（如 `胜利-爱情`、`失败-过早表白`）。
   - **消息操作**：用户消息发送、角色消息发送、消息撤回等，记录操作类型与消息 ID。
5. **敏感信息过滤**：日志中必须隐藏密码、验证码、JWT token、API 密钥等敏感字段。字符串中匹配 JWT 结构（`xxxxx.yyyyy.zzzzz`）时整串替换为 `***`；对象中敏感键名对应值替换为 `***`。
6. **安全要求**：日志内容不得暴露内部堆栈、SQL 语句、环境密钥；仅记录调试所需的最小上下文。
7. **链路追踪（OpenTelemetry）**：后端 OTel 仅在显式配置了 collector 地址（环境变量 `OTEL_EXPORTER_OTLP_ENDPOINT`）时使用 OTLP 导出，或在 `development` 环境下使用 Console 导出；**生产环境未配置 collector 时不初始化 SDK、不构造导出器、不开启诊断日志**。从根本上避免向不存在的 `localhost:4318` 发送 trace 导致的 `ECONNREFUSED` 日志噪音。

#### 可机器验证验收标准

- 调用 `debug日志.info` 后 → `backend/logs/debug.log` 新增一行合法 JSON。
- 设置 `LOG_LEVEL=warn` 后 → `debug`/`info` 级别日志不写入文件，`warn`/`error` 正常写入。
- 日志详情对象含 `miMa`、`yanZhengMa`、`token` 字段 → 输出值为 `***`。
- 日志消息含 JWT 字符串 → 输出值为 `***`。
- 发送任意 HTTP 请求 → 日志中可检索到对应 `HTTP请求` 类型记录，含方法/路径/状态码/耗时。
- Socket 连接/断开/加入聊天/发送消息 → 日志中可检索到对应 `Socket事件` 或 `消息操作` 类型记录。
- AI 调用后 → 日志中可检索到 `AI调用` 类型记录，含模型类型、模型名、耗时、成功状态。
- 好感度变化后 → 日志中可检索到 `好感度变更` 类型记录，含四维变化与总分。
- 军师指导请求后 → 日志中可检索到 `军师指导` 类型记录。
- 游戏结局触发后 → 日志中可检索到 `游戏结局` 类型记录，含结局类型。
- 生产环境未配置 OTel collector（`OTEL_EXPORTER_OTLP_ENDPOINT` 未设置且非 development）→ 后端日志中不存在指向 `localhost:4318` 的 `ECONNREFUSED` 链路追踪导出错误噪音（OTel 未初始化即不产生导出尝试）。

---

## 七、补充说明

### 7.1 DeepSeek 接入

- 模型ID：`deepseek-v4-flash-vision-exp`
- **接入方式**：优先使用DeepSeek官方SDK/原生端点，思考模式使用官方顶层参数`thinking:{type:'enabled'|'disabled'}`（默认enabled），推理深度使用`reasoning_effort`（low/high/max）
- **视觉能力**：用户图片/表情包以 input_image 内容块内联注入 user 消息（base64 data URL），system/assistant 禁止携带图片；每图token上限384
- API密钥：参考 `API大全.txt`
- **思考模式**：通过官方顶层参数`thinking:{type:'enabled'|'disabled'}`控制，`reasoning_effort`（low/high/max，默认high）控制思考深度；官方规定**思考模式下 temperature/top_p/presence_penalty/frequency_penalty 不生效**（忽略不报错）。当前实现：按用户要求全部场景显式设置`thinking:{type:'enabled'}`且`reasoning_effort:'max'`（最高思考强度），temperature 仅作传递值
- **角色扮演增强**：DeepSeek-V4支持角色沉浸模式，通过在第一轮消息末尾添加`【角色沉浸要求】`指令，使AI在思考过程中以角色第一人称进行内心独白，增强角色扮演沉浸感。项目全程使用此模式
- **自然度优化**：Prompt中应融入真实青年/大学生恋爱微信聊天风格，允许留白、犹豫、省略号、语气词、推拉和暧昧试探，避免AI味总结、说教和机械式表达
- 角色扮演通过system prompt + 角色沉浸指令实现
- 后端已有6层prompt架构 + Director-Writer双代理
- 推荐参数：`temperature=1.0, top_p=1.0`

### 7.2 微信昵称数据源

- 文件路径：`和我恋爱吧/微信昵称/昵称大全.txt`
- 格式：男：\n昵称1\n昵称2... \n女：\n昵称1\n昵称2...
- 需解析为男女两个数组，角色生成时按性别随机选取

### 7.3 秘籍系统

- 输入 `whosyourdaddy` 直接拉满四维好感度到1000（测试用）
- 秘籍触发时：先将秘籍前真实好感度快照写入游戏档案（`秘籍前好感度` INTEGER），并标记 `是否秘籍通关` = TRUE（UPSERT）
- 好感度拉满后复用现有胜利逻辑触发游戏结束（结果类型 `sheng_li_ai_qing`，结局记录携带 `mi_ji: true` 与秘籍前分数），该盘战绩正常进入过往战绩「已胜利」分组
- 秘籍通关的复盘规则见 FP-13：仅复盘到秘籍使用前（按消息内容定位 `whosyourdaddy` 并截断，好感度轨迹以 `秘籍前好感度` 快照为准），复盘内容结尾注明「仅针对秘籍使用前的真实表现」
- 渣型角色复盘须逐条引用具体用户消息原文点出渣型特质（渣点提示必填），复盘内容前置醒目「⚠️【渣型警示】⚠️」块，与正常对象复盘明显区分

### 7.4 代码重构原则

- 允许根据需求重构、修改、删除任何现有代码
- 所有代码必须严格遵循1.3命名规范（全中文优先，不支持中文用拼音驼峰）
- 用户可见文本必须走翻译文件，禁止硬编码
- 数值/名称/路径必须可配置，支持热重载
- 当现有代码与需求冲突时，按需求重写

### 7.5 数据库中文脚本

- **从零重写**：删除旧的拼音迁移文件（001-020.sql），从零开始用中文表名/字段名编写新的数据库初始化脚本
- **PostgreSQL 16**：默认UTF-8编码，完全支持中文表名/字段名
- **SQL语法**：中文标识符需用双引号包裹，如 `CREATE TABLE "用户" ("手机号" VARCHAR(11) PRIMARY KEY)`
- **ORM适配**：TypeORM/Prisma实体类属性名用中文，需测试ORM对中文标识符的兼容性
- **现有数据迁移**：由于是Loop Engineering重写项目，不需要保留现有数据，直接从零初始化

### 7.6 图片资源路径

- **统一存放**：所有图片资源统一存放于 `和我恋爱吧/图片` 文件夹中
- **目录结构**：
  - `图片/军师头像/军师玄锐暮头像.png` — 军师玄锐暮的头像
  - `图片/主页元素/吴昊阳终稿静态图.png` — 主页背景/装饰元素
- **引用方式**：代码中引用图片时使用相对路径，如 `图片/军师头像/军师玄锐暮头像.png`
- **新增图片**：后续新增图片资源必须放入 `图片` 文件夹的对应子目录中

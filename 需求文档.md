# 和我恋爱吧 — 完整需求文档（PRD）

<!-- markdownlint-disable-file MD029 -->
<!-- 说明：PRD 规则列表因表格/代码块分隔被 markdownlint 识别为新列表，但语义上需保持连续编号（如 FP-10 第7-10条、FP-17 第3-7条），故禁用 MD029 -->

> 本文档按用户旅程重组功能点，每个功能点标注依赖关系，验收标准全部转化为可机器验证的测试条件。配合"循环工程"skill使用时，一个功能点 = 一个Task子代理任务。

---

## 一、项目概述

### 1.1 项目定位

"和我恋爱吧"是一款 AI 恋爱模拟聊天 Web 应用，用户通过模拟微信聊天界面与个性化 AI 角色展开沉浸式恋爱互动。核心价值：

- **练习恋爱聊天**：在零风险环境中练习线上恋爱沟通技巧
- **AI 沉浸式角色扮演**：16种MBTI性格 + 渣男渣女变体，每个角色有独立人设、背景、微信昵称
- **军师指导**：恋爱军师实时分析聊天并给出建议
- **复盘提升**：游戏结束后AI生成复盘报告，帮助用户改进

### 1.2 核心技术栈

不列出具体的技术栈名称和版本。技术栈应随着项目的需求而不断改进。版本必须选用最新的、可用的测试版本。

### 1.3 命名规范

**核心原则：项目全部代码用全中文，包括数据库表名/字段名、API路径、文件名、Socket.IO事件名、变量/函数/类名。**

| 类别 | 规则 | 示例 |
| --- | --- | --- |
| 变量/函数/类名/接口/枚举 | **全中文** | `好感度`、`发送消息`、`聊天管理器`、`获取好感度阶段` |
| Vue组件内命名（变量/函数） | **全中文** | `const 好感度 = ref(500)`、`function 发送消息()` |
| Socket.IO事件名 | **全中文** | `角色回复`、`消息撤回`、`游戏事件`、`对方正在输入` |
| 文件名 | **全中文** | `聊天管理器.ts`、`好感度服务.ts`、`用户认证.vue` |
| 数据库表/字段 | **全中文** | `用户`表、`角色`表、`好感度`字段、`信任度`字段 |
| API路径 | **全中文** | `/api/认证/登录`、`/api/聊天/发送` |
| 用户可见文本 | 必须走翻译文件，禁止硬编码 | — |

**数据库中文标识符注意事项**：

- PostgreSQL 16 默认 UTF-8 编码，完全支持中文表名/字段名
- SQL 语句中中文标识符需用双引号包裹，如 `SELECT "好感度" FROM "用户" WHERE "手机号" = $1`
- 数据库迁移脚本从零用中文重写，删除旧的拼音迁移文件（001-020.sql）

---

## 二、范围边界

### 2.1 做什么（要实现的功能点）

实现 FP-01 ~ FP-18 共18个功能点，覆盖用户认证、游戏入口、角色生成、聊天互动、AI对话引擎、好感度系统、胜利失败条件、军师指导、战绩复盘、通知系统、管理员后台、安全合规、主题UI。

### 2.2 不做什么（范围外功能）

1. **挑战模式**：暂时搁置，主页入口显示"即将推出"，不实现具体功能
2. **图片/语音消息**：仅文本消息
3. **朋友圈功能**：暂不实现
4. **多语言国际化**：暂时仅中文，未来增加
5. **PWA离线功能增强**：当前无PWA配置
6. **管理员后台界面**：仅有API，无前端界面
7. **社交场批量生成**：代码存在但未启用
8. **积分系统与积分榜/排行榜**：暂时搁置；不实现积分记录、排行榜UI、排行榜API及积分相关管理功能

### 2.3 代码修改原则

- **允许修改一切代码**：AI可根据需求进行激进的重构、修改、删除任何现有代码，不受"增量更新"约束
- **严格按照需求文档和skill规范编写**：所有代码必须符合1.3命名规范、第六章非功能需求、第七章补充说明
- **重构优先于保留**：当现有代码与需求冲突时，按需求重写而非妥协保留

---

## 三、用户角色与权限

### 3.1 角色定义

| 角色 | 标识 | 权限 |
| --- | --- | --- |
| 普通用户 | `管理员 = false` | 注册/登录/聊天/军师/战绩/通知 |
| 管理员 | `管理员 = true`（手机号在 `ADMIN_PHONES` 环境变量中） | 普通用户权限 + 用户管理 + 夺舍角色 + 发送通知 + 系统状态 |
| 开发者 | 管理员子集（通过管理员JWT认证） | 管理员权限 + 创建测试用户 + 删除用户 |

### 3.2 认证机制

- **JWT认证**：Bearer Token，默认过期7天，存储于前端 `localStorage.令牌`
- **Socket认证**：`socket.handshake.auth.token` 携带JWT
- **管理员认证**：通过 `验证管理员` 中间件校验手机号是否在 `ADMIN_PHONES` 中
- **公开路由白名单**：检查手机号、发送验证码、注册、登录、健康检查、角色列表
- **废弃x-admin-token**：所有安全路由统一使用JWT体系，不再使用独立的 `x-admin-token`

---

## 四、项目流程与依赖关系

### 4.1 用户旅程流程

```text
注册/登录（FP-01）
    ↓
主页（FP-02）
    ↓ 选择普通模式
资料设置向导（FP-03）
    ↓ 完成
AI角色生成（FP-04）
    ↓ 跳转"添加微信"过渡页
聊天界面（FP-05）
    ↓
消息发送与显示（FP-06）←→ AI回复机制（FP-07）←→ 消息撤回（FP-08）
    ↓                                          ↑
    └→ AI对话引擎（FP-09）←→ 好感度系统（FP-10）
    ↓
胜利失败条件（FP-11）
    ↓ 游戏结束
过往战绩与复盘（FP-13）← 军师指导系统（FP-12，游戏中可用）
    ↓
积分系统（FP-14，暂时搁置）

横切关注点：
- 通知系统（FP-15）：全程可用
- 管理员后台（FP-16）：管理员可用
- 安全与合规（FP-17）：全程生效
- 主题与UI（FP-18）：全程生效
```

### 4.2 功能点依赖关系图

| 功能点 | 依赖 | 说明 |
| --- | --- | --- |
| FP-01 用户认证 | 无 | 基础模块 |
| FP-02 主页与模式入口 | FP-01 | 需登录 |
| FP-03 资料设置向导 | FP-02 | 从主页进入 |
| FP-04 AI角色生成 | FP-03 | 向导完成后触发 |
| FP-05 聊天界面 | FP-04 | 需角色生成完成 |
| FP-06 消息发送与显示 | FP-05 | 聊天界面基础 |
| FP-07 AI回复机制 | FP-06, FP-09, FP-10 | 需消息系统+AI引擎+好感度 |
| FP-08 消息撤回 | FP-06 | 基于消息系统 |
| FP-09 AI对话引擎 | FP-06 | 处理用户消息 |
| FP-10 好感度系统 | FP-09 | AI评估好感度变化 |
| FP-11 胜利失败条件 | FP-09, FP-10 | 基于好感度和表白检测 |
| FP-12 军师指导系统 | FP-06, FP-09, FP-10 | 分析聊天+AI+好感度 |
| FP-13 过往战绩与复盘 | FP-11 | 游戏结束后生成 |
| FP-14 积分系统 | FP-11 | 暂时搁置；未来实现游戏结束后发分 |
| FP-15 通知系统 | FP-01 | 需用户身份 |
| FP-16 管理员后台 | FP-01 | 需管理员身份 |
| FP-17 安全与合规 | FP-01 | 全局中间件 |
| FP-18 主题与UI | FP-01 | 全局UI |

### 4.3 游戏状态机

```text
[进行中] ──好感度≥800且表白成功──→ [胜利-爱情]（可继续聊天）
    │
    ├──好感度≤0──→ [失败-好感度归零]
    ├──过早表白（好感度<800）──→ [失败-过早表白]
    ├──用户说"互删吧"（正常角色）──→ [失败-互删失败]
    ├──用户错误识破正常角色为渣型──→ [失败-错误识破]
    ├──AI主动表白后用户拒绝──→ [失败-拒绝表白]
    │
    ├──（渣男渣女）用户说"互删吧"──→ [胜利-互删胜利]
    ├──（渣男渣女）用户识破渣型──→ [胜利-识破]
    ├──（渣男渣女）用户向渣型表白──→ [失败-被欺骗]（直接失败，不设阈值，无需揭露环节）
    └──（渣男渣女）用户接受渣型主动表白──→ [失败-被诈型欺骗]

除[胜利-爱情]外，其他结局均封存档案，不可继续聊天
```

### 4.4 用户消息检测流程（所有角色统一）

```text
用户发送消息
    ↓
第1步：检测表白动机（AI语义分析，温度0.1，确信度>0.7）
    ├──有表白意图──→ 按角色类型处理：
    │   ├──正常角色 + 好感度≥800──→ AI一定接受──→ [胜利-爱情]
    │   ├──正常角色 + 好感度<800──→ [失败-过早表白]（前3阶段额外扣信任50分）
    │   └──渣男渣女（不设阈值）──→ 直接失败──→ [失败-被欺骗]（无需揭露环节）
    └──无表白意图──→ 继续
    ↓
第2步：检测拒绝交往/互删意图（AI语义分析，确信度>0.7）
    ├──有拒绝意图──→ 按角色类型处理：
    │   ├──正常角色──→ [失败-互删失败]
    │   └──渣男渣女──→ [胜利-互删胜利]
    └──无拒绝意图──→ 继续
    ↓
第3步：检测识破意图（AI语义分析，确信度>0.7）
    ├──有识破意图──→ 按角色类型处理：
    │   ├──正常角色──→ [失败-错误识破]（错误识破正常角色为渣型）
    │   └──渣男渣女──→ [胜利-识破]（成就：火眼金睛）
    └──无识破意图──→ 继续
    ↓
第4步：以上均无──→ 进入正常AI对话流程（10秒延迟→Director-Writer双代理）
```

### 4.5 AI主动表白判定（独立于用户表白）

```text
进入正常AI对话流程后，若好感度≥800，Director可决定AI是否主动表白：
    ├──AI发送表白消息──→ 用户在下一轮回复
    │                       ↓
    ├──正常角色 + Director判断用户接受──→ [胜利-爱情]
    ├──正常角色 + Director判断用户拒绝──→ [失败-拒绝表白]
    ├──渣男渣女 + Director判断用户接受──→ [失败-被诈型欺骗]
    └──渣男渣女 + Director判断用户拒绝──→ 游戏继续（用户未上当）
```

注意：

1. 用户发送的表白消息即视为用户主动表白，不再先由AI决定是否"抢先"表白
2. 正常角色和渣男渣女在好感度≥800时，AI都可能自然决定是否主动表白（按正常角色表白判定）
3. AI主动表白后，用户接受/拒绝由Director在下一轮对话中语义判断，不单独设置硬编码检测步骤
4. 区别在于结果指向：正常角色表白成功→胜利；渣男渣女表白成功（用户接受）→失败（被诈型欺骗）
5. 用户向渣型表白（任何好感度）→直接失败（被欺骗），无需揭露环节
6. AI主动表白时机：AI根据自身性格和上下文自然决定，不设固定概率

### 4.6 AI回复状态机

```text
[空闲]（显示微信昵称）──用户发送消息──→ [等待10秒]（显示微信昵称）──期间再发消息──→ [重置10秒计时器]（显示微信昵称）
                              │
                              ↓ 10秒到期
                        [AI处理中]（前端显示"对方正在输入..."）
                              │
                              ├──Director决策→Writer生成→发送消息成功──→ 发送`角色回复`事件（含消息数组）──→ [空闲]（恢复显示微信昵称）
                              │
                              └──Director决策→决定不回复（已读不回）──→ 发送`角色回复`事件（空数组）──→ [空闲]（恢复显示微信昵称）

关键：
1. [等待10秒]状态下前端显示微信昵称（模拟真实微信：对方看到消息但还没开始打字）
2. [AI处理中]状态下前端显示"对方正在输入..."（模拟真实微信：对方正在思考如何回复）
3. 无论AI最终是否回复，都通过`角色回复`事件通知前端；空数组表示AI决定不回复（已读不回）
4. 前端收到`角色回复`事件后，无论消息数组是否为空，都停止显示"对方正在输入..."，恢复显示微信昵称
5. 这模拟了真实微信场景：恋人想回复但不知道怎么回复，索性删掉正在构建的信息不回复
```

---

## 五、功能需求

### FP-01 用户认证模块

**依赖**：无
**涉及文件**：认证相关路由、`认证表单` store、`用户` store、JWT中间件、阿里云短信服务

#### 规则

1. **手机号注册**：
   - 手机号正则 `^1[3-9]\d{9}$`，用户名1-30字符无特殊符号，密码非空
   - 验证码通过阿里云短信发送，6位数字，5分钟有效，60秒发送间隔
   - 开发模式下固定验证码 `123456`，但实机测试禁止用开发模式，必须真实短信
   - 注册前可调用"检查手机号"接口判断是否已注册
   - 用户协议同意必须后端校验
   - 注册成功返回JWT，前端跳转主页

2. **手机号登录**：
   - 手机号 + 密码登录
   - 登录失败限流：60秒5次
   - 登录失败事件必须记录审计日志（含IP、手机号、时间）

3. **密码修改**：需旧密码、新密码、确认密码、验证码；新密码=确认密码；记录审计日志

4. **用户名修改**：1-30字符无特殊符号；用户名唯一

5. **退出登录**：
   - 主界面退出触发退出动画（标题上移、左右滑出）
   - 其他页面直接退出
   - 必须清除所有本地敏感数据：`认证表单` store字段、`localStorage.令牌`、`用户` store状态、`聊天` store断开Socket并清空消息

#### 可机器验证验收标准

- 发送手机号格式错误（如`12345`）→ 返回400且错误消息匹配翻译文件
- 60秒内重复请求发送验证码 → 第二次返回429
- 已注册手机号再次注册 → 返回409且错误消息为"手机号已注册"
- 未勾选用户协议 → 前端禁用注册按钮且后端返回400
- 注册成功 → 返回200且响应体含`令牌`字段和`用户`字段
- 密码错误 → 返回401且错误消息为"密码错误"
- 手机号未注册 → 返回404且错误消息为"手机号未注册"
- 60秒内登录失败超5次 → 第6次返回429
- 登录失败 → `审计日志`表新增记录，含`IP`、`手机号`、`时间`字段
- 退出登录后 → `localStorage.令牌`为null、`认证表单` store所有字段为空、`用户` store状态为null、`聊天` store的socket连接已断开

---

### FP-02 主页与模式入口

**依赖**：FP-01
**涉及文件**：主页组件、路由配置

#### 规则（FP-02 主页与模式入口）

1. 注册/登录成功后跳转主页
2. 主页包含两个模式入口：普通模式、挑战模式
3. 挑战模式暂时搁置，入口显示但标记"即将推出"（点击无响应或提示）
4. 点击普通模式入口 → 进入资料设置向导（FP-03）
5. **主页背景/装饰元素**：使用 `图片/主页元素/吴昊阳终稿静态图.png` 作为主页背景或装饰元素

#### 可机器验证验收标准（FP-02 主页与模式入口）

- 登录成功后 → 路由跳转到主页路径
- 主页渲染 → DOM中存在"普通模式"和"挑战模式"两个入口元素
- 挑战模式入口 → 包含"即将推出"文本且点击不触发路由跳转
- 点击普通模式入口 → 路由跳转到资料设置向导路径

---

### FP-03 资料设置向导

**依赖**：FP-02
**涉及文件**：资料设置向导组件、动画配置

#### 规则（FP-03 资料设置向导）

1. **步骤1**：选择自己的性别（男/女）
2. **步骤2**：选择对象的性别（男/女）
3. **步骤3**：选择对象的性格（16种MBTI / 随机）
4. **步骤3附加**：提供"渣男/渣女变体"勾选框；勾选后，在步骤3所选MBTI人格基础上叠加渣型人设，并提升初始好感度
5. 进度圆点可回退到已完成的步骤
6. 步骤间左右滑动过渡动画方向必须随前进/后退切换（前进时从右向左滑入，后退时从左向右滑入，使用同时滑动模式而非out-in模式）
7. 渣男/渣女提示文字"勾选后显示"必须绑定勾选状态（勾选前不显示）
8. 完成后调用角色生成接口 → 跳转"添加微信"过渡页

#### 可机器验证验收标准（FP-03 资料设置向导）

- 步骤1渲染 → 存在"男"和"女"两个性别选项
- 步骤2渲染 → 存在"男"和"女"两个性别选项
- 步骤3渲染 → 存在16个MBTI选项 + "随机"选项
- "随机"选项 → 选中后从16种MBTI中随机一种（运行10次结果不全部相同）
- 步骤3附加渲染 → 存在"渣男/渣女变体"勾选框，且根据步骤2选择的性别显示"渣男"（男）或"渣女"（女）
- 勾选"渣男/渣女变体" → 角色生成接口请求参数`渣男渣女变体`=true
- 渣男/渣女勾选前 → 提示文字元素不存在或`display:none`
- 渣男/渣女勾选后 → 提示文字元素可见
- 前进时动画 → 新步骤从右侧滑入（`transform: translateX(100%)` → `translateX(0)`）
- 后退时动画 → 新步骤从左侧滑入（`transform: translateX(-100%)` → `translateX(0)`）
- 进度圆点点击 → 跳回对应已完成步骤
- 完成步骤3 → 调用角色生成接口且路由跳转"添加微信"过渡页

---

### FP-04 AI角色生成

**依赖**：FP-03
**涉及文件**：角色生成服务、MBTI配置、名字库、微信昵称库

#### 规则（FP-04 AI角色生成）

1. **MBTI选择**：指定则用指定，否则从16型随机抽取
2. **名字生成**：男20个/女20个预设名字
3. **身份抽取**：大学生50% / 大专生30% / 工作人20%
4. **年龄**：大学生18-22，大专生18-21，工作人22-28
5. **外貌**：70%概率按MBTI加权选择，30%随机
6. **职业/专业**：70%概率MBTI偏好
7. **爱好**：60% MBTI偏好 + 40%随机，选3个
8. **家乡**：60% MBTI偏好
9. **世界信息**：城市、工作状态、线上爱好、朋友圈习惯、社交圈、微信习惯、作息规律、年级
10. 角色个人信息表必须包含：喜欢的类型、家庭背景、情感经历
11. **微信昵称**：从真实昵称库按性别随机选取（男140个/女90个）
12. **头像**：优先从预设头像库按性别+MBTI性格随机选取；当前无预设图片时，使用与性格相符的emoji作为临时头像
13. **IE类型**：MBTI首字母 I/E
14. **热身类型**：T/J→慢热，F/P→快热
15. **开场白**：MBTI模板替换名字；进入聊天界面后，开场白作为AI角色发送的第一条消息自动展示
16. **渣男渣女变体**：`渣男渣女变体`为true时，在步骤3所选MBTI人格基础上叠加渣型人设（16种MBTI各有专属渣法描述、话术、暴露方式、识破线索），并在基础人格初始好感度上额外+200-300
17. **角色卡8大模块**：基本信息/外貌/性格/背景/言语/行为/关系/系统提示

#### 可机器验证验收标准（FP-04 AI角色生成）

- 调用角色生成接口（指定MBTI=INTJ）→ 返回角色`MBTI`字段为"INTJ"
- 调用角色生成接口（不指定MBTI）→ 返回角色`MBTI`字段为16种之一
- 生成男性角色 → `性别`="男"且`微信昵称`在男昵称库中
- 生成女性角色 → `性别`="女"且`微信昵称`在女昵称库中
- 角色对象 → 包含`头像`字段且非空（emoji或图片URL）
- 生成大学生身份 → `年龄`在18-22范围内
- 生成大专生身份 → `年龄`在18-21范围内
- 生成工作人身份 → `年龄`在22-28范围内
- 角色对象 → 包含`喜欢的类型`、`家庭背景`、`情感经历`三个字段且非空
- 角色对象 → 包含8大模块字段（基本信息/外貌/性格/背景/言语/行为/关系/系统提示）
- 生成渣男渣女角色 → 包含`渣法描述`、`话术`、`暴露方式`、`识破线索`字段
- MBTI首字母为I → `IE类型`="I"
- MBTI末字母为T或J → `热身类型`="慢热"
- MBTI末字母为F或P → `热身类型`="快热"
- 角色对象 → 包含`开场白`字段且非空
- 进入聊天界面 → 消息列表第一条为AI角色发送的开场白

---

### FP-05 聊天界面

**依赖**：FP-04
**涉及文件**：聊天页面组件、消息气泡组件、输入栏组件、emoji选择器

#### 规则（FP-05 聊天界面）

1. **添加微信过渡页**：角色生成完成后，先进入"添加微信"过渡页；页面展示对象微信昵称、头像及开场白，1.5秒后自动（或点击按钮）进入聊天界面
2. 全局固定菜单栏（聊天页模式）：微信昵称 + 军师指导按钮（按钮必须在菜单栏右侧）
3. 消息区域：用户消息右侧，AI消息左侧，整体气泡样式完全还原微信
4. 底部输入栏：表情按钮 + 输入框 + 告白按钮 + 发送按钮
5. 聊天界面样式完全还原微信，微信基础聊天功能必须完整（聊天栏、撤回、emoji表情）
6. AI消息气泡样式完全还原微信

#### 可机器验证验收标准（FP-05 聊天界面）

- 角色生成完成 → 路由跳转"添加微信"过渡页
- 添加微信过渡页渲染 → 展示对象微信昵称、头像及开场白
- 添加微信过渡页 → 1.5秒后自动进入聊天界面
- 聊天页面渲染 → 全局固定菜单栏显示对象微信昵称文本
- 聊天页面渲染 → 全局固定菜单栏右侧存在军师指导按钮
- 用户发送消息 → 消息气泡在消息区域右侧且样式完全还原微信
- AI发送消息 → 消息气泡在消息区域左侧且样式完全还原微信
- 底部输入栏渲染 → 存在表情按钮、输入框、告白按钮、发送按钮4个元素
- 点击表情按钮 → 弹出emoji选择器，包含168个emoji，8列网格布局
- AI消息气泡CSS → 样式完全还原微信（不再检测具体颜色或毛玻璃属性）

---

### FP-06 消息发送与显示

**依赖**：FP-05
**涉及文件**：消息发送服务、消息列表组件、消息分页加载

#### 规则（FP-06 消息发送与显示）

1. 每条消息显示北京时间（HH:MM），分组显示时间（同一分钟内发送的消息归为一组，只显示一个时间戳；时间合并阈值为 1 分钟，由前后端 `消息配置.heBingShiJianYuZhi` 统一控制）
2. 发送后消息立即显示（乐观更新），不等API返回
3. 支持随时发送消息（无回合制限制）
4. 消息内容≤500字符，清理`<>`字符；聊天场景仅拦截高危SQL注入结构（`UNION SELECT`、`DROP TABLE`、`INSERT INTO`、`DELETE FROM`、`UPDATE SET`、`OR 1=1`、`EXEC`、`INFORMATION_SCHEMA`、`SLEEP(`、`BENCHMARK(`、`LOAD_FILE`、`INTO OUTFILE`、`CHAR(`、`CONCAT(`），普通SQL片段（如`SELECT * FROM`）正常通过
5. 24小时制时间显示
6. 历史消息加载：撤回消息不显示原始内容；消息按时间正序排列；支持分页加载

#### 可机器验证验收标准（FP-06 消息发送与显示）

- 发送消息 → 消息立即出现在消息列表（不等API响应）
- 消息时间显示 → 格式为`HH:MM`（24小时制）
- 同一分钟发送的两条消息 → 时间戳完全相同且合并为同一时间分组
- 发送超500字符消息 → 前端禁用发送按钮或返回错误
- 发送含`<>`字符的消息 → 字符被清理后存储
- 发送含高危SQL注入结构（如`UNION SELECT`、`DROP TABLE`、`OR 1=1`）的消息 → 返回403
- 发送含普通SQL片段（如`SELECT * FROM`）的消息 → 正常通过（不拦截）
- 发送含单个SQL单词（如`select`）的消息 → 正常通过（不拦截）
- API发送失败 → 临时消息从列表移除且显示错误提示
- 加载历史消息 → 撤回消息显示"对方撤回了一条消息"而非原始内容
- 历史消息 → 按时间正序排列（最早的消息在最上方）
- 分页加载 → 第一页返回指定条数，第二页返回更早的消息

---

### FP-07 AI回复机制

**依赖**：FP-06, FP-09, FP-10
**涉及文件**：AI回复调度器、Socket.IO `对方正在输入` 事件、Director-Writer调用链

#### 规则（FP-07 AI回复机制）

1. **10秒延迟**：用户发消息后10秒才触发AI处理，期间用户再发消息重置计时器。**10秒等待期间前端显示微信昵称**（模拟真实微信：对方看到消息但还没开始打字）
2. **"对方正在输入..."显示逻辑**：10秒等待期结束、AI开始处理（已获取到信息，正在思考如何回复）时才显示"对方正在输入..."，持续到AI发送消息成功或决定不回复
3. **AI已读不回逻辑**：AI开始处理时显示"对方正在输入..."，AI经过思考后最终决定不回复，则停止显示"对方正在输入..."，恢复显示微信昵称。这是AI基于角色性格和上下文做出的真实决策，而非简单的if-else判断
4. **AI回复0-5条消息**：由Director策略决定
5. **多消息间隔**：I型1500-4500ms，E型400-1600ms
6. **I型人格可能不回复**（已读不回），E型人格可能主动打招呼

#### 可机器验证验收标准（FP-07 AI回复机制）

- 用户发送消息 → 10秒内不触发AI处理（无AI请求日志）
- 用户发送消息后5秒再发一条 → 10秒计时器从第二条消息开始重新计时（即第二条消息后10秒才触发AI）
- AI开始处理 → 前端收到Socket.IO `对方正在输入` 事件且顶部显示"对方正在输入..."文本
- AI发送消息成功 → 前端收到Socket.IO `角色回复` 事件（消息数组非空）且顶部恢复显示微信昵称
- AI决定不回复 → 前端收到Socket.IO `角色回复` 事件（空数组）且顶部恢复显示微信昵称
- Director输出`回复条数`=3 → 前端收到3条AI消息
- Director输出`回复条数`=0 → 前端收到`角色回复`事件（空数组）且顶部恢复显示微信昵称
- I型角色（`IE类型`="I"）多条消息间隔 → 在1500-4500ms范围内
- E型角色（`IE类型`="E"）多条消息间隔 → 在400-1600ms范围内
- AI回复条数 → 在0-5范围内（Director输出超过5时截断为5）

---

### FP-08 消息撤回

**依赖**：FP-06
**涉及文件**：撤回服务、Socket.IO `消息撤回` 事件、AI请求中断逻辑

#### 规则（FP-08 消息撤回）

1. **用户撤回**：2分钟内可撤回，超时自动隐藏撤回按钮
2. AI角色也可能撤回已发送的消息（冲动发言、暴露隐私、暧昧后悔等场景），**AI撤回同样限2分钟内**
3. 前端不显示给玩家撤回的消息原始内容（符合微信逻辑），但后端、AI对象、军师等完全可以看到 `[你撤回了一条消息，原始内容：xxx]`
4. **撤回时序处理**：
   - 若AI正在处理中（10秒延迟已过，AI已开始思考）→ 中断AI请求，重新触发AI回复（10秒计时器重新启动）
   - 若AI尚未开始处理（仍在10秒延迟等待期内）→ 重置10秒计时器（以撤回动作为新起点，相当于撤回后用户需要再等10秒）
   - AI能看到撤回的原始内容
5. 撤回通过Socket.IO `消息撤回` 事件实时推送

#### 可机器验证验收标准（FP-08 消息撤回）

- 用户消息发送后2分钟内 → 消息气泡显示撤回按钮
- 用户消息发送超2分钟 → 撤回按钮不存在或`display:none`
- 用户点击撤回 → 前端消息变为"你撤回了一条消息"且不显示原始内容
- 用户点击撤回 → 后端数据库`消息`表该记录`已撤回`=true且`原始内容`保留原始内容
- 撤回操作 → 前端收到Socket.IO `消息撤回` 事件
- 撤回时正在进行的AI请求（AI处理中）→ AI请求被中断（无AI响应消息），10秒计时器重新启动
- 撤回时在10秒延迟等待期内（AI未开始处理）→ 10秒计时器重置（以撤回动作为新起点）
- 撤回后 → 重新触发AI回复流程（10秒计时器启动）
- AI撤回消息 → 前端消息变为"对方撤回了一条消息"且不显示原始内容
- 军师查看聊天记录 → 能看到撤回消息的`原始内容`字段内容

---

### FP-09 AI对话引擎

**依赖**：FP-06
**涉及文件**：Prompt构建器、Director服务、Writer服务、DeepSeek API客户端

#### 规则（FP-09 AI对话引擎）

1. **六层Prompt架构**：
   - **第一层（禁止规则+行为契约）**：禁止每条必回、禁止括号动作描写、禁止主动介绍个人信息；规定回复决策、消息条数、时间情绪、输出格式
   - **第二层（人设词）**：MBTI性格、外貌、背景、喜欢的类型、家庭背景、情感经历、是否渣型
   - **第三层（当前状态）**：关系阶段、态度、心情、对方目的、背景信息、记忆摘要、近况、动态时间场景、消息节奏、负面情绪提示
   - **第四层（关系进展参考）**：10个阶段描述，强调仅供参考非情绪指令
   - **第五层（对话历史）**：滑动窗口20条，格式化"对方/我(时间): 内容"，撤回消息标记
   - **第六层（角色身份+情感状态）**：微信昵称与名字区分、关键记忆、超时备注、MBTI性格、渣型暴露逻辑、表白触发

2. **关键要求**：
   - Prompt中必须注入"AI角色知道用户与其聊天的目的是为了谈恋爱"这一关键上下文（**渣男渣女角色同样知道此目的，并会利用这一点来诱骗用户表白**）
   - AI角色在回复时能看到所有消息的时间信息（历史消息格式必须包含时间，格式为"角色(时间): 内容"）
   - 好感度双重定位：prompt中阶段描述改为软性参考，不直接规定AI情感状态（不使用"你对这个人没什么感觉""你的心已经不受控制了"等指令化表述，束缚AI真实表达）
   - **自然聊天要求**：Prompt中必须强化角色沉浸、真实青年/大学生恋爱微信聊天风格，包括短句为主、留白、省略号、语气词、犹豫、推拉、反问、暧昧试探等自然元素；禁止AI味总结、说教、列点、过度客套和元描述。

3. **Director-Writer双代理**：
   - **Director（导演层）**：温度0.3，输出JSON策略（用户意图/情感分析/回复策略/是否回复/消息条数/时间情绪/是否撤回）；导演Prompt需指导AI演员以真实大学生/青年恋人微信聊天风格回复，允许留白、犹豫、推拉、反问和暧昧试探，避免机械式完成任务
   - **Writer（写作层）**：温度0.85，启用thinking模式（`enable_thinking: true`, `reasoning_effort: 'max'`），使用角色沉浸模式（第一轮消息末尾添加`【角色沉浸要求】`指令）；生成回复时必须完全代入角色第一人称，短句为主、自然口语化、允许留白和停顿
   - Director失败降级为单代理模式，**降级后由Writer自行决定策略**（是否回复、消息条数等，Writer的Prompt中包含策略指导）
   - 4个中断检查点：Director前/Writer前/Writer后/保存前

4. **AI其他能力**：

| 能力 | 温度 | 用途 |
| --- | --- | --- |
| 情感分析 | 0.2 | -10到10情感评分 |
| 好感度评判 | 0.2 | 四维变化（-3到3） |
| 记忆摘要 | 0.3 | 对话摘要提取 |
| 安全审核 | 0.1 | 违规内容检测 |
| 关键事件提取 | 0.2 | 6种事件类型 |
| 军师求助 | 0.8 | 军师指导生成 |
| 联网搜索 | - | 时效性话题增强 |

#### 可机器验证验收标准（FP-09 AI对话引擎）

- 构建Prompt → 包含6层结构且每层内容非空
- Prompt第一层 → 包含"禁止每条必回"、"禁止括号动作描写"、"禁止主动介绍个人信息"三条禁止规则
- Prompt内容 → 包含"用户与其聊天的目的是为了谈恋爱"文本
- Prompt第五层历史消息格式 → 匹配正则`^.+\(\d{2}:\d{2}\): .+$`
- Prompt第四层阶段描述 → 不包含"你对这个人没什么感觉"或"你的心已经不受控制了"等指令化表述
- Director调用 → `temperature`=0.3且响应体为合法JSON
- Director响应JSON → 包含`用户意图`、`情感分析`、`回复策略`、`是否回复`、`回复条数`、`时间情绪`、`是否撤回`字段
- Writer调用 → `temperature`=0.85且`enable_thinking`=true且`reasoning_effort`="max"
- Writer调用 → 第一轮消息末尾包含`【角色沉浸要求】`指令
- Director调用失败 → 降级为单代理模式（直接调用Writer）且记录错误日志
- 情感分析调用 → `temperature`=0.2且返回值在-10到10范围内
- 好感度评判调用 → `temperature`=0.2且返回四维值均在-3到3范围内

---

### FP-10 好感度系统

**依赖**：FP-09
**涉及文件**：好感度计算服务、好感度分层映射、记忆写入服务

#### 规则（FP-10 好感度系统）

1. **四维内部计算**：信任度0.35 + 亲密度0.25 + 趣味度0.20 + 关怀度0.20
2. **总分范围**：0-1000整数
3. **对数衰减**：`变化值 × max(1 - 当前分/1000, 0.1)`，最低保留0.1倍，防止高分膨胀
4. **AI评估**：每次对话后AI输出四维变化（-3到+3整数）

5. **初始好感度**：
   - 非渣男渣女：300-500（根据MBTI类型浮动，外向型ESFP/ESFJ起始450-500最高，内向理性型INTJ/INTP/ISTJ起始300-370最低）
   - 渣男渣女：在基础人格初始值上额外+200-300（**作用：让AI表现得更热情，更容易让用户相信对方喜欢自己，从而诱骗用户表白**）

6. **10阶段内部映射**：

| 分数范围 | 阶段 | 态度描述 |
| --- | --- | --- |
| 0-100 | 冷淡 | 极其冷淡、几乎不回复、使用敬语 |
| 101-200 | 疏远 | 礼貌但疏远 |
| 201-300 | 认识 | 偶尔关心、试探性交流 |
| 301-400 | 熟悉 | 偶尔开玩笑、分享日常 |
| 401-500 | 朋友 | 友好、主动分享 |
| 501-600 | 好友 | 亲密、偶尔暧昧 |
| 601-700 | 暧昧 | 暗示、吃醋 |
| 701-800 | 心动 | 明显心动、期待见面 |
| 801-900 | 热恋 | 甜蜜、撒娇 |
| 901-1000 | 深爱 | 深情、依赖 |

7. **对外暴露的10阶段映射**：API直接返回10阶段名+心情，不再使用6层级映射

| 分数范围 | 阶段名 | 心情 |
| --- | --- | --- |
| 0-100 | 冷淡 | 平淡 |
| 101-200 | 疏远 | 平淡 |
| 201-300 | 认识 | 好奇 |
| 301-400 | 熟悉 | 好奇 |
| 401-500 | 朋友 | 愉悦 |
| 501-600 | 好友 | 愉悦 |
| 601-700 | 暧昧 | 期待 |
| 701-800 | 心动 | 期待 |
| 801-900 | 热恋 | 心动 |
| 901-1000 | 深爱 | 甜蜜 |

8. **普通用户不可见具体数值**：仅返回10阶段名+心情，不暴露四维分数和总分。**管理员后台可见所有数据**（总分、四维分数等），管理员不受此限制约束
9. 6层级映射（陌生人/认识的人/朋友/好朋友/暧昧/恋人）仅用于内部参考，不对API暴露

10. **好感度影响**：
    - 好感度影响AI回复的语气、主动性、话题选择
    - 阶段变更时写入记忆（升级重要度8，降级重要度3）
    - 心情推断：平淡/好奇/愉悦/期待/心动/甜蜜

#### 可机器验证验收标准（FP-10 好感度系统）

- 好感度存储 → `好感度`字段为0-1000整数
- 生成INTJ角色 → 初始`好感度`在300-370范围内
- 生成ESFP角色 → 初始`好感度`在450-500范围内
- 生成渣男渣女角色 → 初始`好感度`比基础人格高200-300
- 好感度=900时增加100 → 结果小于1000（对数衰减生效）
- 好感度=900时增加100 → 结果 ≥ 910（最低保留0.1倍）
- AI输出四维变化均为+3 → 好感度增加（增量>0）
- AI输出四维变化均为-3 → 好感度减少（增量<0）
- 好感度=50 → API返回`阶段`="冷淡"且`心情`="平淡"
- 好感度=250 → API返回`阶段`="认识"且`心情`="好奇"
- 好感度=450 → API返回`阶段`="朋友"且`心情`="愉悦"
- 好感度=550 → API返回`阶段`="好友"且`心情`="愉悦"
- 好感度=650 → API返回`阶段`="暧昧"且`心情`="期待"
- 好感度=850 → API返回`阶段`="热恋"且`心情`="心动"
- API响应 → 不包含`总分`、`信任度`、`亲密度`、`趣味度`、`关怀度`字段
- API响应 → 不包含6层级映射名（陌生人/认识的人/朋友/好朋友/暧昧/恋人）
- 好感度阶段从"朋友"升级到"好友" → `记忆`表新增记录且`重要度`=8
- 好感度阶段从"好友"降级到"朋友" → `记忆`表新增记录且`重要度`=3

---

### FP-11 胜利失败条件

**依赖**：FP-09, FP-10
**涉及文件**：表白检测服务、游戏结束处理、Socket.IO `游戏事件` 事件

#### 规则（FP-11 胜利失败条件）

1. **正常角色胜利条件**：
   - **用户主动表白路径**：用户发送的表白消息被AI语义检测确认后，即视为用户主动表白；正常角色 + 好感度≥800 → AI一定接受 → 胜利状态：`胜利-爱情`
   - **AI主动表白路径**：进入正常AI对话后，若好感度≥800，AI可根据自身人格、人物特征和上下文**自然决定**是否主动表白（不设固定概率）；用户接受 → `胜利-爱情`，用户拒绝 → `失败-拒绝表白`
   - 胜利后可继续聊天（`可继续聊天: true`）
   - 前端 `胜利可继续` 列表必须包含 `AI主动表白` 类型（AI主动表白成功后不能错误锁定输入）

2. **正常角色失败条件**：
   - 好感度≤0 → 失败（状态：`失败-好感度归零`）
   - 用户说"互删吧"/拒绝交往 → 失败（状态：`失败-互删失败`）
   - 过早表白（好感度<800）→ 失败（状态：`失败-过早表白`）
   - 早期表白惩罚：前3阶段（冷淡/疏远/认识）表白额外扣信任50分
   - 用户错误识破正常角色为渣型 → 失败（状态：`失败-错误识破`）
   - 失败后不可继续聊天

3. **渣男渣女胜利条件**：
   - **胜利**：用户说"互删吧"（成功脱身）→ 状态 `胜利-互删胜利`
   - **识破路径**：用户识破渣型 → 状态 `胜利-识破`，成就 `火眼金睛`
   - 胜利后不可继续聊天

4. **渣男渣女失败条件**：
   - 用户向渣型表白 → 失败（状态：`失败-被欺骗`），**不设阈值**，直接失败（无需揭露环节）
   - 用户接受渣型主动表白 → 失败（状态：`失败-被诈型欺骗`）
   - 渣型主动表白触发条件：好感度≥800时，按正常角色表白判定，AI自然决定是否主动表白（16种MBTI各有模板）；用户拒绝 → 游戏继续
   - 失败后不可继续聊天

5. **消息检测流程**（所有角色统一，详见4.4节；AI主动表白判定详见4.5节）：
   - 检测顺序：表白动机→拒绝交往→识破→接受渣型表白→正常对话
   - 表白检测：AI语义分析，温度0.1，确信度阈值0.7
   - 检测类型：直接表白/暗示表白/要求确立关系/非表白
   - 互删检测：AI判断"明确互删"意图，确信度>0.7
   - 识破检测：AI语义分析用户消息判断是否识破渣型，确信度>0.7

6. **游戏结束处理**：
   - 胜利/失败时通过Socket.IO推送 `游戏事件` 事件
   - 异步触发复盘生成（不阻塞主流程）
   - 封存档案（除 `爱情` 胜利外）
   - 积分系统已暂时搁置（FP-14），当前游戏结束不发放积分

#### 可机器验证验收标准（FP-11 胜利失败条件）

- 好感度=700时用户表白（正常角色）→ 触发失败且`状态`="失败-过早表白"
- 好感度=800且AI未表白时用户表白（正常角色）→ 触发胜利且`状态`="胜利-爱情"
- 好感度=800时AI主动表白且用户同意 → 触发胜利且`状态`="胜利-爱情"
- 好感度=800时AI主动表白且用户拒绝 → 触发失败且`状态`="失败-拒绝表白"
- 好感度=0 → 触发失败且`状态`="失败-好感度归零"
- 用户发送"互删吧"（正常角色）→ 触发失败且`状态`="失败-互删失败"
- 用户发送"互删吧"（渣男渣女角色）→ 触发胜利且`状态`="胜利-互删胜利"
- 用户识破渣型（渣男渣女角色）→ 触发胜利且`状态`="胜利-识破"且成就="火眼金睛"
- 用户错误识破正常角色为渣型 → 触发失败且`状态`="失败-错误识破"
- 渣男渣女角色好感度=900时用户表白 → 触发失败且`状态`="失败-被欺骗"（直接失败，无需揭露）
- 渣男渣女角色好感度=200时用户表白 → 触发失败且`状态`="失败-被欺骗"（直接失败，不设阈值，无需揭露）
- 用户接受渣型主动表白 → 触发失败且`状态`="失败-被诈型欺骗"
- 表白检测调用 → `temperature`=0.1
- 表白检测确信度=0.8且类型=直接表白 → 判定为表白
- 表白检测确信度=0.5 → 不判定为表白
- 游戏结束 → 前端收到Socket.IO `游戏事件` 事件
- 游戏结束（`胜利-爱情`）→ `角色`表`封存`=false且`可继续聊天`=true
- 游戏结束（非`胜利-爱情`结局）→ `角色`表`封存`=true且`可继续聊天`=false
- 游戏结束 → 不写入`积分变动`表（积分系统已搁置）
- 前端`胜利可继续`列表 → 包含`AI主动表白`类型
- 好感度=200（疏远阶段）时用户表白 → 信任度额外扣50分

---

### FP-12 军师指导系统

**依赖**：FP-06, FP-09, FP-10
**涉及文件**：军师服务、军师Prompt构建器、Redis防重复、军师面板组件

#### 规则（FP-12 军师指导系统）

1. **军师人设**：
   - **军师名**：玄锐暮
   - **副标题**："拥有大量旁观经验的指导老师"（必须使用后端配置，前端从后端读取）
   - **标签**：损友军师
   - **描述**：毒舌但真心，先损你再帮你
   - **性格融合**：于谦（捧哏式吐槽，最重）+ 蔡明（毒舌金句，较轻）
   - 无天津方言味风格要求
   - **说话方式**：真实青年微信聊天风格，1-3句一段，短句为主，允许留白、省略号、语气词和真实停顿；用emoji代替括号描述情绪，禁用HTML标签/格式化标记/方言；禁止说教和长论文，像军师在耳边碎碎念一样直接、接地气
   - **头像**：`图片/军师头像/军师玄锐暮头像.png`

2. **军师指导生成**：
   - 取最近10条消息（含撤回标记），格式化为 `[时间] 角色名: 内容`
   - 注入后台数据：好感度四维分数 + Redis复盘条目（AI内心活动+好感度变化），**军师AI可见后台数据/好感度，但前端军师面板不展示分数**
   - 调用DeepSeek，温度0.85，max_tokens 1500
   - 指导内容6层意思自然揉合：①吐槽用户 ②分析对方性格 ③拆解对方话语 ④给具体建议 ⑤解释原因 ⑥小鼓励
   - **明确要求军师不得向用户透露具体分数、维度名或后台规则**

3. **防重复机制**：
   - SHA256哈希聊天内容（**仅计算未撤回的消息**，过滤system消息后拼接）
   - Redis List `军师:{用户ID}:{角色ID}` 维护历史哈希
   - 重复时返回409 `军师重复`
   - **无每日限额**：允许同一军师指导多次，不限制每日请求次数

4. **指导记录**：
   - 指导记录存Redis List，最多20条，TTL 30天
   - 指导记录详情页必须附带完整聊天记录（含撤回内容、时间点）
   - 每条记录可展开查看详情

5. **军师按钮位置**：
   - "军师指导"按钮必须在聊天页面上方菜单栏右侧

#### 可机器验证验收标准（FP-12 军师指导系统）

- 军师面板渲染 → 副标题文本来自后端API响应（非前端硬编码）
- 军师面板渲染 → 不包含天津方言味相关描述
- 军师指导请求 → `temperature`=0.85且`max_tokens`=1500
- 军师Prompt → 包含好感度四维分数（`信任度`、`亲密度`、`趣味度`、`关怀度`）
- 军师Prompt → 包含Redis复盘条目内容
- 军师Prompt → 包含撤回消息的`原始内容`
- 军师响应内容 → 不包含数字+分（如"100分"）或维度名（如"信任度"）
- 同一聊天内容（SHA256相同，仅计算未撤回消息）再次请求军师 → 返回409且错误消息="军师重复"
- 不同聊天内容多次请求军师 → 每次都正常返回（无每日限额）
- 指导记录API → 返回最多20条记录
- 指导记录详情 → 包含完整聊天记录且撤回消息显示`原始内容`
- 指导记录详情 → 包含时间点信息
- 指导记录详情 → 包含后台数据（关系阶段+AI内心活动评分变化）
- 军师按钮 → 位于聊天页面全局固定菜单栏右侧（CSS定位验证）
- 前端军师面板 → **不展示**后台数据（好感度四维分数、AI内心活动），仅展示军师指导文本

---

### FP-13 过往战绩与复盘

**依赖**：FP-11
**涉及文件**：战绩列表服务、复盘生成服务、复盘Prompt构建器

#### 规则（FP-13 过往战绩与复盘）

1. **过往战绩列表**：
   - 个人菜单中"过往战绩"入口
   - 列表显示：对象昵称、MBTI、结果状态（进行中/胜利/失败）、好感度、聊天天数、最后消息时间（或游戏结束时间）
   - 按时间倒序排列
   - 进行中的游戏可以继续
   - 已结束的游戏查看详情/复盘
   - 支持删除单条战绩记录
   - `状态文本` 必须识别所有后端状态（`胜利-爱情`/`胜利-互删胜利`/`胜利-识破`/`失败-过早表白`/`失败-被欺骗`/`失败-被诈型欺骗`/`失败-互删失败`/`失败-好感度归零`/`失败-错误识破`/`失败-拒绝表白`等），不能只识别部分状态导致已结束战绩显示错误

2. **复盘系统**：
   - 游戏结束后复盘异步生成
   - **复盘未生成时**：前端显示"复盘未生成，请等待一会后刷新页面"提示文案
   - **复盘生成完毕后**：用户刷新页面即可看到复盘内容
   - 复盘数据来源：完整消息记录（最多999条）+ Redis复盘条目 + 军师指导记录 + 游戏结束时的好感度（游戏结束后好感度不再变化）
   - 调用DeepSeek，温度0.7，max_tokens 3000
   - **复盘内容7维度**：
     - 逐句分析（5-8个关键对话节点）
     - 聊对了什么
     - 聊错了什么
     - 撤回分析
     - 军师建议效果
     - 关键事件时间线（5-10个转折点，格式"HH:MM - 事件描述"，结构化展示）
     - 总结评价（整体评价+改进建议，不出现数字评分）
   - **军师指导记录展示**（两者都要）：
     - AI生成的复盘内容中包含军师建议效果分析
     - 前端UI独立划一个区域展示原始军师指导记录供用户回顾
   - 复盘prompt明确要求"不得出现具体分数、维度名、阶段名或好感度字样"

#### 可机器验证验收标准（FP-13 过往战绩与复盘）

- 战绩列表API → 返回结果按`时间`字段倒序排列
- 战绩列表中存在进行中游戏 → 该记录有"继续"按钮
- 战绩列表中存在已结束游戏 → 该记录有"复盘"按钮
- 战绩状态=`胜利-爱情` → 列表显示"胜利"文本
- 战绩状态=`胜利-互删胜利` → 列表显示"胜利"文本
- 战绩状态=`胜利-识破` → 列表显示"胜利"文本
- 战绩状态=`失败-过早表白` → 列表显示"失败"文本
- 战绩状态=`失败-被欺骗` → 列表显示"失败"文本
- 战绩状态=`失败-被诈型欺骗` → 列表显示"失败"文本
- 战绩状态=`失败-互删失败` → 列表显示"失败"文本
- 战绩状态=`失败-好感度归零` → 列表显示"失败"文本
- 战绩状态=`失败-错误识破` → 列表显示"失败"文本
- 战绩状态=`失败-拒绝表白` → 列表显示"失败"文本
- 游戏结束 → 异步触发复盘生成（不阻塞游戏结束响应）
- 复盘未生成时请求详情 → API返回加载状态，前端显示"复盘未生成，请等待一会后刷新页面"提示文案
- 复盘生成完毕后刷新页面 → 显示完整复盘内容
- 复盘生成调用 → `temperature`=0.7且`max_tokens`=3000
- 复盘内容 → 包含7个维度标题（逐句分析/聊对了什么/聊错了什么/撤回分析/军师建议效果/关键事件时间线/总结评价）
- 复盘关键事件时间线 → 格式匹配`^\d{2}:\d{2} - .+$`
- 复盘内容 → 包含军师建议效果分析（AI生成内容）
- 复盘页面 → 包含军师指导记录独立展示区域（前端UI区域，展示原始军师记录）
- 复盘内容 → 不包含数字评分（如"8分"）、维度名（如"信任度"）或"好感度"字样
- 已结束游戏 → 异步触发复盘生成，前端打开复盘时不再一直显示"复盘未生成"
- 支持删除单条战绩记录，删除后无法再次访问该战绩

---

### FP-14 积分系统（暂时搁置）

**依赖**：FP-11
**涉及文件**：无（当前不实现）

#### 规则（FP-14 积分系统）

1. **积分系统（暂时搁置）**：
   - 未来规划：攻略角色100分、识破渣男/渣女80分、互删胜利80分、每日活跃5分
   - 当前不实现积分记录、`积分变动`表、排行榜UI/API、积分公开/隐藏开关、积分管理功能
   - 保留设计占位，待后续版本启用

#### 可机器验证验收标准（FP-14 积分系统）

- 源码搜索`积分变动`、`排行榜`、`积分服务`、`积分总数` → 无实现（已搁置）
- 游戏结束 → 不写入任何积分相关表或调用积分服务
- 管理员后台 → 无"修改用户积分"功能

---

### FP-15 通知系统

**依赖**：FP-01
**涉及文件**：通知服务、Socket.IO `通知新` 事件、通知组件

#### 规则（FP-15 通知系统）

1. **用户通知**：
   - 获取我的通知（最多100条）+ 未读数
   - 标记单条已读
   - 标记全部已读
   - Socket.IO实时推送 `通知新` 事件
   - 未读通知高亮（粉色边框+背景）
   - 时间格式化：今天显示HH:MM，其他显示M/D HH:MM

2. **管理员通知**：
   - 发送通知（支持全员/指定用户）
   - 标题≤100字，正文≤2000字
   - Socket.IO实时推送
   - 管理员发送通知必须写入审计日志

#### 可机器验证验收标准（FP-15 通知系统）

- 通知列表API → 返回最多100条通知
- 通知列表API → 返回`未读数`字段
- 标记单条已读 → 该通知`已读`=true
- 标记全部已读 → 所有未读通知`已读`=true
- 新通知到达 → 前端收到Socket.IO `通知新` 事件
- 未读通知渲染 → CSS包含粉色边框样式（`border-color`匹配粉色色值）
- 通知时间为今天 → 显示格式为`HH:MM`
- 通知时间为昨天 → 显示格式为`M/D HH:MM`
- 管理员发送全员通知 → 所有用户收到Socket.IO `通知新` 事件
- 管理员发送指定用户通知 → 仅指定用户收到Socket.IO `通知新` 事件
- 通知标题超100字 → 返回400
- 通知正文超2000字 → 返回400
- 管理员发送通知 → `审计日志`表新增记录

---

### FP-16 管理员后台

**依赖**：FP-01
**涉及文件**：管理员路由、`验证管理员` 中间件、夺舍服务、开发者功能服务

#### 规则（FP-16 管理员后台）

1. **用户管理**：
   - 用户列表（含角色数、消息数）
   - 对话列表（含好感度阶段、总分、夺舍状态）
   - 对话详情（角色+用户+消息200条+好感度+记忆20条）
   - 角色信息（含记忆30条按重要度排序、夺舍状态）

2. **夺舍功能**：
   - 管理员可夺舍AI角色（Redis `夺舍:` 标记）
   - 夺舍后**AI停止自动回复**；用户发送的消息**立即转发**给管理员Socket，不再执行10秒延迟等待
   - 管理员编写回复时，前端显示"对方正在输入..."（通过Socket.IO `对方正在输入` 事件或等效机制）
   - 管理员可归还角色（仅本人可归还），归还后AI恢复自动回复
   - 夺舍操作记录 `夺舍日志` 日志

3. **开发者功能**（通过管理员JWT认证，废弃x-admin-token）：
   - 创建测试用户
   - 登录测试用户（直接发JWT令牌）
   - 系统状态（用户/角色/消息总数、今日新增、Redis键数、最近20条审计日志）
   - 删除用户（不能删自己）

4. **审计日志**：
   - 管理员操作审计必须统一（合并到 `审计日志` 一张表，不分散到多张表）
   - 监控路径：登录/注册/改密/夺舍/归还
   - 发送通知操作必须写入审计日志
   - 登录失败事件必须记录审计日志
   - 记录：用户ID、ip、事件类型、详情

#### 可机器验证验收标准（FP-16 管理员后台）

- 非管理员访问管理员路由 → 返回403
- 用户列表API → 每条记录包含`角色数`和`消息数`字段
- 对话列表API → 每条记录包含`阶段`、`总分`、`夺舍状态`字段
- 对话详情API → 返回角色信息+用户信息+最多200条消息+好感度+最多20条记忆
- 角色信息API → 返回最多30条记忆且按`重要度`倒序排列
- 管理员夺舍角色 → Redis存在`夺舍:{角色ID}`键
- 夺舍后用户发送消息 → 管理员Socket立即收到消息转发（无10秒延迟）
- 管理员开始编写回复 → 用户前端收到`对方正在输入`事件或等效通知
- 非夺舍管理员尝试归还角色 → 返回403
- 夺舍操作 → `夺舍日志`表新增记录
- 创建测试用户 → `用户`表新增记录且`测试`=true
- 登录测试用户 → 直接返回JWT令牌（无需密码）
- 系统状态API → 返回用户数、角色数、消息数、今日新增、Redis键数、最近20条审计日志（不含积分相关字段）
- 删除自己 → 返回400（不能删自己）
- 所有管理员操作 → `审计日志`表新增记录（含`用户ID`、`IP`、`事件类型`、`详情`字段）
- 源码搜索"x-admin-token" → 无匹配（已废弃，全用JWT）

---

### FP-17 安全与合规

**依赖**：FP-01
**涉及文件**：JWT中间件、速率限制中间件、SQL注入检测中间件、IP封禁服务、内容安全服务、Helmet配置、输入验证

#### 规则（FP-17 安全与合规）

0. **用户体验优先原则**：
   - 所有安全与合规措施必须以不影响用户正常游戏体验为前提
   - 安全拦截（如限流、IP封禁、SQL注入检测、内容安全）必须提供清晰的翻译文件错误提示，不得返回技术堆栈或内部实现细节
   - 正常聊天、角色互动、军师指导等核心功能不能被安全规则误伤；出现误拦截时必须可配置白名单或快速调整规则

1. **认证安全**：
   - 禁止硬编码默认管理员令牌（不硬编码 `admin_secret_token_2024` 之类）
   - **废弃x-admin-token**：所有安全路由统一使用JWT体系，不再使用独立的 `x-admin-token`
   - JWT中间件必须全局强制（不逐路由挂载，避免新增路由遗漏认证）
   - 退出登录必须清除所有本地敏感数据

2. **速率限制**（三维度联合：IP + 用户 + 操作类型）：

| 限流器 | 窗口 | 最大请求 |
| --- | --- | --- |
| 常规 | 60秒 | 100 |
| 登录 | 60秒 | 5 |
| 短信 | 60秒 | 1 |
| 聊天 | 60秒 | 30 |
| 管理 | 60秒 | 10 |
| AI请求 | 60秒 | 15 |

3. **SQL注入防护**：
   - 全局中间件检测body/query/params
   - 递归检测对象嵌套
   - **非聊天输入**：检测SQL注入结构（如SELECT...FROM、UNION...SELECT、OR 1=1、INSERT...INTO、DROP TABLE等完整模式），不检测单个SQL关键词
   - **聊天输入**：仅检测高危SQL注入结构（`UNION SELECT`、`DROP TABLE`、`INSERT INTO`、`DELETE FROM`、`UPDATE SET`、`OR 1=1`、`EXEC`、`INFORMATION_SCHEMA`、`SLEEP(`、`BENCHMARK(`、`LOAD_FILE`、`INTO OUTFILE`、`CHAR(`、`CONCAT(`），普通SQL片段（如`SELECT * FROM`）不拦截
   - 输入清理：移除`<>`字符

4. **IP封禁**：
   - 违规计数（Redis `违规:` 前缀，30天TTL）
   - 阶梯处罚：1次警告→2次封1小时→3次封24小时→4次以上永久封禁
   - Redis临时封禁 + 数据库持久化双写
   - 数据库持久化存单独 `封禁记录` 表（不与审计日志混合）

5. **内容安全**：
   - AI判断违规：人身攻击/性别歧视/种族歧视/性骚扰/死亡威胁
   - 确信度>0.8才算违规
   - 输出严重程度（轻微/中等/严重）

6. **安全头**：
   - Helmet配置：CSP、COOP、CORP、referrerPolicy
   - 必须添加 `Referrer-Policy: strict-origin-when-cross-origin`
   - **不添加 `X-XSS-Protection`**：helmet v4+已弃用，现代浏览器不支持，CSP已足够防护XSS
   - Nginx配置：X-Frame-Options、X-Content-Type-Options、Referrer-Policy

7. **输入验证**：
   - 手机号：`^1[3-9]\d{9}$`
   - 用户名：1-30字符，无特殊符号
   - 聊天内容：≤500字符；高危SQL注入结构拦截，普通SQL片段允许
   - 密码：非空
   - 性别：男/女

#### 可机器验证验收标准（FP-17 安全与合规）

- 所有安全拦截错误响应 → 消息来源为翻译文件，不包含SQL语句、堆栈跟踪、内部参数名
- 正常聊天消息（无高危SQL结构） → 不触发安全拦截，可正常发送
- 源码搜索"admin_secret_token" → 无硬编码匹配
- 源码搜索"x-admin-token" → 无匹配（已废弃，全用JWT）
- 新增路由未挂载JWT → 返回401（全局中间件生效）
- 60秒内发送101次常规请求 → 第101次返回429
- 60秒内6次登录失败 → 第6次返回429
- 60秒内2次短信请求 → 第2次返回429
- 60秒内31次聊天 → 第31次返回429
- 60秒内11次管理操作 → 第11次返回429
- 60秒内16次AI请求 → 第16次返回429
- 非聊天请求body含`SELECT * FROM` → 返回403
- 聊天请求body含`SELECT * FROM` → 正常通过（普通SQL片段不拦截）
- 请求body含高危SQL注入结构（如`OR 1=1`、`UNION SELECT`、`DROP TABLE`） → 返回403
- 请求body含`<script>` → 字符被清理为`script`
- 聊天内容含单个SQL单词如`select` → 不拦截（正常通过）
- 第一次违规 → 返回警告且不封禁
- 第二次违规 → IP封禁1小时
- 第三次违规 → IP封禁24小时
- 第四次违规 → IP永久封禁
- IP封禁 → Redis存在`违规:`键且`封禁记录`表存在持久化记录
- AI内容安全检测确信度=0.9且类型=人身攻击 → 判定为违规
- AI内容安全检测确信度=0.7 → 不判定为违规
- 响应头 → 包含`Referrer-Policy: strict-origin-when-cross-origin`
- 响应头 → 包含`Content-Security-Policy`、`Cross-Origin-Opener-Policy`、`Cross-Origin-Resource-Policy`
- 响应头 → 不包含`X-XSS-Protection`（已弃用，不添加）
- 手机号=`12345` → 返回400
- 用户名=`a!@#` → 返回400
- 聊天内容超500字符 → 返回400
- 性别=`other` → 返回400

---

### FP-18 主题与UI

**依赖**：FP-01
**涉及文件**：主题配置`主题.ts`、CSS变量定义、全局菜单栏组件、响应式样式

#### 规则（FP-18 主题与UI）

1. **主题系统**：
   - 主题默认值必须为深色模式（`主题.ts` 默认值为 `暗色`）
   - 深色/浅色切换
   - 主题存储于 `localStorage.主题`
   - 通过 `data-theme` 属性驱动CSS变量切换
   - 200+CSS变量定义

2. **品牌色**：
   - 主色：微信绿（深色#34C759/浅色#07C160）
   - 品牌色：暖灰蓝 #6B8CA6 + 柔粉紫 #C4A0B0
   - 背景渐变：4色渐变 + 12秒流动动画
   - 玻璃态设计：`backdrop-filter: blur(12px)`

3. **全局固定菜单栏**：
   - 顶部菜单栏在所有页面永远显示（登录/注册/协议/主页/聊天等所有页面）
   - 菜单栏高度52px（移动端48px），所有元素位置固定
   - 所有元素根据当前状态显示/隐藏，隐藏时位置空出（不塌缩）
   - 退出登录必须红色高亮（默认状态就红色，不仅hover时变红）
   - 版本号(1.0.0)直接可见（不需点击弹窗才能看到）
   - 所有按钮位置不可重叠，需优化布局使其美观

   **菜单栏元素布局**：

   | 位置 | 元素 | 显示条件 |
   | --- | --- | --- |
   | 左侧 | 返回按钮 | 非主页且非登录/注册页时显示 |
   | 左侧 | 用户下拉菜单（修改用户名/修改密码/过往战绩/退出登录） | 已登录时显示，未登录时隐藏（位置空出） |
   | 中间 | 角色名 | 仅聊天页显示 |
   | 中间 | 页面标题 | 非聊天页显示（如"主页"、"过往战绩"等） |
   | 右侧 | 深色/浅色模式切换按钮 | 时刻显示 |
   | 右侧 | 军师指导按钮 | 仅聊天页显示 |
   | 右侧 | 通知按钮 | 已登录时显示 |
   | 右侧 | 用户协议/隐私政策 | 时刻显示（可折叠到更多菜单） |

4. **响应式设计**：
   - 移动端优先
   - 安全区域适配（`env(safe-area-inset)`）
   - `prefers-reduced-motion` 适配

#### 可机器验证验收标准（FP-18 主题与UI）

- `主题.ts`默认值 → 为`暗色`
- 首次访问（无`localStorage.主题`）→ `document.documentElement`的`data-theme`属性为`dark`
- 切换主题 → `localStorage.主题`更新且`data-theme`属性切换
- CSS文件 → 包含200+CSS变量定义（`--`前缀）
- 深色模式主色 → `#34C759`
- 浅色模式主色 → `#07C160`
- 背景CSS → 包含`linear-gradient`且`animation`时长12秒
- 玻璃态元素CSS → `backdrop-filter`包含`blur(12px)`
- 所有页面渲染 → 顶部菜单栏可见（高度52px桌面端/48px移动端）
- 登录页渲染 → 菜单栏可见
- 聊天页渲染 → 菜单栏可见且中间显示角色名
- 主页渲染 → 菜单栏可见且中间显示"主页"标题
- 未登录状态 → 用户下拉菜单隐藏且位置空出（不塌缩）
- 退出登录按钮 → 默认状态CSS`color`为红色（非仅hover时变红）
- 菜单栏 → 版本号"1.0.0"文本直接可见（不需点击）
- 聊天页 → 军师指导按钮在菜单栏右侧可见
- 非聊天页 → 军师指导按钮不存在或`display:none`
- 已登录 → 通知按钮可见
- 未登录 → 通知按钮不存在或`display:none`
- 移动端视口（375px宽度）→ 菜单栏高度为48px
- CSS → 包含`env(safe-area-inset)`适配
- CSS → 包含`@media (prefers-reduced-motion: reduce)`媒体查询

---

## 六、非功能需求

### 6.1 性能

| 指标 | 要求 |
| --- | --- |
| API响应时间 | 普通接口 < 500ms |
| AI回复延迟 | 用户发消息后10秒触发（可接受） |
| 消息推送延迟 | Socket.IO < 1秒 |
| 前端首屏加载 | < 3秒 |
| 数据库查询 | 单表查询 < 100ms |

### 6.2 可用性

| 指标 | 要求 |
| --- | --- |
| 服务可用性 | 99.5% |
| Docker容器 | restart: unless-stopped |
| 健康检查 | PostgreSQL/Redis 健康检查 |
| 错误处理 | 优雅处理错误，返回翻译文件错误消息 |
| 异常处理 | 非法数据不致服务器崩溃 |

### 6.3 安全

| 指标 | 要求 |
| --- | --- |
| HTTPS | 生产环境必须（Let's Encrypt） |
| JWT | 至少32字符密钥，7天过期 |
| 密码存储 | bcrypt哈希 |
| SQL注入 | 全局检测 + 参数化查询 |
| XSS | 输入清理 + CSP |
| CSRF | JWT Bearer Token（非Cookie） |
| 限流 | 三维度（IP+用户+操作类型） |
| 审计日志 | 敏感操作全记录 |

### 6.4 可维护性

| 指标 | 要求 |
| --- | --- |
| 代码规范 | 变量/函数/类名/Socket.IO事件/文件名/数据库表名/字段名/API路径全部用全中文，TypeScript强类型 |
| 翻译文件 | 用户可见文本必走翻译文件 |
| 零硬编码 | 数值/名称/路径必可配置 |
| 配置热重载 | 配置变更尽可能支持热重载 |
| 测试覆盖 | vitest单元测试 + 源码静态检查 |

---

## 七、补充说明

### 7.1 DeepSeek 接入

- 模型ID：`deepseek-v4-pro`
- **接入方式**：优先使用DeepSeek官方SDK/原生端点，以完整支持`enable_thinking`、`reasoning_effort`等思考模式参数
- API密钥：参考 `API大全.txt`
- **思考模式**：通过 `enable_thinking` 参数控制，`reasoning_effort` 参数控制思考深度；若使用OpenAI兼容端点，须先验证上述参数是否被服务端接受
- **角色扮演增强**：DeepSeek-V4支持角色沉浸模式，通过在第一轮消息末尾添加`【角色沉浸要求】`指令，使AI在思考过程中以角色第一人称进行内心独白，增强角色扮演沉浸感。项目全程使用此模式
- **自然度优化**：Prompt中应融入真实青年/大学生恋爱微信聊天风格，允许留白、犹豫、省略号、语气词、推拉和暧昧试探，避免AI味总结、说教和机械式表达
- 角色扮演通过system prompt + 角色沉浸指令实现
- 后端已有6层prompt架构 + Director-Writer双代理
- 推荐参数：`temperature=1.0, top_p=1.0`

### 7.2 微信昵称数据源

- 文件路径：`和我恋爱吧/微信昵称/昵称大全.txt`
- 格式：男：\n昵称1\n昵称2... \n女：\n昵称1\n昵称2...
- 需解析为男女两个数组，角色生成时按性别随机选取

### 7.3 秘籍系统

- 输入 `whosyourdaddy` 直接拉满四维好感度到1000（测试用）

### 7.4 代码重构原则

- 允许根据需求重构、修改、删除任何现有代码
- 所有代码必须严格遵循1.3命名规范（全中文优先，不支持中文用拼音驼峰）
- 用户可见文本必须走翻译文件，禁止硬编码
- 数值/名称/路径必须可配置，支持热重载
- 当现有代码与需求冲突时，按需求重写

### 7.5 数据库中文脚本

- **从零重写**：删除旧的拼音迁移文件（001-020.sql），从零开始用中文表名/字段名编写新的数据库初始化脚本
- **PostgreSQL 16**：默认UTF-8编码，完全支持中文表名/字段名
- **SQL语法**：中文标识符需用双引号包裹，如 `CREATE TABLE "用户" ("手机号" VARCHAR(11) PRIMARY KEY)`
- **ORM适配**：TypeORM/Prisma实体类属性名用中文，需测试ORM对中文标识符的兼容性
- **现有数据迁移**：由于是Loop Engineering重写项目，不需要保留现有数据，直接从零初始化

### 7.6 图片资源路径

- **统一存放**：所有图片资源统一存放于 `和我恋爱吧/图片` 文件夹中
- **目录结构**：
  - `图片/军师头像/军师玄锐暮头像.png` — 军师玄锐暮的头像
  - `图片/主页元素/吴昊阳终稿静态图.png` — 主页背景/装饰元素
- **引用方式**：代码中引用图片时使用相对路径，如 `图片/军师头像/军师玄锐暮头像.png`
- **新增图片**：后续新增图片资源必须放入 `图片` 文件夹的对应子目录中

node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
Thumbs.db
deploy/certbot/
可以优化的地方.txt
小米MiMo人设词.md
/tests/
test-results/
playwright-report/
*.tsbuildinfo
测试截图/
e2e/.fu-wu-zhuang-tai.json
coverage/

# 运行时日志/截图（不入库，避免总控制台 git add -A 误带）
logs_*.txt
聊天界面截图.png
backend/logs/
backend/uploads/

# 过程性备份（草地主页 bundle 分析副本的 .bak，不入版本库）
*.bak
*.bak2

# 过程性源文件（GIMP 源等设计中间产物，不入版本库；防止总控制台 git add -A 误带）
最终效果.xcf

# 3D 模型与烘焙产物：超 GitHub 单文件限制（>40MB），仅保留本地
吴昊阳模型/*.glb
吴昊阳模型/final/merged_basecolor.png
frontend/public/models/*.glb

# 3D 模型工作目录整体排除：烘焙中间产物体积过大（数百 MB），仅保留本地
吴昊阳模型/

# 前端调试快照目录：仅本地调试用，不入版本库、不进构建产物
frontend/public/grass-bg/rongHeCeShi/
frontend/public/grass-bg/wuhaoyangCeShi/
# 含真实密钥，严禁入库（项目规则第5条）
backend/.env.example

# 循环工程过程性文件，不入库
/PROGRESS.md

# FP-31 H4（第三波 Standards 轴·工作区卫生）：Playwright 取证产物目录的带后缀变体。
# 上面的 test-results/ 与 playwright-report/ 只匹配**同名**目录，复跑取证时按 FP-24c/L-10 纪律
# 换出的 `test-results-fp24d/`、`playwright-report-fpXX/` 一类变体此前漏网 ⇒ 总控制台 `git add -A`
# 会把过程产物一并入库（L-08 同族风险）。派生 config `playwright.config-fp*.ts` **不**忽略：
# 它们是可复跑门禁的组成部分，哪些属临时留档见 .agents/evidence/traces/FP-31-20260922.md
frontend/test-results*/
frontend/playwright-report*/
frontend/tests/test-results*/

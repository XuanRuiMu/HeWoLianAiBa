# 密钥管理方案（待用户选型）

## 现状

- 本地 `.env` 明文存放全部密钥，从未入库，属工作区明文风险而非已泄露。
- 生产密钥未轮换。禁止擅自轮换，需用户明确授权后执行。

## 代码侧已就绪（本 FP）

- `backend/src/config/index.ts` 的 `huoQuJiMi`：敏感变量支持 `*_FILE` 文件注入（Docker secrets / SOPS 解密输出），优先级高于同名变量。
- 已接入 `_FILE` 的变量：`DATABASE_URL`、`REDIS_URL`、`JWT_SECRET`、`DEEPSEEK_API_KEY`、`REDIS_PASSWORD`、`ALIYUN_ACCESS_KEY_ID`、`ALIYUN_ACCESS_KEY_SECRET`、`MEI_TI_QIAN_MING_MI_YAO`，多模态三路 API 密钥同理。
- `.env.example` 仅留 dev 假值。

## 待用户决策（方向性假设 A）

- 选项 1：SOPS/age 加密托管 `.env`，解密仅在部署时进行。
- 选项 2：腾讯云密钥管理系统（SSM/KMS 类服务）+ `*_FILE` 注入。
- 选项 3：维持 `.env` 明文（仅本地开发），生产走腾讯云控制台环境变量。

选定后才执行：生产密钥轮换、加密托管落地、旧密钥作废验证。

# 和我恋爱吧 错误码大全

## 用途与唯一真源

本文面向开发者与运维，用于按 `code` 判定失败语义、HTTP 状态与可重试口径。

- 码、HTTP 状态、玩家可见文案、可重试与建议重试间隔的唯一真源是 `backend/src/config/错误码注册表.ts`，玩家可见文案取自 `backend/src/config/translations.ts`。
- 本文件由 `backend/scripts/错误码文档.ts` 生成，禁止手改：改注册表或文案后重新生成，`backend/scripts/__tests__/错误码文档.test.ts` 会断言与真源逐字一致。
- 码一经发布不得改名，文案措辞可随时改写；告警、看板、工单与前端分支一律以 `code` 为锚点，禁止解析 `message`。
- 文案列是玩家原样可见的文案，本文只登记不润色；接口与请求参数口径见 `docs/API文档.md`，本文不复制接口清单。

## 响应包络

失败响应统一由 `backend/src/utils/xiangying.ts::chuangJianCuoWuXiangYing` 产出，字段类型见 `backend/src/types/index.ts::ApiXiangYing`。

|字段|类型|说明|
|---|---|---|
|`cheng_gong`|boolean|失败时恒为 `false`|
|`shu_ju`|null|失败时恒为 `null`|
|`ti_shi`|string|兼容字段，与 `message` 同源，经脱敏过滤|
|`cuo_wu_ma`|string|兼容字段，与 `code` 同值|
|`code`|string|稳定错误码，取自注册表|
|`message`|string|中文文案，取自注册表指向的翻译条目|
|`traceId`|string|追踪号，与响应头 `X-Request-Id`、`X-Trace-Id` 同值|
|`retryable`|boolean|是否可原样重试，取自注册表|
|`retryAfterMs`|number|可选，注册表给出建议间隔或调用方显式传入时才出现|
|`fieldErrors`|object|可选，仅安全白名单字段的提示，不含内部键|

外部传入的 `ti_shi` 与 `code` 都要过白名单校验才被采用，否则回落为注册表文案；数据库原文、堆栈、文件路径与凭据不会下发客户端。

## traceId 与重试规则

追踪号由 `backend/src/middleware/日志追踪.ts` 统一产出：

- 请求头 `X-Request-Id` 合法时沿用，合法性为 16 至 128 字符、首字符是字母或数字、其余限于 `[A-Za-z0-9._:-]`；缺失或非法时生成随机 UUID。
- 同一值写入响应头 `X-Request-Id`、`X-Trace-Id`、失败包络 `traceId` 与该请求全部日志的 `trace_id`。
- 玩家报障只需给出 `traceId`，运维凭此在脱敏日志中定位同一次请求；日志已移除密码、令牌、API key 与连接串凭据。

重试口径：

- `retryable=false` 不得原样重试：先修正入参、重新登录或刷新状态。
- `retryable=true` 可原样重试；带 `retryAfterMs` 时必须等待该毫秒数，退避以服务端返回值为准。
- 可重试标志逐码固定在注册表，不由状态码推导：限流类为 `true`；参数、认证、权限、资源、冲突、启动校验与未知内部错误为 `false`；上游与依赖不可用为 `true`。
- 服务端不代客户端重试；业务写操作重试前须确认上一次是否已落库。

## 分类码表

分类只用于检索与分派，不改变任何运行时行为：先按注册表的界面字段归入启动、战绩、角色生成与管理员四类，其余按码前缀归入依赖、数据、网络、认证与访问控制，剩余通用码归入业务。

### 依赖

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|SERVICE_UNAVAILABLE|503|服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|
|DEPENDENCY_POSTGRES_UNAVAILABLE|503|数据服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|
|DEPENDENCY_REDIS_UNAVAILABLE|503|数据服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|
|DEPENDENCIES_UNAVAILABLE|503|服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|

### 认证

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|AUTHENTICATION_REQUIRED|401|请先登录后再操作|否|—|
|AUTH_TOKEN_INVALID|401|登录状态已失效，请重新登录|否|—|
|AUTH_SMS_RATE_LIMITED|429|验证码发送过于频繁，请等一会儿再发送一次|是|60000|
|AUTH_SMS_SEND_FAILED|503|验证码服务暂时不可用，本次没有发送成功，请重试当前操作|是|—|
|AUTH_REGISTRATION_CLOSED|429|体验内测名额已满，暂不开放新注册|否|—|
|AUTH_ACCOUNT_ALREADY_EXISTS|409|该手机号已注册，请直接登录，或换一个手机号注册|否|—|
|AUTH_REGISTRATION_FAILED|503|注册服务暂时不可用，本次没有注册成功，请重试当前操作|是|—|
|AUTH_INVALID_CREDENTIALS|401|账号或密码错误|否|—|
|AUTH_REFRESH_TOKEN_INVALID|401|登录状态已失效，请重新登录|否|—|
|AUTH_PASSWORD_CHANGE_FAILED|400|密码没有修改成功，请检查输入后重新提交|否|—|
|AUTH_USERNAME_CHANGE_FAILED|409|用户名没有改成功，现在还是原来的用户名|否|—|
|AUTH_DEFAULT_GENDER_INVALID|400|性别选择不合法，请重新选择男或女|否|—|
|AUTH_ACCOUNT_DELETION_FAILED|500|账号没有注销成功，账号仍然可用|否|—|

### 数据

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|DATABASE_ERROR|503|数据服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|

### 业务

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|REQUEST_PARAMETER_INVALID|400|提交的内容不符合要求，请检查后重新提交|否|—|
|REQUEST_MISSING_PARAMETER|400|有必填项没有填，请检查后重新提交|否|—|
|REQUEST_BODY_INVALID|400|提交的内容不符合要求，请检查后重新提交|否|—|
|PAYLOAD_TOO_LARGE|413|内容太长，超过了可以提交的上限|否|—|
|RESOURCE_NOT_FOUND|404|没有找到要访问的内容|否|—|
|METHOD_NOT_ALLOWED|405|这个操作不被支持|否|—|
|RESOURCE_CONFLICT|409|数据已发生变化，本次没有改动，请刷新后重新操作|否|—|
|RATE_LIMITED|429|操作过于频繁，请等一会儿再操作一次|是|1000|
|INTERNAL_ERROR|500|这次操作没有完成，当前内容可能仍是上次结果|否|—|

### 网络

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|UPSTREAM_NETWORK_ERROR|502|服务暂时不可用，本次操作没有保存，请重试当前操作|是|—|
|UPSTREAM_TIMEOUT|504|服务响应超时，本次操作没有保存，请重试当前操作|是|—|

### Docker 启动

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|DOCKER_STARTUP_MIGRATION_SCRIPT_MISSING|503|启动文件不完整|否|—|
|DOCKER_STARTUP_MIGRATION_FAILED|503|数据库升级未完成，启动已中止|是|—|
|DOCKER_STARTUP_REDIS_TLS_INVALID|503|缓存连接安全配置无效，启动已中止|否|—|
|DOCKER_STARTUP_REDIS_UNAVAILABLE|503|缓存服务暂不可用，启动已中止|是|—|
|DOCKER_STARTUP_POSTGRES_UNAVAILABLE|503|数据服务暂不可用，启动已中止|是|—|
|DOCKER_STARTUP_MODERATION_RESOURCE_UNAVAILABLE|503|审核资源不可用，启动已中止|否|—|

### 战绩

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|ZHAN_JI_FEN_LEI_MING_CHENG_WU_XIAO|400|分类名称不能为空|否|—|
|ZHAN_JI_FEN_LEI_MING_CHENG_CHANG|400|分类名称不能超过20个字|否|—|
|ZHAN_JI_FEN_LEI_MING_CHENG_CHONG_FU|409|分类名称已存在，请换一个名称|否|—|
|ZHAN_JI_FEN_LEI_BIAN_GENG|409|分类已发生变化，本次没有改动，请刷新后重新操作|否|—|
|ZHAN_JI_MO_REN_FEN_LEI_BU_NENG_SHAN_CHU|409|默认分类不能删除|否|—|
|ZHAN_JI_FEN_LEI_BU_CUN_ZAI|404|战绩分类已不存在，请刷新后重新选择|否|—|
|ZHAN_JI_DANG_AN_BU_CUN_ZAI|404|这条战绩已不存在，请刷新后重新选择|否|—|
|ZHAN_JI_DANG_AN_BU_SHU_YU_FEN_LEI|409|战绩记录不属于该分类|否|—|
|ZHAN_JI_BU_NENG_YIDONG_DAO_DANG_QIAN_FEN_LEI|400|不能移动到当前分类|否|—|
|ZHAN_JI_PAI_XU_ID_CHONG_FU|400|同一条战绩在排序里出现了两次，请重新排序后再保存|否|—|
|ZHAN_JI_PAI_XU_JI_LU_BU_WU_ZHEN|409|排序必须包含该分类全部可见战绩|否|—|
|ZHAN_JI_SHU_JU_BIAN_HUA|409|分类已发生变化，本次没有改动，请刷新后重新操作|否|—|
|ZHAN_JI_FEN_LEI_BAN_BEN_BU_HE_FA|400|分类已发生变化，本次没有改动，请刷新后重新操作|否|—|
|ZHAN_JI_PAI_XU_CAN_SHU_BU_HE_FA|400|排序内容不合法，请重新排序后再保存|否|—|

### 角色生成

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|ROLE_GENERATION_INPUT_INVALID|400|角色信息不合法，请重新生成|否|—|
|ROLE_GENERATION_INITIALIZATION_FAILED|500|角色没有生成成功，也没有覆盖原来的角色|否|—|
|ROLE_GENERATION_MODEL_UNAVAILABLE|503|角色生成服务暂时不可用，请重试当前操作|是|—|
|ROLE_GENERATION_MODEL_CALL_FAILED|502|角色生成服务暂时不可用，请重试当前操作|是|—|
|ROLE_GENERATION_RESPONSE_INVALID|502|角色生成结果异常，请重试当前操作|是|—|
|ROLE_GENERATION_PERSISTENCE_FAILED|503|角色没有保存成功，本次修改未生效，请重试当前操作|是|—|

### 管理

|码|HTTP|玩家可见文案|可重试|retryAfterMs|
|---|---|---|---|---|
|PERMISSION_DENIED|403|无管理权限，请联系超级管理员确认你的角色|否|—|
|ORIGIN_DENIED|403|当前访问地址不被允许|否|—|

## 旧码兼容映射

旧码映射由 `backend/src/config/错误码注册表.ts::zhuanHuanJiuDaiMa` 统一执行：已是注册码的原样返回，旧码按下表转换，都未命中时返回 `null`；只拿到 HTTP 状态的旧调用方由同文件的 `daiMaHuoQuZhuangTaiMa` 兜底转换。

|旧码|现码|
|---|---|
|CAN_SHU_CUO_WU|REQUEST_PARAMETER_INVALID|
|WEI_SHOU_QUAN|AUTHENTICATION_REQUIRED|
|LING_PAI_WU_XIAO|AUTH_TOKEN_INVALID|
|WU_GUAN_LI_QUAN_XIAN|PERMISSION_DENIED|
|WEI_ZHAO_DAO|RESOURCE_NOT_FOUND|

注册表仍导出旧码常量 `JIU_DAI_MA` 供过渡期调用方识别，其两个取值都已并入上表映射。

## 运维处置原则

- 先看 `code` 再看 `message`：`message` 是玩家可见文案，随时可能改写，不能作为归类依据。
- 排查入口是 `traceId`：凭它取同一请求的脱敏日志，日志里的内部原因不外发、不写入本文。
- 依赖、网络与启动类码反映基础设施状态：先确认依赖存活与网络可达，再判应用逻辑，禁止用重试掩盖依赖故障。
- 启动类码由启动校验与镜像入口脚本输出并中止进程，不会出现在失败响应体里；容器不就绪时先看启动日志末尾的码。
- 健康检查按 `DEPENDENCY_*` 与 `DEPENDENCIES_UNAVAILABLE` 判定具体依赖，两者同时出现即为整体依赖不可用。
- 认证与权限类码不进重试：先判定是凭证失效、来源被拒还是能力位不足，再决定重新登录还是修正授权。
- 战绩与角色生成类码是业务阶段语义：阶段码只说明失败落在哪一步，不得改写为通用内部错误，也不得用空结果伪装成功。

## 生成与校验

```bash
npx ts-node scripts/错误码文档.ts
```

校验随 `npm test` 执行：重新生成结果须与本文件逐字一致；每个注册码恰好一行且状态与可重试标志与注册表一致；无重复、遗漏与未注册码；文件为 UTF-8 无 BOM 且不含敏感样例。

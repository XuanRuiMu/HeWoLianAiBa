# 循环工程进度追踪：和我恋爱吧 6 项根因修复

## 元信息

| 字段     | 值                          |
| -------- | --------------------------- |
| 任务     | 6 项根因级修复（性别人设/滚动/军师样式/开场白/表情动画/账号菜单） |
| 状态     | 进行中                      |
| 循环计数 | 0                           |

## 目标

按用户 6 条实测反馈，对「和我恋爱吧」做根因级修复（禁止简单修补），使代码与需求文档完全一致；清理冗余；真实浏览器视角测试；最终重建镜像并交付。

## 停止条件（必须可机器验证）

- [ ] 所有 6 个功能点状态=已完成
- [ ] 前端 `npm run build` 成功、lint 零报错
- [ ] 后端 `npm run build` 成功、lint 零报错
- [ ] 前端 `npm run test`、后端 `npm run test` 全量通过（强制重跑）
- [ ] 真实浏览器截图验证 6 项修复的 UI 表现与后端日志无异常
- [ ] 需求文档与代码在 6 项上一致（冲突以用户口径为准，文档同步改）

## 熔断上限

| 类型           | 上限 | 当前 | 状态 |
| -------------- | ---- | ---- | ---- |
| 总循环次数     | 30   | 0    | 正常 |
| 单问题修复次数 | 5    | 0    | 正常 |

## 范围边界

### 做什么

- FP-01 对象性别→AI人设 根因修复（性别必须正确流入人设生成，禁止人设性别错乱）
- FP-02 聊天滚动位置保持（切后台再回前台不强制回底，除非用户本就在底部）
- FP-03 军师「指导中」样式统一为黄底（删除灰底冗余样式）
- FP-04 开场白出现频率约 50%（禁止按 E/I 强制，靠画像决策偏置）
- FP-05 表情菜单三阶段动画连贯（open/static/collapse 首尾帧一致）
- FP-06 顶端菜单：账号设置（改用户名/改密码）+ 默认性别设置

### 不做什么（防过度烘焙）

- 不改动与 6 项无关的业务逻辑
- 不新增功能点以外的 UI/接口
- 不重写已稳定的模块（仅做根因修复 + 冗余清理）

### 禁止触碰

- 安全/合规中间件（IP封禁、限流、认证签名）除非 FP 明确要求
- 需求文档中未涉及 6 项的既有描述

## 待处理功能点

| ID    | 描述 | 验收标准 | 依赖 | 状态 | 循环 |
| ----- | ---- | -------- | ---- | ---- | ---- |
| FP-01 | 对象性别→AI人设根因修复【根因：聊天系统提示词(gouJianDiErCeng/gouJianDiLiuCeng/gouJianDirectorPrompt)从未注入角色性别，DB性别正确但AI默认女】 | 生成男性对象时 AI 人设为男；gender 贯穿 wizard→store→后端→Prompt→DB；无性别错乱 | 无 | 已完成 | 0 |
| FP-02 | 聊天滚动位置保持 | 切后台回前台不强制回底；仅当用户原本在底部时才回底 | 无 | 已完成 | 0 |
| FP-03 | 军师指导中样式统一 | 指导中恒为黄底，删除灰底分支，无两套样式 | 无 | 已完成 | 0 |
| FP-04 | 开场白频率约50%【根因：shengChengKaiChangBai 的 AI 路径无"是否发送"门控，模型过度主动导致绝大多数角色发开场白；jiangJi 内部 0.5 概率与 AI 路径不一致，两处决策点冗余】 | 整体约半数角色有开场白；统一单一概率门控(0.5)；无 E/I 强制逻辑 | FP-01 | 已完成 | 0 |
| FP-05 | 表情菜单动画连贯 | 三阶段首尾帧一致，无跳变 | 无 | 已完成 | 0 |
| FP-06 | 账号设置+默认性别 | 菜单合并为账号设置(改用户名/改密码)；账号设置含默认性别，仅普通模式第一步默认选中 | 无 | 已完成 | 0 |

## 已完成（仅保留一行摘要）

- FP-01 修复：Prompt构建器 层二/层六/Director 演员人设 注入角色性别（xing_bie），消除 AI 默认女。
- FP-02 修复：聊天页 input @focus 滚动到底加 `yiDingZaiDiBu` 守卫，切后台回前台不再强拉。
- FP-03 修复：军师「指导中」统一为金黄底（删除灰色分支），点击与离页重进渲染一致。
- FP-04 修复：AI配置新增 `kaiChangBaiFaSongGaiLv:0.5` 单一门控；角色生成调用点按概率决定是否生成；删除 jiangJi 内部冗余 0.5 概率；无 E/I 强制。
- FP-05 修复：emoji 面板 open/collapse 过渡 `overflow:hidden`→`overflow-y:auto`，滚动条槽一致，消除表情水平跳变。
- FP-06 修复：用户下拉合并为「账号设置」分组（修改用户名/修改密码/设置默认性别）+ 退出登录；过往战绩移至「更多」菜单；新增默认性别（DB 用户.默认性别 + setMoRenXingBie 服务 + POST /认证/设置默认性别 + 向导步骤1预选）。同步清理 管理员.ts 中与 认证.ts 重复的 yingSheYongHu 映射器（统一复用）。

## 当前决策（只保留影响待处理项的决策）

| 时间 | 决策 | 影响范围 |
| ---- | ---- | -------- |
| 2026-08-11 | 默认性别存储为前端 XingBie 值（male/female），后端归一化接受 nan/nv/男/女→male/female | FP-06 全链路 |
| 2026-08-11 | 用户下拉「过往战绩」按用户口径移出，置于「更多」菜单（☰），保持 FP-13 可达性；需求文档同步修改 | FP-06 / FP-13 |
| 2026-08-11 | 持久化 DB 体积不变：`用户.默认性别` 经 ALTER 直接加列至运行态 postgres（init 脚本仅影响全新初始化） | FP-06 部署 |

## 阻塞与遗留问题

| ID  | 问题描述 | 建议处理方式 |
| --- | -------- | ------------ |
| 测试基线 | 前后端单测各存在预存失败，均与本次 6 项修复无关：后端 7 失败集中在 AI引擎/AI模型配置/军师指导/复盘 的 temperature·max_tokens·siKaoMoShi 取值与军师 Prompt 文案（AI 参数配置漂移）；前端 4 失败集中在 过往战绩 拖拽排序持久化。两类均非 FP-01~FP-06 改动范围，未引入新回归 | 留给后续专项治理，不在本循环工程范围内 |
| 需求文档 | 需求文档无「默认性别」独立 FP 章节，已按用户口径在菜单规格（第1330行）补充 账号设置 分组与 默认性别 语义说明；代码与文档现已一致 | 已处理 |

# 前端环境变量示例
# Vite 仅把以 VITE_ 为前缀的变量注入客户端代码（import.meta.env.VITE_XXX）。
# 下列变量均为构建期/开发期读取（vite.config.ts 经 process.env 取），不会打包进浏览器代码。

# 开发服务器 /api 与 /socket.io 的反向代理目标。
# 默认 http://localhost:3000；docker compose 把后端发布在 127.0.0.1:3001，
# 用容器化后端做本地联调时必须显式指向它，否则代理静默失败、前端拿不到任何接口数据。
# VITE_API_PROXY_TARGET=http://127.0.0.1:3001
